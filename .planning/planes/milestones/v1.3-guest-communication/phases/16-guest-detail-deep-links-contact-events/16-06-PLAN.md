---
phase: 16-guest-detail-deep-links-contact-events
plan: 6
type: execute
wave: 4
depends_on: [16-00, 16-01, 16-02, 16-03, 16-04, 16-05]
files_modified:
  - .planning/phases/16-guest-detail-deep-links-contact-events/16-MANUAL-QA-CHECKLIST.md
  - .planning/phases/16-guest-detail-deep-links-contact-events/16-CLOSEOUT.md
  - .planning/V1.3-MILESTONE-CLOSEOUT.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [GCC-06, GCC-07, GCC-08, GCC-09, GCC-10, GCC-11, GCC-12]
must_haves:
  truths:
    - "Full backend + frontend test suites are green simultaneously"
    - "Zero TypeScript errors in both workspaces"
    - "Zero raw hex colors and zero forbidden palette classes in any Phase 16 file"
    - "Manual QA checklist documented (8 scenarios) with human sign-off"
    - "Phase 16 CLOSEOUT.md exists and is accurate"
    - "V1.3 MILESTONE CLOSEOUT.md summarises Phases 15 + 16, 12 GCC-IDs verified, carry-forward to v1.4 listed"
    - "STATE.md and ROADMAP.md reflect Phase 16 complete + v1.3 milestone closed"
  artifacts:
    - path: ".planning/phases/16-guest-detail-deep-links-contact-events/16-MANUAL-QA-CHECKLIST.md"
      provides: "8 manual scenarios with pass/fail and observation columns"
    - path: ".planning/phases/16-guest-detail-deep-links-contact-events/16-CLOSEOUT.md"
      provides: "Phase 16 deliverables summary + GCC-06..12 verification table"
    - path: ".planning/V1.3-MILESTONE-CLOSEOUT.md"
      provides: "v1.3 milestone wrap-up — 12 GCC-IDs, both phases, carry-forward"
  key_links:
    - from: ".planning/STATE.md"
      to: "milestone: v1.3 status completed"
      via: "frontmatter update"
      pattern: "milestone:\\s*v1\\.3"
    - from: ".planning/ROADMAP.md"
      to: "Phase 16 row marked Complete with plans count"
      via: "table update"
      pattern: "16\\.\\s*Guest Detail.*Complete"
---

<objective>
Phase 16 verification + v1.3 milestone closeout. Full regression on both workspaces, zero-hex / zero-palette gates on new code, 8-scenario manual QA with human sign-off, Phase 16 CLOSEOUT, **V1.3 MILESTONE CLOSEOUT**, and STATE/ROADMAP updates. Verifies all 7 phase requirements: GCC-06..12.

Purpose: Confirm v1.3 (Guest Communication Hub — 12 GCC-IDs across Phases 15+16) is fully shipped before v1.4.
Output: 5 documentation files updated; STATE shows v1.3 complete; ROADMAP updated; human sign-off captured.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-guest-detail-deep-links-contact-events/16-CONTEXT.md
@.planning/phases/16-guest-detail-deep-links-contact-events/16-00-SUMMARY.md
@.planning/phases/16-guest-detail-deep-links-contact-events/16-01-SUMMARY.md
@.planning/phases/16-guest-detail-deep-links-contact-events/16-02-SUMMARY.md
@.planning/phases/16-guest-detail-deep-links-contact-events/16-03-SUMMARY.md
@.planning/phases/16-guest-detail-deep-links-contact-events/16-04-SUMMARY.md
@.planning/phases/16-guest-detail-deep-links-contact-events/16-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full regression — backend + frontend test suites + tsc + zero-hex / zero-palette gates</name>
  <read_first>
    - All prior plan SUMMARYs to confirm each task green
    - 16-RESEARCH.md "Sampling Rate" → Phase gate definition
  </read_first>
  <files>(verification only; outputs captured in Task 3 CLOSEOUT)</files>
  <action>
    Execute all gates and capture exit codes + counts:

    ```bash
    # Backend regression
    cd apps/api && npx tsc --noEmit                   # MUST be exit 0
    cd apps/api && npx prisma validate                # MUST be exit 0
    cd apps/api && npx prisma migrate status          # MUST show 20260527000000_phase16_guest_contact_events applied
    cd apps/api && npx vitest run                     # MUST be exit 0; capture test count

    # Frontend regression
    cd apps/web && npx tsc --noEmit                   # MUST be exit 0
    cd apps/web && npx vitest run                     # MUST be exit 0; capture test count

    # Zero-hex gate (NEW Phase 16 files only)
    rg "#[0-9a-fA-F]{3,6}" \
      apps/web/src/features/guests/GuestDetailPage.tsx \
      apps/web/src/features/guests/components/ContactButtons.tsx \
      apps/web/src/features/guests/hooks/useGuestContactEvents.ts \
      apps/web/src/lib/socket.ts
    # MUST return 0 (excluding comments — review manually if matches)

    # Zero-palette gate (NEW Phase 16 files only)
    rg "text-(red|blue|green|yellow|gray|slate|zinc|stone)-\d{3}|bg-(red|blue|green|yellow|gray|slate|zinc|stone)-\d{3}" \
      apps/web/src/features/guests/GuestDetailPage.tsx \
      apps/web/src/features/guests/components/ContactButtons.tsx
    # MUST return 0 matches
    ```

    If any check fails, stop and surface to user — do NOT proceed to closeout until green. Record exit codes, test counts, grep results — they go into CLOSEOUT.
  </action>
  <verify>
    <automated>cd apps/api &amp;&amp; npx tsc --noEmit &amp;&amp; npx vitest run &amp;&amp; cd ../web &amp;&amp; npx tsc --noEmit &amp;&amp; npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - apps/api: tsc exit 0, prisma validate exit 0, migrate status shows phase16 applied, vitest exit 0
    - apps/web: tsc exit 0, vitest exit 0
    - Both grep gates return 0 matches in Phase 16 new files (excluding comments)
    - Test counts captured for CLOSEOUT
  </acceptance_criteria>
  <done>All automated gates green; outputs recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual QA — 8 scenarios with human sign-off</name>
  <files>.planning/phases/16-guest-detail-deep-links-contact-events/16-MANUAL-QA-CHECKLIST.md</files>
  <what-built>
    Phase 16 end-to-end: new staff route /guests/:id, ContactButtons component, real-time Socket.io updates, GuestsPage extension with Último contacto column. Full v1.3 deliverable.
  </what-built>
  <action>
    Create `.planning/phases/16-guest-detail-deep-links-contact-events/16-MANUAL-QA-CHECKLIST.md` with the 8-scenario table below. Then walk the human reviewer through each scenario and mark Pass/Fail with observations. Block phase closure on any failure.
  </action>
  <how-to-verify>
    File `.planning/phases/16-guest-detail-deep-links-contact-events/16-MANUAL-QA-CHECKLIST.md` with this exact table (mark each Pass/Fail and record observations):

    | # | Scenario | Steps | Expected | Pass/Fail | Notes |
    |---|----------|-------|----------|-----------|-------|
    | 1 | POST event success (HTTP) | Login as RECEPTION; `curl -X POST http://localhost:3000/api/guests/{validId}/contact-events -H "Authorization: Bearer {jwt}" -d '{"method":"CALL"}'` | 201 + event row with staffUser.name joined | | |
    | 2 | 2-tab realtime push | 2 browsers as Staff A + Staff B logged in; both navigate to `/guests/{X}`; A clicks WhatsApp button | A sees "✓ WhatsApp registrado" + wa.me opens in new tab; B sees "{A.name} inició contacto por WhatsApp con este huésped" toast within 1s; B Últimos contactos refreshes | | |
    | 3 | Button disabled state | Visit `/guests/{id}` for a guest with phone=null + email=null + whatsappNumber=null | All 3 ContactButtons render disabled (visually grayed, cannot click) | | |
    | 4 | Navigation from list | Visit `/guests`, click any row | Browser navigates to `/guests/:id` (NOT drawer opens) | | |
    | 5 | Último contacto column | After step 1, visit `/guests` | Guest's row shows "hace menos de un minuto" (or similar Spanish relative time) in Último contacto column | | |
    | 6 | Nunca state | Visit `/guests`; find a guest with NO contact events | Their Último contacto column shows "Nunca" | | |
    | 7 | Edit inline | Visit `/guests/:id`, click Editar, change `dietaryRestrictions`, Save | Form closes, info section shows new value, no full page reload | | |
    | 8 | Deep link encoding | Click WhatsApp on a guest whose name contains an accent (e.g., "José María") | wa.me URL contains firstName encoded correctly ("José" → "Jos%C3%A9"), no broken URL | | |

    Plus final row: **Sign-off**: tester name + date + overall pass/fail.

    ASK the human reviewer to walk through and mark each row. Surface any failure as a blocker — Phase 16 cannot close until all 8 pass.
  </how-to-verify>
  <verify>All 8 scenarios marked Pass by human reviewer with date + tester name on sign-off row</verify>
  <done>16-MANUAL-QA-CHECKLIST.md committed with full sign-off; all scenarios Pass.</done>
  <resume-signal>Type "approved" once all 8 rows are marked Pass with notes, or describe failures.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Phase 16 CLOSEOUT + V1.3 MILESTONE CLOSEOUT + STATE/ROADMAP updates</name>
  <read_first>
    - All 6 prior plan SUMMARYs (16-00 through 16-05)
    - .planning/STATE.md frontmatter
    - .planning/ROADMAP.md Phase 16 entry + v1.3 milestone block
    - .planning/REQUIREMENTS.md → GCC-01..12 (full v1.3 scope)
    - .planning/phases/15-extended-contact-capture/15-CLOSEOUT.md (Phase 15 reference for milestone summary)
  </read_first>
  <files>
    .planning/phases/16-guest-detail-deep-links-contact-events/16-CLOSEOUT.md,
    .planning/V1.3-MILESTONE-CLOSEOUT.md,
    .planning/STATE.md,
    .planning/ROADMAP.md
  </files>
  <action>
    **3a. `16-CLOSEOUT.md`** — Phase 16 closeout. Include:
    - Phase header (name, milestone, completed date)
    - Plans completed table (16-00..16-06) with task counts + key files
    - GCC-06..12 verification table (each requirement → which plan delivered + acceptance evidence)
    - Test counts (backend + frontend, before + after Phase 16)
    - Files inventory (delta — new + modified)
    - Key decisions made during execution (from each SUMMARY's "Decisions" section)
    - Pitfalls hit + how resolved (research traps that actually fired)
    - Carry-forward to v1.4 (deferred items from CONTEXT.md §Deferred Ideas)
    - Manual QA outcome (link to 16-MANUAL-QA-CHECKLIST.md)

    **3b. `V1.3-MILESTONE-CLOSEOUT.md`** — at `.planning/` root. Mirror format from prior milestone closeouts (look for existing `.planning/V1.*.md` files; if a v1.2 closeout exists, use it as template). Include:
    - Milestone name: "v1.3 — Guest Communication Hub"
    - Duration: start date (Phase 15 start) → end date (today)
    - Phases shipped: 15 (Extended Contact Capture, 4 plans) + 16 (Guest Detail + Deep Links + Contact Events, 7 plans = 16-00..16-06)
    - Requirements closed: GCC-01..12 (12 IDs) — full table with brief description per ID
    - Key technical artifacts:
      - 2 Prisma migrations: `20260526000000_phase15_extended_guest_contact`, `20260527000000_phase16_guest_contact_events`
      - New Prisma model: `GuestContactEvent` + `ContactMethod` enum
      - 6 new Guest scalar columns (preferredLanguage, contactPreference, whatsappNumber, marketingConsent, dietaryRestrictions, specialRequests)
      - 1 new NestJS module: `guest-contact/` (controller + service + gateway + repo + DTOs + tests)
      - 2 new frontend deps: `sonner`, `date-fns`
      - 1 new staff route: `/guests/:id` with GuestDetailPage
      - 1 new reusable component: `<ContactButtons />`
      - 1 new hook: `useGuestContactEvents`
      - 1 new shared utility: `apps/web/src/lib/socket.ts`
    - Velocity stats (from STATE.md table)
    - Carry-forward to v1.4 (full Deferred list from both phase contexts):
      - WhatsApp Business API real (templated outbound)
      - Email templates library expansion (welcome / pre-arrival / thank-you)
      - Pre-arrival reminder cron (1 day before check-in)
      - Notes field on contact events (post-call summary)
      - Contact log filters / search
      - Soft-delete on contact events for GDPR
      - Bulk contact action
      - Contact event analytics dashboard
      - Photo upload in guest detail (v2)
      - Pasarela de pago (explicit v1.3 deferral noted in ROADMAP)
      - useHousekeepingSocket migration to shared singleton (carried over from 16-03)
    - Sign-off line (date + completed status)

    **3c. `STATE.md`** updates:
    - Frontmatter: `milestone: v1.3`, `status: completed`, `last_updated: <today>`, `last_activity: <today> — Phase 16 closeout + v1.3 milestone closed`, increment `completed_phases` (v1.3 phases done = 2; total counts updated), `completed_plans` += 7 (16-00..16-06)
    - "Current Position" block: set Phase to "v1.4 (next)" or similar; Plan to "—"
    - "Last activity" updated
    - Append to "Decisions" the 5 most material Phase 16 decisions (one per SUMMARY's decisions section, or curated list)
    - Append to "v1.3 Carry-Forward" block at the bottom (similar pattern to existing v1.0 / v1.1 / v1.2 carry-forward blocks)

    **3d. `ROADMAP.md`** updates:
    - Phase 16 row in the Progress table at bottom: status "Complete", completed "<today>", plans `7/7`
    - Phase 16 section header: change "Plans: TBD" to "Plans: 7 plans" + list all 7 with [x] marks pointing to actual filenames (16-00-PLAN.md ... 16-06-PLAN.md)
    - Add milestone v1.3 status note: "Milestone v1.3 — Guest Communication Hub: ✓ COMPLETE — 12 GCC-IDs shipped across Phases 15+16"

    **3e. Commit** (final phase commit, includes all 4 doc files):
    ```bash
    git add .planning/phases/16-guest-detail-deep-links-contact-events/16-MANUAL-QA-CHECKLIST.md \
            .planning/phases/16-guest-detail-deep-links-contact-events/16-CLOSEOUT.md \
            .planning/V1.3-MILESTONE-CLOSEOUT.md \
            .planning/STATE.md \
            .planning/ROADMAP.md
    git commit -m "docs(16-06): Phase 16 closeout + v1.3 milestone closed (12 GCC-IDs shipped)"
    ```

    **Threat model on documentation:** No new attack surface; documentation is informational. The milestone closeout MUST list pasarela de pago deferral explicitly (locked at v1.3 ROADMAP scope) so the next planner does not assume payment integration was attempted.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); ['.planning/phases/16-guest-detail-deep-links-contact-events/16-CLOSEOUT.md','.planning/V1.3-MILESTONE-CLOSEOUT.md'].forEach(p=>{if(!fs.existsSync(p))process.exit(1); if(fs.statSync(p).size<500)process.exit(2)})"</automated>
  </verify>
  <acceptance_criteria>
    - `16-CLOSEOUT.md` exists, > 500 bytes, references all 7 plans + 7 GCC-IDs (06..12)
    - `V1.3-MILESTONE-CLOSEOUT.md` exists, > 500 bytes, lists all 12 GCC-IDs (01..12), references both Phase 15 + Phase 16
    - STATE.md frontmatter `milestone: v1.3` `status: completed`
    - ROADMAP.md Phase 16 row shows Complete `7/7`
    - ROADMAP.md Phase 16 section header: `Plans: 7 plans` with all 7 listed [x]
    - Commit created with the 5-file changeset
    - `cd apps/api && npx tsc --noEmit && cd ../web && npx tsc --noEmit` still green
    - `cd apps/api && npx vitest run && cd ../web && npx vitest run` still green (no regression from doc updates)
  </acceptance_criteria>
  <done>Phase 16 documented as Complete; v1.3 milestone closed; STATE + ROADMAP synchronized; final commit landed.</done>
</task>

</tasks>

<verification>
1. Both workspaces: tsc + vitest exit 0
2. Migration applied, prisma validates
3. Zero-hex + zero-palette gates green on new code
4. Manual QA: 8 scenarios all Pass with human sign-off
5. STATE.md frontmatter shows `milestone: v1.3, status: completed`
6. ROADMAP.md Phase 16 + v1.3 marked complete
7. CLOSEOUT files exist and reference all deliverables
</verification>

<success_criteria>
- [ ] Full regression green (backend + frontend)
- [ ] Zero-hex + zero-palette gates green
- [ ] 8 Manual QA scenarios signed off
- [ ] 16-CLOSEOUT.md written (references GCC-06..12)
- [ ] V1.3-MILESTONE-CLOSEOUT.md written (references GCC-01..12)
- [ ] STATE.md updated (milestone completed)
- [ ] ROADMAP.md updated (Phase 16 + v1.3 closed)
- [ ] Final commit `docs(16-06): Phase 16 closeout + v1.3 milestone closed`
</success_criteria>

<output>
This is the LAST plan of v1.3. No 16-06-SUMMARY required — the closeout files ARE the summary.

After this plan completes:
- Run `/gsd-complete-milestone v1.3` to archive
- Plan v1.4 (carry-forward list is in V1.3-MILESTONE-CLOSEOUT.md)
</output>
