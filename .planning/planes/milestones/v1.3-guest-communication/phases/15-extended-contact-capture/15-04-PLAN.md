---
phase: 15-extended-contact-capture
plan: 04
type: execute
wave: 3
depends_on: [15-01, 15-02, 15-03]
files_modified:
  - .planning/phases/15-extended-contact-capture/MANUAL-QA-CHECKLIST.md
  - .planning/phases/15-extended-contact-capture/15-SUMMARY.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [GCC-01, GCC-02, GCC-03, GCC-04, GCC-05]
must_haves:
  truths:
    - "Full TypeScript regression green: apps/api tsc --noEmit AND apps/web tsc --noEmit BOTH exit 0"
    - "Full Vitest regression green: apps/api and apps/web suites pass with NO new failures introduced by Phase 15"
    - "Zero hex/raw-palette leak in the NEW sections of BookingFormPage.tsx (token discipline preserved)"
    - "MANUAL-QA-CHECKLIST.md exists with 7 documented scenarios + outcomes"
    - "Phase 15 SUMMARY documents shipped REQ-IDs (GCC-01..05) + deferred items + carry-forward decisions"
    - "STATE.md reflects Phase 15 completion (1/2 phases of v1.3 done)"
    - "ROADMAP.md Phase 15 entry shows [x] with plans count + completion date"
    - "REQUIREMENTS.md GCC-01..05 marked [x] with phase + date"
  artifacts:
    - path: ".planning/phases/15-extended-contact-capture/MANUAL-QA-CHECKLIST.md"
      provides: "7-scenario QA checklist with outcomes"
      contains: "Submit con todos los campos opcionales"
    - path: ".planning/phases/15-extended-contact-capture/15-SUMMARY.md"
      provides: "Phase 15 closeout summary"
      contains: "GCC-01"
  key_links:
    - from: "Phase closeout"
      to: "STATE.md + ROADMAP.md + REQUIREMENTS.md"
      via: "atomic updates"
      pattern: "GCC-01..05.*\\[x\\]"
---

<objective>
Cerrar Phase 15 con un gate de regresión completo, una checklist de QA manual ejecutada, y la actualización atómica de los 3 artefactos canónicos (STATE/ROADMAP/REQUIREMENTS). Este plan es el "fin de fase" — verifica que Waves 1+2 no introdujeron regresiones, captura el conocimiento operativo en QA, y deja v1.3 listo para Phase 16.

Purpose: Sin este plan, el equipo no tiene una señal clara de "Phase 15 está terminado y verificado". Las verificaciones distribuidas en planes anteriores son sandboxed; este plan las integra y firma.

Output: Checklist QA (markdown), SUMMARY de fase, y los 3 artefactos planning actualizados.
</objective>

<execution_context>
@/Users/oscarpeinado/.claude/get-shit-done/workflows/execute-plan.md
@/Users/oscarpeinado/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-extended-contact-capture/15-CONTEXT.md
@.planning/phases/15-extended-contact-capture/15-01-SUMMARY.md
@.planning/phases/15-extended-contact-capture/15-02-SUMMARY.md
@.planning/phases/15-extended-contact-capture/15-03-SUMMARY.md
</context>

<threat_model>
- **Regression silent leak**: Phase 15 touched 3 backend modules + 1 frontend page. A pre-existing test for `public-booking` might silently break because the txResult type widened. tsc + vitest are the safety net — both must exit 0.
- **QA shortcut temptation**: Some scenarios are tedious (sending a real email to verify the preferences section). Document EACH scenario with expected outcome AND actual outcome — empty "TODO" entries are unacceptable.
- **Documentation drift**: STATE/ROADMAP/REQUIREMENTS must be updated atomically in the SAME commit as the SUMMARY. Otherwise the next session reads stale state and may re-plan.
- **Phase boundary leak**: Resist the urge to start implementing Phase 16 (guest detail page) inside this plan. If something feels like it belongs to GCC-06..12, document it as a Phase 16 carry-forward note in the SUMMARY, do NOT execute.
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run full regression suite (tsc + vitest both apps) and capture results</name>
  <read_first>
    - @.planning/phases/15-extended-contact-capture/15-01-SUMMARY.md
    - @.planning/phases/15-extended-contact-capture/15-02-SUMMARY.md
    - @.planning/phases/15-extended-contact-capture/15-03-SUMMARY.md
    - @CLAUDE.md §Confirmed Decisions (tsc + vitest are the regression gates for this stack)
  </read_first>
  <files>
    .planning/phases/15-extended-contact-capture/MANUAL-QA-CHECKLIST.md
  </files>
  <action>
    1. Run TypeScript regression across both apps (capture exit codes):
       ```
       pnpm --filter api tsc --noEmit
       echo "API tsc exit: $?"
       pnpm --filter web tsc --noEmit
       echo "WEB tsc exit: $?"
       ```
       BOTH must exit 0. If non-zero, STOP and triage — do NOT proceed to QA until tsc is clean.

    2. Run Vitest regression across both apps (capture pass/fail counts):
       ```
       pnpm --filter api vitest run
       pnpm --filter web vitest run
       ```
       Record total tests / passed / failed for each. Acceptable outcomes:
       - All green
       - Pre-existing skipped tests still skipped (not new skips)
       - NO new failures introduced by Phase 15

       If failures appear in modules NOT touched by Phase 15 (e.g., a flake in reviews.service.spec), document them in the QA checklist as known-flakes and proceed. If failures appear in `guests/`, `public-booking/`, or `email/` modules → these are Phase 15 regressions, STOP and triage.

    3. Run a token-discipline check on the new sections of BookingFormPage.tsx ONLY:
       ```
       rg "#[0-9a-fA-F]{3,6}" apps/web/src/features/public-booking/BookingFormPage.tsx
       ```
       Note: existing hex values pre-date Phase 15 (per Plan 15-03 scope decision). The acceptance is: NO new hex values in the lines added by Phase 15 (the two `<details>` blocks). Inspect the diff:
       ```
       git diff apps/web/src/features/public-booking/BookingFormPage.tsx | grep "^+" | rg "#[0-9a-fA-F]{3,6}"
       ```
       Expected: 0 matches in added lines.

    4. Create `.planning/phases/15-extended-contact-capture/MANUAL-QA-CHECKLIST.md` with the following 7 scenarios. Document expected AND actual outcome for each (the EXECUTOR runs them with a local backend + browser, OR documents which scenarios are deferred to live QA on Railway):

       ```markdown
       # Phase 15 — Manual QA Checklist

       **Date:** {date of execution}
       **Executor:** {name}
       **Backend URL:** {http://localhost:3001 or Railway URL}
       **Frontend URL:** {http://localhost:5173 or Railway URL}

       ## Scenario 1 — Submit with ALL optional fields populated
       Steps:
       1. Navigate to /booking, search dates, pick a room, reach /booking/checkout
       2. Fill all required fields (existing 7)
       3. Expand "Preferencias de contacto" → fill WhatsApp `+57 300 123 4567`, select WhatsApp radio, check marketing consent, leave language as Español
       4. Expand "Preferencias adicionales" → fill dietary `vegetariano, sin lactosa`, fill special `cama extra para niño`
       5. Submit

       Expected: 201 + redirect to /booking/confirmation. Guest row in DB has all 6 values populated (whatsappNumber stripped: `+573001234567`).
       Actual: ___

       ## Scenario 2 — Submit WITHOUT touching optional sections (backward compat)
       Steps:
       1. Same as Scenario 1 but DO NOT expand either `<details>` section
       2. Submit

       Expected: 201 + redirect. Guest row has nulls for nullable fields, `preferredLanguage='es'`, `marketingConsent=false`.
       Actual: ___

       ## Scenario 3 — Invalid E.164 WhatsApp rejected client-side
       Steps:
       1. Reach the form
       2. Expand "Preferencias de contacto", type `abc` in WhatsApp
       3. Try to submit

       Expected: Inline terracotta error "WhatsApp debe ser formato E.164...". Form does NOT submit.
       Actual: ___

       ## Scenario 4 — Marketing consent is opt-in (Ley 1581 compliance)
       Steps:
       1. Fresh load /booking/checkout
       2. Expand "Preferencias de contacto"

       Expected: Marketing consent checkbox is UNCHECKED. Label reads verbatim: "Quiero recibir ofertas y novedades del hotel. Puedo darme de baja en cualquier momento."
       Actual: ___

       ## Scenario 5 — Confirmation email INCLUDES preferences section
       Steps:
       1. Execute Scenario 1 with a real `RESEND_API_KEY` configured (or check Resend dashboard sent logs)
       2. Open the received email

       Expected: After the reservation table, a warm-paper block titled "Sus preferencias" appears with 4 `<p>` lines (WhatsApp, contact preference, dietary, special). Background `#f4efe6`. Heading uses Instrument Serif.
       Actual: ___

       ## Scenario 6 — Confirmation email OMITS preferences section when no fields filled
       Steps:
       1. Execute Scenario 2
       2. Open the received email

       Expected: NO "Sus preferencias" block. Email layout identical to pre-Phase-15.
       Actual: ___

       ## Scenario 7 — XSS payload in dietaryRestrictions is escaped in email
       Steps:
       1. Reach the form
       2. Fill all required fields
       3. Expand "Preferencias adicionales", type into dietary: `<script>alert(1)</script>`
       4. Submit; open the received email and view source

       Expected: Email HTML source contains `&lt;script&gt;alert(1)&lt;/script&gt;`. NO `<script>` tag in raw form. Email client does not execute the script.
       Actual: ___

       ---

       ## Summary

       Passed: __/7
       Failed: __/7
       Deferred: __/7 (which scenarios deferred to live Railway QA, if any — explain)

       Blockers found: ___
       ```

    5. Execute the checklist (locally or document Railway deferral) and fill in "Actual" for each row.
  </action>
  <verify>
    <automated>pnpm --filter api tsc --noEmit && pnpm --filter web tsc --noEmit && pnpm --filter api vitest run && pnpm --filter web vitest run</automated>
    All four commands MUST exit 0 (or with only pre-existing acceptable skips). Then check:
    ```
    test -f .planning/phases/15-extended-contact-capture/MANUAL-QA-CHECKLIST.md
    ```
  </verify>
  <acceptance_criteria>
    - `pnpm --filter api tsc --noEmit` exit 0
    - `pnpm --filter web tsc --noEmit` exit 0
    - `pnpm --filter api vitest run` — no Phase 15-attributable failures
    - `pnpm --filter web vitest run` — no Phase 15-attributable failures
    - `MANUAL-QA-CHECKLIST.md` exists with all 7 scenarios documented
    - Each scenario has both Expected and Actual filled (Actual may be "Deferred to Railway QA" with a reason, max 2 scenarios deferred)
    - Zero new hex values in added lines of BookingFormPage.tsx (git diff grep returns empty)
  </acceptance_criteria>
  <done>
    Regression suite green. QA checklist executed and saved. Token discipline confirmed on new code.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human sign-off on QA checklist + Phase 15 closeout</name>
  <what-built>
    Phase 15 — Extended Contact Capture — is complete:
    - Guest schema has 6 new fields + ContactPreference enum (Wave 1 / Plan 15-01)
    - Public booking accepts the new fields and persists them (Wave 2 / Plan 15-02)
    - Email confirmation renders a conditional "Sus preferencias" section with XSS escaping (Wave 2 / Plan 15-02)
    - BookingFormPage has 2 new collapsible sections with full client-side validation + WhatsApp trim + Ley 1581 opt-in checkbox (Wave 2 / Plan 15-03)
    - Full regression + 7-scenario QA checklist executed (Wave 3 / Plan 15-04 Task 1)

    All 5 REQ-IDs (GCC-01..05) shipped. Phase 16 (Guest Detail + Deep Links + Contact Events) consumes these fields next.
  </what-built>
  <how-to-verify>
    1. Read `.planning/phases/15-extended-contact-capture/MANUAL-QA-CHECKLIST.md`. Confirm:
       - All 7 scenarios have "Actual" outcomes (not "TODO")
       - Passed count >= 5/7 (2 deferral max for Railway-only scenarios like real email send)
       - No blockers listed
    2. Optionally run a smoke booking yourself through the public portal to validate visually.
    3. Confirm tsc + vitest output from Task 1 is green.
    4. If anything fails, type the issue and STOP — Task 3 will not execute.
    5. If approved, type "approved" (or "aprobado") and Task 3 will commit the closeout.
  </how-to-verify>
  <resume-signal>Type "approved" / "aprobado" to proceed to Task 3, OR describe the blocker.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Write 15-SUMMARY.md + atomic update to STATE/ROADMAP/REQUIREMENTS</name>
  <read_first>
    - @.planning/STATE.md (current Position, Decisions, Pending Todos sections — these will be updated)
    - @.planning/ROADMAP.md (Phase 15 section + Progress table)
    - @.planning/REQUIREMENTS.md (GCC-01..05 + Traceability table)
    - @.planning/phases/15-extended-contact-capture/15-CONTEXT.md (for canonical REQ-ID list and locked decisions to surface in SUMMARY)
    - @.planning/phases/15-extended-contact-capture/15-01-SUMMARY.md / 15-02-SUMMARY.md / 15-03-SUMMARY.md
    - @.planning/phases/15-extended-contact-capture/MANUAL-QA-CHECKLIST.md
  </read_first>
  <files>
    .planning/phases/15-extended-contact-capture/15-SUMMARY.md
    .planning/STATE.md
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
  </files>
  <action>
    1. **Create `.planning/phases/15-extended-contact-capture/15-SUMMARY.md`**:

       ```markdown
       # Phase 15: Extended Contact Capture — SUMMARY

       **Started:** {date 15-01 began}
       **Completed:** {today}
       **Plans:** 4 plans (15-01 through 15-04)
       **Status:** Complete

       ## REQ-IDs shipped
       - [x] GCC-01: Prisma Guest extended with 6 nullable columns + ContactPreference enum + migration `20260519000001_phase15_extended_guest_contact` applied to Railway
       - [x] GCC-02: CreateGuestDto + UpdateGuestDto (auto via .partial()) extended via Zod, E.164 validation enforced, PATCH /api/guests/:id accepts partial updates
       - [x] GCC-03: POST /api/public/bookings accepts 6 new optional fields without breaking change
       - [x] GCC-04: BookingFormPage.tsx renders 2 collapsible `<details>` sections (Preferencias de contacto + Preferencias adicionales) with client-side Zod validation + WhatsApp space-strip transform + Ley 1581 opt-in marketing consent
       - [x] GCC-05: buildConfirmationHtml extended with `buildPreferencesSection()` helper — conditional warm-paper block with escapeHtml against XSS in user-provided text fields

       ## Architecture decisions (carry-forward)
       - **Explicit field mapping over spread**: Both GuestsService.create and PublicBookingService.tx.guest.create continue the project's explicit `data: {...}` pattern. Adding new Guest fields ALWAYS requires updating these mappings — silent-drop trap reconfirmed.
       - **txResult inline type widening**: chose inline annotation widening over extracting a named type. If a third consumer of txResult appears in Phase 16+, refactor to a `GuestEmailFields` named type.
       - **BookingConfirmationParams uses 4 scalar fields, not full Guest entity**: EmailService remains decoupled from Prisma — confirmed pattern.
       - **escapeHtml is module-local in EmailService**: NOT a shared util — only EmailService produces server-side HTML strings. If a second producer appears (e.g., admin export PDF), extract to a shared module.
       - **Native `<details>/<summary>` chosen over custom Collapsible**: zero new deps, browser-accessible by default, future shadcn Collapsible primitive can replace in-place if/when added.
       - **marketingConsent is NOT echoed in email**: Internal hotel flag for future v1.4+ marketing batches. Phase 15 captures it; v1.4 will use it.
       - **Migration timestamp**: `20260519000001` (sequential after `20260519000000_add_tra_export_log`).

       ## Deferred to Phase 16+
       - Guest detail page (`/guests/:id`) — GCC-09
       - ContactButtons component (Llamar/WhatsApp/Email deep links) — GCC-10
       - guest_contact_events table + Socket.io broadcast — GCC-06..08
       - useGuestContactEvents hook with realtime subscription — GCC-11
       - GuestsPage "Último contacto" column — GCC-12

       ## Deferred to v1.4+
       - WhatsApp Business API (templated outbound, not just deep links)
       - Email template library (welcome / pre-arrival / thank-you)
       - Country code dropdown for phone inputs
       - i18n EN/ES toggle on portal

       ## Test counts
       - apps/api/vitest: {pass}/{total}
       - apps/web/vitest: {pass}/{total} (or "no public-booking suite — confirmed acceptable per STATE.md")
       - QA scenarios: {pass}/7 ({deferred} deferred to Railway live)

       ## Files modified (canonical list)
       - apps/api/prisma/schema.prisma
       - apps/api/prisma/migrations/20260519000001_phase15_extended_guest_contact/migration.sql (new)
       - apps/api/src/modules/guests/dto/create-guest.dto.ts
       - apps/api/src/modules/guests/guests.service.ts
       - apps/api/src/modules/guests/guests.service.spec.ts
       - apps/api/src/modules/public-booking/dto/create-public-booking.dto.ts
       - apps/api/src/modules/public-booking/public-booking.service.ts
       - apps/api/src/modules/public-booking/public-booking.service.spec.ts
       - apps/api/src/modules/email/email.service.ts
       - apps/api/src/modules/email/email.service.spec.ts
       - apps/web/src/features/public-booking/BookingFormPage.tsx
       - apps/web/src/features/public-booking/public-booking.api.ts
       (+ .planning/* artifacts)

       ## Next phase
       Phase 16 — Guest Detail + Deep Links + Contact Events. Consumes the 6 new Guest fields from Phase 15. Builds the staff-facing guest detail page + real-time contact event log + Socket.io broadcast.
       ```

    2. **Update `.planning/STATE.md`**:
       - Bump `progress.completed_phases` by 1 (was 0 → 1 for v1.3)
       - Bump `progress.completed_plans` to reflect 4 plans done
       - Update `last_updated` to today
       - Under `## Current Position`: change `Phase: Not started` → `Phase: 15 — Complete; ready for Phase 16`
       - Under `### Decisions`: append the 7 carry-forward decisions from the SUMMARY (as `[Phase 15-XX]` bullet entries matching existing format)
       - Under `## Session Continuity`: update `Stopped at: Completed Phase 15 (4 plans)` + `Next: /gsd-discuss-phase 16` (or equivalent next step)

    3. **Update `.planning/ROADMAP.md`**:
       - Phase 15 section: change `**Plans**: TBD` → `**Plans**: 4 plans` and list:
         ```
         Plans:
         - [x] 15-01-PLAN.md — Schema migration + Guest DTOs + service mapping (GCC-01, GCC-02)
         - [x] 15-02-PLAN.md — Public booking integration + email preferences section with XSS escape (GCC-03, GCC-05)
         - [x] 15-03-PLAN.md — BookingFormPage 2 collapsible sections + client-side validation (GCC-04)
         - [x] 15-04-PLAN.md — Regression gate + QA checklist + Phase 15 closeout (GCC-01..05)
         ```
       - In the "Progress" table at the bottom: change `| 15. Extended Contact Capture | 0/TBD | Not started | — |` → `| 15. Extended Contact Capture | 4/4 | Complete | {today} |`

    4. **Update `.planning/REQUIREMENTS.md`**:
       - Lines for GCC-01..05: change `- [ ] **GCC-XX**:` → `- [x] **GCC-XX**:`
       - In the Traceability table at the bottom: change `| GCC-01..05 | Phase 15 — Extended Contact Capture | Pending |` → `| GCC-01..05 | Phase 15 — Extended Contact Capture | Complete ({today}) |`

    5. Run a final sanity check:
       ```
       rg "Phase 15.*Not started" .planning/  # should return 0 matches
       rg "GCC-0[1-5].*\\[ \\]" .planning/REQUIREMENTS.md  # should return 0 matches
       rg "15-extended-contact-capture.*0/TBD" .planning/ROADMAP.md  # should return 0 matches
       ```
       All three searches MUST return zero results.

    6. Atomic git commit (use conventional commit format, NO Co-Authored-By per global rules):
       ```
       git add .planning/phases/15-extended-contact-capture/ .planning/STATE.md .planning/ROADMAP.md .planning/REQUIREMENTS.md
       git commit -m "docs(15): close Phase 15 — Extended Contact Capture shipped (GCC-01..05)"
       ```
  </action>
  <verify>
    <automated>rg "Phase 15.*Not started" .planning/ ; rg "GCC-0[1-5].*\[ \]" .planning/REQUIREMENTS.md ; rg "15-extended-contact-capture.*0/TBD" .planning/ROADMAP.md</automated>
    All three MUST return 0 matches. Then:
    ```
    git log -1 --oneline
    ```
    Should show the docs(15) closeout commit at HEAD.
  </verify>
  <acceptance_criteria>
    - `.planning/phases/15-extended-contact-capture/15-SUMMARY.md` exists with all REQ-IDs marked [x]
    - `.planning/STATE.md` has `completed_phases >= 1` for v1.3 and Phase 15 in Decisions section
    - `.planning/ROADMAP.md` Phase 15 row shows `4/4 | Complete | {date}`
    - `.planning/REQUIREMENTS.md` GCC-01..05 all `[x]` and Traceability table shows Complete
    - `rg "Phase 15.*Not started"` returns 0 matches across .planning/
    - Git HEAD is a `docs(15): close Phase 15` commit
    - NO "Co-Authored-By" line in the commit (per global rules)
  </acceptance_criteria>
  <done>
    Phase 15 closed. STATE/ROADMAP/REQUIREMENTS reflect completion. Repository ready for /gsd-discuss-phase 16.
  </done>
</task>

</tasks>

<verification>
**Final Phase Gate:**
1. tsc + vitest green on both apps (no Phase 15-attributable regressions)
2. MANUAL-QA-CHECKLIST.md filled with 7 scenarios (≥5 pass, ≤2 deferred to Railway)
3. Human sign-off recorded (Task 2 checkpoint approved)
4. SUMMARY + STATE + ROADMAP + REQUIREMENTS atomically updated
5. Single commit `docs(15): close Phase 15...` at HEAD
6. Searches return zero stale markers (no "Phase 15.*Not started", no "GCC-0X [ ]", no "0/TBD")
</verification>

<success_criteria>
- All 5 REQ-IDs (GCC-01..05) marked complete in REQUIREMENTS.md
- Phase 15 ROADMAP entry shows 4/4 plans complete with today's date
- STATE.md reflects v1.3 progress (1/2 phases done)
- SUMMARY documents 7 architectural carry-forward decisions for Phase 16+
- Manual QA checklist passes (or has documented Railway-deferred items)
- Phase 16 can start cleanly with `/gsd-discuss-phase 16` or equivalent
</success_criteria>

<output>
This plan IS the closeout. No further per-plan SUMMARY needed beyond `15-SUMMARY.md` written in Task 3.
</output>
