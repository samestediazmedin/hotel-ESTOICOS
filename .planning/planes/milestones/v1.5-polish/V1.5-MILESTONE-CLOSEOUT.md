# Milestone v1.5 — Polish & Defect Cleanup — CLOSEOUT

**Started:** 2026-05-23
**Completed:** 2026-05-23 (same-day delivery)
**Status:** ✓ COMPLETE — all carry-forward items from v1.4 closed
**Trigger:** v1.4 left polish + defect items as carry-forward; user opted to close them out instead of starting a feature milestone.

---

## Scope

Pure polish + defect cleanup. No new features. The 5 items addressed were carry-forward from prior milestones (v1.3 + v1.4 + UX sweep + security sweep).

---

## Items delivered

| # | Item | Priority | Commit | Outcome |
|---|------|----------|--------|---------|
| 1 | Routing double-prefix bug (`/api/api/night-audit`, `/api/api/tra-export`) | 🔴 HIGH | `d077425` | 2 controllers + 4 frontend calls fixed (defect was symmetric in backend AND frontend) |
| 2 | Token consolidation in GuestsPage + GuestDetailPage | 🟡 MED | `39c78dd` | 64 token replacements (17 + 47) — adjacent to OBS-007 |
| 3 | Lint cleanup — 48 errors + 5 warnings → 0/0 | 🟡 MED | `561f8ef` | 29 files modified + 4 real bugs discovered & fixed |
| 4 | Calendar cross-row drag + drag-to-resize | 🟢 LOW | `93bf881` | Both extensions added; 4 new tests; existing G3 architecture supported it naturally |
| 5 | AI concierge hero + category mosaic (OBS-003) | 🟢 LOW | `f3169db` | New ConciergeHero component; 6-category mosaic for empty state |

---

## Final verification

| Gate | Result |
|------|--------|
| `pnpm audit --prod` | ✓ "No known vulnerabilities found" |
| API typecheck | ✓ 0 errors |
| Web typecheck | ✓ 0 errors |
| shared-kernel typecheck | ✓ 0 errors |
| **Web lint** | ✓ **0 errors / 0 warnings** (started v1.5 with 48/5) |
| API tests | ✓ 869 + 17 skipped (886 total) |
| Web Vitest tests | ✓ **163/163** (was 159 — +4 from calendar resize/cross-row spec) |
| Playwright E2E tests | ✓ 50 listed (unchanged) |
| **Total test surface** | **1,099 tests** (+4 vs v1.4 close) |

---

## Real bugs discovered during polish (filed and fixed in same commits)

| Bug | File | Discovered while | Fix |
|-----|------|------------------|-----|
| Routing double-prefix symmetric defect | `tra-export.controller.ts` + `night-audit.controller.ts` + 2 frontend api files | Phase 19 authz audit | Removed leading `api/` from both controllers and 4 frontend call sites |
| `formatCOP` dead code | `ReservationDrawer.tsx` | Lint cleanup item 3 | Removed unused `Intl.NumberFormat` instance |
| `multiplierHint` dead function | `SeasonDrawer.tsx` | Lint cleanup item 3 | Removed unused helper |
| `useReservationDraft` stale deps | `useReservationDraft.ts` | Lint cleanup item 3 | Object literal recreated each render — fixed with `useMemo` |
| Obsolete `eslint-disable` directive | `PhotoUploader.tsx` | Lint cleanup item 3 | Removed (rule wasn't enabled anyway) |

5 real bugs surfaced. Each was a code smell that lint had been missing because ESLint 10 crashed before reaching them in v1.3.

---

## Carry-forward — empty

There is no carry-forward from v1.5. All identified polish + defect items are closed.

### Truly remaining (no longer in any roadmap)

- **Pasarela de pago** — confirmed OUT OF SCOPE 2026-05-23. Not in v1.6 or any future planned version.
- **First Lighthouse + k6 baseline measurement** — requires manual execution by user (running API + web preview, then `lhci autorun` + k6 scripts). Documented in `.planning/quality-baseline.md` and `tests/perf/README.md`. Not a code task.

---

## v1.5 sub-agent attribution

| Sub-agent | Item(s) | Commits |
|-----------|---------|---------|
| (orchestrator inline) | 1 | `d077425` |
| `olaf` | 2, 3, 4 | `39c78dd`, `561f8ef`, `93bf881` |
| `pixel` | 5 | `f3169db` |

---

## Strategic outcomes

1. **Zero lint debt** — `pnpm --filter @hotel/web lint` runs at 0/0. CI lint gate can now be tightened from `continue-on-error: true` to blocking in a future micro-phase.
2. **Routing defects eliminated** — Phase 19 finding closed; no more `/api/api/...` confusion.
3. **Token system fully consistent** — `text-text-*` and `bg-surface-*` legacy patterns eliminated across all feature folders touched in v1.3+.
4. **Calendar feature-complete for v1.x** — drag-to-move (same row), drag-to-move (cross-row), drag-to-resize all supported. Only Playwright E2E coverage for resize remains optional polish.
5. **First-impression polish on Concierge IA** — hero + category mosaic replaces bare chat input as initial visual.

---

## Verdict

**APROBADO**. v1.5 milestone polished the system to a state where every identified concrete defect or polish item from prior milestones (and the external QA audit) is closed. No remaining technical debt at the planning level. Pasarela de pago is the only feature explicitly out of scope.

**Ready to tag `v1.5.0`** if user chooses.
