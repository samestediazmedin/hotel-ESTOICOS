---
phase: 14-public-reviews-system
plan: 06
type: execute
wave: 3
depends_on:
  - "14-01"
  - "14-02"
  - "14-03"
  - "14-04"
  - "14-05"
files_modified:
  - .planning/phases/14-public-reviews-system/14-MANUAL-QA-CHECKLIST.md
  - .planning/phases/14-public-reviews-system/14-CLOSEOUT.md
  - .planning/phases/14-public-reviews-system/V1.2-MILESTONE-CLOSEOUT.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements:
  - REV-01
  - REV-02
  - REV-03
  - REV-04
  - REV-05
  - REV-06
  - REV-07
  - REV-08

must_haves:
  truths:
    - "Full regression passes: apps/api tsc + vitest, apps/web tsc + vitest, all suites green"
    - "Zero hex color references in new folders (review-submit + reviews-admin) — only tokens"
    - "8 end-to-end QA scenarios documented and executed by user (checkpoint)"
    - "Phase 14 closeout document summarizes work, REV-IDs closed, decisions, deviations"
    - "v1.2 milestone closeout document summarizes Phases 12+13+14, all 22 REV-IDs verified"
    - "REQUIREMENTS.md updated: REV-01..08 marked [x] with completion date + plan reference"
    - "ROADMAP.md updated: Phase 14 status → Complete; v1.2 status → Complete"
    - "STATE.md updated: milestone status, completed_plans count, last_activity"
  artifacts:
    - path: ".planning/phases/14-public-reviews-system/14-MANUAL-QA-CHECKLIST.md"
      provides: "8-scenario end-to-end verification checklist"
      min_lines: 80
    - path: ".planning/phases/14-public-reviews-system/14-CLOSEOUT.md"
      provides: "Phase 14 closeout: REV-IDs closed, decisions, files inventory, deviations"
      min_lines: 60
    - path: ".planning/phases/14-public-reviews-system/V1.2-MILESTONE-CLOSEOUT.md"
      provides: "v1.2 milestone summary: 22 REV-IDs across Phases 12+13+14, carry-forward, next-milestone candidates"
      min_lines: 100
  key_links:
    - from: "REQUIREMENTS.md"
      to: "REV-01..08 closure"
      via: "Update checkbox [ ] → [x] + add completion date + plan reference"
      pattern: "REV-0[1-8].*Done"
    - from: "ROADMAP.md"
      to: "Phase 14 Complete + v1.2 Complete"
      via: "Update status line + plans table"
      pattern: "14.*Complete"

threat_model:
  - "Regression introduces silent breakage in Phase 10/12/13 tests → blocked by full vitest run requirement"
  - "Hex color leak in new folders → blocked by zero-hex grep gate"
  - "Manual QA reveals UAT gap → /gsd-plan-phase --gaps cycle (not blocked by this plan)"
---

<objective>
Close Phase 14 with a full regression gate, manual end-to-end QA checklist, Phase 14 closeout document, AND the v1.2 milestone closeout (since Phase 14 is the final phase of v1.2).

Purpose: Ship Phase 14 with confidence. Confirm no Phase 10/12/13 regression. Verify the full email-to-portal vertical slice works. Document the v1.2 milestone (22 REV-IDs across Phases 12+13+14) for the project record.
Output: 3 documentation artifacts + REQUIREMENTS.md/ROADMAP.md/STATE.md updates.
</objective>

<execution_context>
@C:/Users/oscarpeinado/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/oscarpeinado/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-public-reviews-system/14-CONTEXT.md
@.planning/phases/14-public-reviews-system/14-RESEARCH.md
@.planning/phases/14-public-reviews-system/14-01-PLAN.md
@.planning/phases/14-public-reviews-system/14-02-PLAN.md
@.planning/phases/14-public-reviews-system/14-03-PLAN.md
@.planning/phases/14-public-reviews-system/14-04-PLAN.md
@.planning/phases/14-public-reviews-system/14-05-PLAN.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-hotel-settings-admin-page/13-CLOSEOUT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Full regression gate + zero-hex audit on new folders</name>
  <files>(no files modified — pure verification)</files>
  <read_first>
    - .planning/phases/13-hotel-settings-admin-page/13-05-PLAN.md (regression gate pattern for Phase 13)
    - .planning/STATE.md (note Phase 13-05: vitest fails with `pnpm --filter` — use `npx vitest run` from app dir directly)
  </read_first>
  <action>
    1. **Backend regression:**
       ```powershell
       cd apps/api
       npx tsc --noEmit
       npx vitest run
       pnpm prisma migrate status
       cd ../..
       ```
       Expected: tsc exit 0; vitest all suites green; migrate status reports DB up to date.
       Record total test count delta vs baseline.

    2. **Frontend regression:**
       ```powershell
       cd apps/web
       npx tsc --noEmit
       npx vitest run
       cd ../..
       ```
       Expected: tsc exit 0; vitest all suites green including Phase 10 (HotelHomePage, useReservationDraft) + Phase 12 + Phase 13.
       Record any new tests added in Phase 14 plans.

    3. **Zero-hex audit on NEW folders only** (not full repo — Phase 9 already enforces zero-hex elsewhere):
       ```powershell
       rg "#[0-9a-fA-F]{3,8}" apps/web/src/features/review-submit/ --type ts --type tsx
       rg "#[0-9a-fA-F]{3,8}" apps/web/src/features/reviews-admin/ --type ts --type tsx
       ```
       Expected: 0 matches. The only acceptable exception is hex inside `apps/api` email HTML template (server-side email cannot use CSS vars — clients render the raw HTML).

    4. **Orphan file check:**
       ```powershell
       rg "from.*data/reviews" apps/web/src
       Test-Path apps/web/src/features/public-portal/data/reviews.ts
       ```
       Expected: 0 matches + Test-Path returns False (file deleted per 14-04).

    5. **Endpoint smoke test** (API + DB live):
       ```powershell
       # Start API in background
       cd apps/api; pnpm start:dev
       # In separate shell:
       curl -s http://localhost:3011/api/public/reviews
       curl -s "http://localhost:3011/api/public/reviews/validate-token?token=bogus"
       curl -s http://localhost:3011/api/reviews
       ```
       Expected: GET public returns 200 + valid JSON, validate-token bogus returns 401, GET admin without auth returns 401.

    NOTE: Per STATE.md Phase 13-05 decision, `pnpm --filter X vitest` fails — use `npx vitest run` from app dir directly.
    NOTE: Report all results in 14-CLOSEOUT.md regardless of outcome. Any failure → STOP and escalate before continuing.
  </action>
  <verify>
    <automated>cd apps/api && npx tsc --noEmit</automated>
    <automated>cd apps/api && npx vitest run 2>&1 | tail -5</automated>
    <automated>cd apps/web && npx tsc --noEmit</automated>
    <automated>cd apps/web && npx vitest run 2>&1 | tail -5</automated>
    <automated>rg "#[0-9a-fA-F]{3,8}" apps/web/src/features/review-submit/ apps/web/src/features/reviews-admin/ ; if ($LASTEXITCODE -eq 0) { exit 1 } else { exit 0 }</automated>
  </verify>
  <acceptance_criteria>
    - All 4 tsc + vitest commands exit 0
    - Zero hex matches in review-submit/ and reviews-admin/
    - File `apps/web/src/features/public-portal/data/reviews.ts` does NOT exist
    - No orphan imports of `data/reviews`
    - All findings recorded for inclusion in 14-CLOSEOUT.md
  </acceptance_criteria>
  <done>Full regression green. Quality gates passed. Ready for manual QA.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual end-to-end QA checklist (8 scenarios)</name>
  <files>.planning/phases/14-public-reviews-system/14-MANUAL-QA-CHECKLIST.md</files>
  <what-built>
    Full Phase 14 reviews pipeline:
    - Backend: Reviews module (5 endpoints + dedicated throttler + single-use JWT)
    - Cron: Night-audit extension sends review-invite emails 1 day after checkout
    - Frontend public: /review/submit?token=... submit form
    - Frontend portal: ReviewsSection self-fetching from real DB
    - Frontend staff: /reviews moderation queue with 3 tabs
  </what-built>
  <how-to-verify>
    **Create `.planning/phases/14-public-reviews-system/14-MANUAL-QA-CHECKLIST.md` with these 8 scenarios. For each scenario, mark [ ] (pending) and let the user check off [x] after performing the test:**

    **Scenario 1 — Token validation: valid path**
    1. In the DB, insert a Reservation row with status=CHECKED_OUT and a fresh guest with email
    2. In the API, call `ReviewsService.signReviewToken({reservationId, guestName, stayDate})` (or use the night-audit cron in step 7)
    3. Visit `http://localhost:5173/review/submit?token=<signed-token>`
    4. Verify: page renders, guestName prefilled, stayDate shown, no error
    5. Submit rating=4 + comment="Excelente estadía, muy recomendado." → Success state "Gracias por tu reseña" appears

    **Scenario 2 — Token validation: invalid signature**
    1. Visit `http://localhost:5173/review/submit?token=eyBOGUS.PAYLOAD.SIG`
    2. Verify: page shows "Este enlace ya no es válido" error state
    3. Verify: no form is rendered

    **Scenario 3 — Token replay (single-use enforcement)**
    1. Reuse the token from Scenario 1
    2. Visit `/review/submit?token=<same-token>`
    3. Verify: page shows "Este enlace ya fue utilizado" (alreadySubmitted=true detected)
    4. Try POSTing directly via curl → 410 Gone

    **Scenario 4 — Form validation client-side**
    1. With a fresh valid token, open submit page
    2. Click submit with 0 stars → error "Selecciona al menos 1 estrella"
    3. Set 5 stars but comment="ok" (3 chars) → error "Mínimo 10 caracteres"
    4. Set 5 stars + 2001-char comment → error "Máximo 2000 caracteres"

    **Scenario 5 — Rate limit (5/IP/hour)**
    1. Use curl to POST to `/api/public/reviews` 6 times within 1 hour (with different fresh tokens)
    2. Verify: 6th attempt returns 429 Too Many Requests
    3. Verify: the dedicated `reviews-submit` throttler is enforcing the limit (not the global `short`)

    **Scenario 6 — Staff moderation flow**
    1. Login as RECEPTION role (lowest staff role)
    2. Sidebar shows "Reseñas" under Administración → click
    3. Verify: /reviews page loads (no 403)
    4. Pending tab shows review from Scenario 1
    5. Click "Aprobar" → review moves to Publicadas tab → counts update

    **Scenario 7 — Portal reflection (cross-cache invalidation)**
    1. Right after Scenario 6 (approve action), visit `/booking`
    2. Verify: Reseñas section shows the just-approved review
    3. Verify: averageRating + total count reflect the new state (not stale)
    4. Wait 60s + refresh → still correct (Cache-Control max-age=60 respected)

    **Scenario 8 — Cron pipeline (end-to-end)**
    1. Insert a Reservation with status=CHECKED_OUT, checkOutDate=yesterday, guest.email set, reviewInviteSentAt=NULL
    2. Manually trigger night-audit backfill: `POST /api/night-audit/backfill?businessDate=today` (use admin JWT)
    3. Verify in Resend dashboard: email sent with subject "Cuéntanos sobre tu estadía en..."
    4. Verify in DB: `reviewInviteSentAt` is now populated for that reservation
    5. Click the link in the email → lands on /review/submit?token=... → form renders correctly

    **End:** All 8 scenarios pass → Phase 14 functionally complete. Document any deviations in 14-CLOSEOUT.md.

    **Then create the checklist file with all 8 scenarios as markdown checkboxes for future reference.**
  </how-to-verify>
  <resume-signal>Type "approved" if all 8 scenarios pass, or describe failures with scenario number.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Phase 14 closeout + v1.2 milestone closeout + ROADMAP/REQUIREMENTS/STATE updates</name>
  <files>.planning/phases/14-public-reviews-system/14-CLOSEOUT.md, .planning/phases/14-public-reviews-system/V1.2-MILESTONE-CLOSEOUT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/phases/14-public-reviews-system/14-01-SUMMARY.md through 14-05-SUMMARY.md (gather files + decisions)
    - .planning/phases/13-hotel-settings-admin-page/13-CLOSEOUT.md (format template)
    - .planning/REQUIREMENTS.md (find REV-01..08 lines + traceability table)
    - .planning/ROADMAP.md (find Phase 14 entry + v1.2 milestone section)
    - .planning/STATE.md (current frontmatter values)
  </read_first>
  <action>
    1. **Create `.planning/phases/14-public-reviews-system/14-CLOSEOUT.md`** with sections:
       - Phase summary (1-paragraph)
       - REV-IDs closed (table: REV-ID · plan · status · verification)
       - Files inventory (created · modified · deleted — aggregate from all 5 SUMMARYs)
       - Key decisions (table: decision · rationale · plan)
       - Deviations from CONTEXT.md/RESEARCH.md (if any)
       - Regression results (test counts, suite status, env covered)
       - Manual QA results (8 scenarios pass/fail)
       - Known gaps / deferred items (carry-forward to v1.3)
       - Files: minimum 60 lines

    2. **Create `.planning/phases/14-public-reviews-system/V1.2-MILESTONE-CLOSEOUT.md`** — the v1.2 wrap-up:
       - **v1.2 Overview**: "Public Data Wiring" — eliminating hardcoded portal data
       - **Phases completed**: 12 (Public Data API + Wiring), 13 (Hotel Settings Admin), 14 (Public Reviews)
       - **REV-IDs closed (22 total)**:
         - PDA-01..08 (Phase 12) — 8
         - HSP-01..06 (Phase 13) — 6
         - REV-01..08 (Phase 14) — 8
       - **Plans executed**: 5 + 5 + 6 = 16 plans
       - **Key architectural decisions** across phases (summarize from PROJECT.md Key Decisions table + STATE.md decisions log):
         - Cache-Control 60s on public endpoints
         - Dual-shape photo URL (R2 key vs legacy url)
         - Single-tenant simplification (column on Reservation for jti)
         - No @react-email/components (inline HTML)
         - Dedicated throttler for review submit (5/IP/hour vs short 60s)
         - DB unique + P2002 catch for token replay
       - **Performance metrics**: aggregate plan durations from STATE.md table
       - **Carry-forward backlog (v1.3+ candidates)**:
         - Photo attachments in reviews
         - Multi-language reviews (ES/EN)
         - Review responses by hotel staff
         - Spam/fake-review detection
         - Auto-publish threshold (e.g., 4+ stars)
         - Email A/B testing
         - Guest editing reviews
       - **Next milestone candidates** (suggest v1.3 themes):
         - "Operational depth" — channel manager prep, payment gateway, OTA stubs
         - "AI enhancements" — staff AI write tools, concierge live data
         - "Guest experience" — self check-in, WhatsApp integration, loyalty
       - Files: minimum 100 lines

    3. **Update `.planning/REQUIREMENTS.md`**:
       - For each of REV-01..08, change `- [ ]` to `- [x]` AND append `Done — Phase 14-NN (2026-05-18)` (replace NN with the plan that closed it):
         - REV-01 → 14-01
         - REV-02 → 14-01 + 14-03
         - REV-03 → 14-01
         - REV-04 → 14-01 + 14-05
         - REV-05 → 14-04
         - REV-06 → 14-01 + 14-05
         - REV-07 → 14-02
         - REV-08 → 14-03
       - Update Traceability table row: `| REV-01..08 | Phase 14 — Public Reviews System | Pending |` → `| REV-01..08 | Phase 14 — Public Reviews System | Complete (2026-05-18) |`

    4. **Update `.planning/ROADMAP.md`**:
       - In Phase 14 section: `**Plans**: TBD` → `**Plans**: 6 plans` with listing of all 6 plan files
       - In Progress table at bottom: `| 14. Public Reviews System | 0/TBD | Not started | — |` → `| 14. Public Reviews System | 6/6 | Complete | 2026-05-18 |`

    5. **Update `.planning/STATE.md`** (YAML frontmatter):
       - `status: Defining requirements` → `status: v1.2 milestone complete`
       - `stopped_at: ...13-05...` → `stopped_at: Completed Phase 14 + v1.2 milestone closeout`
       - `last_updated: 2026-05-18T...` (current timestamp)
       - `last_activity: 2026-05-18 — Phase 14 Public Reviews System complete; v1.2 milestone closed`
       - `progress.total_phases: 14` (unchanged)
       - `progress.completed_phases: 14` (was 5 — Phase 13 was tracked separately; verify current count and bump appropriately)
       - `progress.total_plans: 35` (29 + 6 from Phase 14)
       - `progress.completed_plans: 35`
       - `progress.percent: 100` (or whatever reflects total roadmap completion)
       - Append the Phase 14 plan rows to the Performance Metrics table
       - Append key Phase 14 decisions to the Decisions section

    NOTE: Document this is the v1.2 milestone close — next /gsd-new-milestone or /gsd-complete-milestone should reference V1.2-MILESTONE-CLOSEOUT.md.
  </action>
  <verify>
    <automated>Test-Path .planning/phases/14-public-reviews-system/14-CLOSEOUT.md</automated>
    <automated>Test-Path .planning/phases/14-public-reviews-system/V1.2-MILESTONE-CLOSEOUT.md</automated>
    <automated>rg "REV-01.*\[x\]" .planning/REQUIREMENTS.md</automated>
    <automated>rg "Phase 14.*Complete" .planning/ROADMAP.md</automated>
    <automated>rg "v1.2 milestone complete" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `14-CLOSEOUT.md` exists, ≥60 lines, includes REV-IDs closure table + manual QA results
    - `V1.2-MILESTONE-CLOSEOUT.md` exists, ≥100 lines, covers Phases 12+13+14 + 22 REV-IDs + carry-forward
    - All 8 REV-IDs (REV-01..08) marked `[x]` in REQUIREMENTS.md
    - REQUIREMENTS.md Traceability table updated for Phase 14
    - ROADMAP.md Phase 14 status → Complete; plan list populated
    - STATE.md frontmatter updated with milestone completion
    - STATE.md Performance Metrics + Decisions sections appended with Phase 14 entries
  </acceptance_criteria>
  <done>Phase 14 + v1.2 milestone fully documented. Project state reflects completion. Ready for `/gsd-complete-milestone` or new milestone planning.</done>
</task>

</tasks>

<verification>
- All previous plans (14-01..14-05) reported success
- Regression suites green (apps/api + apps/web)
- Zero-hex audit clean on new folders
- 8 manual QA scenarios pass
- Phase 14 closeout + v1.2 closeout written
- REQUIREMENTS / ROADMAP / STATE updated
</verification>

<success_criteria>
- All 8 REV-IDs (REV-01..08) verified and marked complete
- v1.2 milestone (22 total REV-IDs) closed
- No regression in Phase 10/12/13
- Documentation trail complete for project record
</success_criteria>

<output>
After completion:
- `.planning/phases/14-public-reviews-system/14-CLOSEOUT.md`
- `.planning/phases/14-public-reviews-system/V1.2-MILESTONE-CLOSEOUT.md`
- `.planning/phases/14-public-reviews-system/14-MANUAL-QA-CHECKLIST.md`
- Updated `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`
- Final `14-06-SUMMARY.md` summarizing this closeout plan itself
</output>
