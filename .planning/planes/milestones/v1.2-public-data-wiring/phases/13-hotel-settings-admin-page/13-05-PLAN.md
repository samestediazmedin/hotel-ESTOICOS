---
phase: 13-hotel-settings-admin-page
plan: 05
type: execute
wave: 5
depends_on: [13-01, 13-02, 13-03, 13-04]
files_modified:
  - .planning/phases/13-hotel-settings-admin-page/13-MANUAL-QA-CHECKLIST.md
  - .planning/phases/13-hotel-settings-admin-page/13-SUMMARY.md
  - .planning/STATE.md
autonomous: false
requirements: [HSP-01, HSP-02, HSP-03, HSP-04, HSP-05, HSP-06]
must_haves:
  truths:
    - "Toda la suite Vitest backend pasa (system-config + hotel-photos + public-portal + regresión)"
    - "Toda la suite Vitest frontend pasa (settings/* + public-portal/* + regresión)"
    - "tsc --noEmit clean en ambos paquetes"
    - "Manual QA checklist documentado y ejecutado (9 escenarios)"
    - "Phase 13 SUMMARY documenta los 6 HSP requirements como cubiertos"
    - "STATE.md actualizado con velocity + decisiones nuevas"
  artifacts:
    - path: ".planning/phases/13-hotel-settings-admin-page/13-MANUAL-QA-CHECKLIST.md"
      provides: "9 scenarios with expected results"
      contains: "## Scenario 1"
    - path: ".planning/phases/13-hotel-settings-admin-page/13-SUMMARY.md"
      provides: "Phase 13 closeout"
      contains: "HSP-01"
  key_links:
    - from: "Phase 13 regression"
      to: "Phase 10 + 12 test suites"
      via: "vitest run all + filter on public-portal"
      pattern: "src/features/public-portal/"
    - from: "Manual QA checklist"
      to: "9 scenarios from CONTEXT.md decisions section"
      via: "explicit scenario IDs"
      pattern: "## Scenario"
---

<objective>
Gate de regresión + manual QA + Phase 13 closeout. Ejecutar suites Vitest completas, validar que Phases 10 y 12 no regresionaron, documentar checklist de QA manual (subir foto, reorder, delete, role gate, etc.) y escribir SUMMARY de fase + actualizar STATE.md.

Purpose: Confirmar que los 6 requirements HSP están cerrados, el portal sigue funcionando y queda evidencia escrita para auditoría futura.

Output: Checklist de QA (markdown), SUMMARY de fase, STATE.md actualizado, todos los tests verdes.
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-hotel-settings-admin-page/13-CONTEXT.md
@.planning/phases/13-hotel-settings-admin-page/13-RESEARCH.md
@.planning/phases/13-hotel-settings-admin-page/13-01-PLAN.md
@.planning/phases/13-hotel-settings-admin-page/13-02-PLAN.md
@.planning/phases/13-hotel-settings-admin-page/13-03-PLAN.md
@.planning/phases/13-hotel-settings-admin-page/13-04-PLAN.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-public-data-api-frontend-wiring/12-05-PLAN.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Regression gate — full Vitest + tsc on web and api</name>
  <read_first>
    - .planning/phases/12-public-data-api-frontend-wiring/12-05-PLAN.md (Phase 12 regression gate pattern)
    - apps/api/vitest.config.ts (test glob)
    - apps/web/vitest.config.ts (test glob)
  </read_first>
  <files>
    .planning/phases/13-hotel-settings-admin-page/13-REGRESSION-LOG.md
  </files>
  <action>
    Execute the regression suite and capture outputs in a new log file (markdown) under the phase directory.

    Run sequentially (each command's output captured into the log):

    1. `pnpm --filter api tsc --noEmit` — expected exit 0
    2. `pnpm --filter web tsc --noEmit` — expected exit 0
    3. `pnpm --filter api vitest run src/system-config/` — Phase 13 backend SystemConfig unit tests
    4. `pnpm --filter api vitest run src/modules/hotel-photos/` — Phase 13 backend hotel-photos unit tests
    5. `pnpm --filter api vitest run src/modules/public-portal/` — Phase 12 backend regression (must still pass after Plan 13-01 modifications)
    6. `pnpm --filter api vitest run` — full backend suite
    7. `pnpm --filter web vitest run src/features/settings/` — Phase 13 frontend (any tests created — may be 0 if not authored)
    8. `pnpm --filter web vitest run src/features/public-portal/` — Phase 10 + 12 frontend regression
    9. `pnpm --filter web vitest run` — full frontend suite

    Create `.planning/phases/13-hotel-settings-admin-page/13-REGRESSION-LOG.md` with the format:

    ```markdown
    # Phase 13 — Regression Log

    Captured: <DATE>
    Operator: <agent>

    ## Backend

    ### tsc --noEmit
    Exit code: 0
    <last 5 lines of output if any>

    ### vitest src/system-config/
    Exit code: 0
    Tests passed: N / N
    Duration: Xs

    ### vitest src/modules/hotel-photos/
    ...

    ### vitest src/modules/public-portal/ (Phase 12 regression)
    ...

    ### vitest full
    ...

    ## Frontend
    ...

    ## Summary

    | Suite | Exit | Tests | Notes |
    |-------|------|-------|-------|
    | api tsc | 0 | — | clean |
    | api vitest system-config | 0 | N/N | — |
    | ... | | | |

    All gates green: YES / NO
    ```

    If ANY suite fails:
    - DO NOT proceed to Task 2.
    - File a precise diagnosis: which test, why, what plan likely caused it.
    - Stop and surface to the user.
  </action>
  <verify>
    <automated>cat .planning/phases/13-hotel-settings-admin-page/13-REGRESSION-LOG.md | grep -i "All gates green: YES"</automated>
    Manual: scan the log and confirm no FAIL lines.
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/13-hotel-settings-admin-page/13-REGRESSION-LOG.md` exists with all 9 suites captured
    - Every suite shows exit code 0
    - Summary table all rows show Exit=0
    - "All gates green: YES" line present
    - No new failing tests outside of pre-existing skipped ones (compare against Phase 12-05 regression log if any)
  </acceptance_criteria>
  <done>All regression gates green; log file committed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual QA Checklist (9 scenarios)</name>
  <files>.planning/phases/13-hotel-settings-admin-page/13-MANUAL-QA-CHECKLIST.md</files>
  <action>
    Author `.planning/phases/13-hotel-settings-admin-page/13-MANUAL-QA-CHECKLIST.md` with the 9 scenarios listed in `<how-to-verify>` below. Run a local stack (`pnpm --filter api dev` + `pnpm --filter web dev`) and execute each scenario, recording PASS/FAIL with timestamp inline.
  </action>
  <verify>
    <automated>test -f .planning/phases/13-hotel-settings-admin-page/13-MANUAL-QA-CHECKLIST.md</automated>
    Manual gate: the user/operator inspects the checklist and replies "QA approved" or lists failures.
  </verify>
  <done>9/9 scenarios PASS, checklist saved, user resume-signal received.</done>
  <what-built>
    Phases 13-01 through 13-04 are now functional. The admin can edit hotel identity at `/settings/hotel` (form with 6 fields) and manage hero gallery photos (upload, reorder, delete). All changes propagate to `/api/public/*` endpoints, which the public portal consumes via TanStack Query.
  </what-built>
  <how-to-verify>
    Author `.planning/phases/13-hotel-settings-admin-page/13-MANUAL-QA-CHECKLIST.md` with the 9 scenarios below, then execute each manually against a running local stack (`pnpm --filter api dev` + `pnpm --filter web dev`). Mark each scenario PASS/FAIL with timestamp + screenshots optional.

    **Scenarios:**

    1. **PATCH ADMIN happy path**: Login as ADMIN, navigate to `/settings/hotel`. Form pre-fills with current values. Change "Nombre del hotel" to "Hotel Sumapaz Verificación". Click "Guardar cambios". Expect: success banner appears within 1 second; field stays at new value.

    2. **Public portal reflects name change**: Open a new tab, visit `/booking`. Hotel name in HeroIdentity, TopNav, and PortalFooter shows "Hotel Sumapaz Verificación" (may take up to 60s due to TanStack staleTime — hard refresh forces immediate update).

    3. **PATCH non-ADMIN role gate**: Logout. Login as RECEPTION. Manually navigate to `/settings/hotel`. Expect: "Acceso restringido" surface; no form shown; no leaked data. Inspect DevTools Network — GET `/api/system-config` should return 403.

    4. **PATCH no-auth gate**: With DevTools open, in an incognito window (no JWT), `curl -X PATCH http://localhost:3011/api/system-config -d '{"name":"X"}' -H "Content-Type: application/json"`. Expect: 401 Unauthorized.

    5. **Sidebar visibility**: As ADMIN, sidebar shows "Configuración" under ADMINISTRACIÓN with `SlidersHorizontal` icon. As RECEPTION, "Configuración" does NOT appear in sidebar.

    6. **Photo upload**: As ADMIN at `/settings/hotel`, click "Subir foto", pick a JPG ≤5MB. Expect: progress feedback; photo appears at end of admin grid after ~1-3 seconds. Verify in another tab `/api/public/hotel-photos` includes the new photo URL.

    7. **Photo reorder**: As ADMIN, drag the last photo to the first position. Expect: grid reorders immediately (optimistic); refreshing the page preserves the new order; `/api/public/hotel-photos` returns photos in new order.

    8. **Photo delete with confirmation**: As ADMIN, click X on any photo. Expect: AlertDialog asks "¿Eliminar esta foto?" with Cancelar/Eliminar buttons. Click Cancelar → dialog closes, photo remains. Click Eliminar → photo disappears from grid AND from `/api/public/hotel-photos` AND from R2 bucket (verify in Cloudflare R2 dashboard if available).

    9. **Audit log entry**: After scenario 1, query DB: `SELECT * FROM system_config_change_log ORDER BY "changedAt" DESC LIMIT 1`. Expect: row exists; `fieldsChanged` array contains "hotelName"; `before` JSON has the original name; `after` JSON has the new name; `userId` matches the admin who edited.
  </how-to-verify>
  <resume-signal>Reply "QA approved" (with link to filled-out checklist) or list failing scenarios with details.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Phase 13 SUMMARY + STATE.md update</name>
  <read_first>
    - .planning/phases/12-public-data-api-frontend-wiring/12-05-PLAN.md (SUMMARY format reference if exists)
    - All 13-0X-PLAN.md frontmatter for must_haves rollup
    - .planning/STATE.md (current shape)
    - C:/Users/Admin/.claude/get-shit-done/templates/summary.md
  </read_first>
  <files>
    .planning/phases/13-hotel-settings-admin-page/13-SUMMARY.md,
    .planning/STATE.md,
    .planning/ROADMAP.md
  </files>
  <action>
    **A) Create `.planning/phases/13-hotel-settings-admin-page/13-SUMMARY.md`:**

    Structure:

    ```markdown
    # Phase 13 — Hotel Settings Admin Page — SUMMARY

    **Completed:** <DATE>
    **Milestone:** v1.2 — Public Data Wiring
    **Plans:** 5 (13-01 through 13-05)

    ## What shipped

    - PATCH /api/system-config (ADMIN-only) with Zod validation
    - GET /api/system-config (admin form prefill)
    - GET /api/admin/hotel-photos (admin gallery list with IDs)
    - POST /api/admin/hotel-photos/presign (R2 presigned PUT, 5-min expiry, 5MB cap)
    - POST /api/admin/hotel-photos (confirm + create row, idempotent on key)
    - PATCH /api/admin/hotel-photos/reorder (transactional ordinal update)
    - DELETE /api/admin/hotel-photos/:id (DB delete + best-effort R2 delete)
    - SystemConfigChangeLog audit table (one row per PATCH with diff)
    - Schema: address column on system_config; key column on hotel_photos
    - PublicPortalService now reads address from DB + resolves photo URLs via key
    - /settings/hotel admin route + inline ADMIN role gate
    - HotelInfoForm (6 fields, react-hook-form + zodResolver)
    - HotelGalleryManager (HTML5 drag, upload, delete with AlertDialog)
    - Sidebar "Configuración" entry under ADMINISTRACIÓN (SlidersHorizontal icon, ADMIN only)
    - 2 new UI primitives: Textarea + AlertDialog (Radix-based)

    ## Requirements coverage

    | ID | Description | Plan(s) | Status |
    |----|-------------|---------|--------|
    | HSP-01 | PATCH /api/system-config ADMIN-gated + Zod + audit | 13-01 | DONE |
    | HSP-02 | hotel_photos.key migration | 13-01 | DONE |
    | HSP-03 | /settings/hotel form ADMIN-only | 13-03 | DONE |
    | HSP-04 | Gallery manager (upload + reorder + delete) | 13-04 | DONE |
    | HSP-05 | 4 admin hotel-photos endpoints | 13-02 + 13-04 (GET add) | DONE |
    | HSP-06 | Public endpoints reflect admin edits | 13-01 + 13-03 + 13-04 | DONE |

    ## Decisions logged

    - Schema migration combined 3 changes (address + key + change_log) in one migration `20260525000000_phase13_hotel_settings_admin` — single deployable unit
    - SystemConfig single-row update uses `updateMany({where:{}, data})` then `findFirst()` — Pattern 1 from RESEARCH.md
    - HotelPhotosService DUPLICATES R2 client setup from PhotosService (no cross-module DI with InventoryRepository)
    - HotelPhoto.url is set to '' for new R2 uploads; URL resolved at read time via key (mirrors RoomPhoto convention)
    - SystemConfigChangeLog written AFTER update with try/catch (audit failure must not break user update)
    - Sidebar icon: SlidersHorizontal (NOT Settings — collision with Usuarios)
    - Role gate INLINE in HotelSettingsPage component (ProtectedRoute has no roles prop — confirmed via direct read of router.tsx)
    - HTML5 native drag-and-drop (no @dnd-kit install)
    - AlertDialog primitive added via @radix-ui/react-alert-dialog (new dep)
    - Textarea primitive hand-rolled to match existing Input token pattern (no shadcn CLI dep on agent env)

    ## Tests

    - Backend: HotelPhotosService unit tests (≥5, Vitest + @nestjs/testing)
    - Regression: Phase 10 + 12 web tests + Phase 12 api tests — all green
    - Full vitest run: see 13-REGRESSION-LOG.md

    ## Manual QA

    See 13-MANUAL-QA-CHECKLIST.md (9 scenarios, all PASS).

    ## Velocity

    | Plan | Tasks | Files touched | Duration (min) |
    |------|-------|---------------|----------------|
    | 13-01 | 3 | 6 | — |
    | 13-02 | 2 | 8 | — |
    | 13-03 | 3 | 11 | — |
    | 13-04 | 3 | 8 | — |
    | 13-05 | 3 | 3 | — |

    ## Phase 13 — Closed
    ```

    Fill in actual durations after execution.

    **B) Update `.planning/STATE.md`:**

    - Increment `progress.completed_plans` by 5 → 29
    - Increment `progress.completed_phases` by 1 → 5
    - Recompute `progress.percent`
    - Update `last_activity` and `stopped_at`
    - Append Phase 13 decisions to "Decisions" list (use bullet format consistent with prior phases):
      - `[Phase 13-01]`: migration combined address + key + change_log in one file (`20260525000000_phase13_hotel_settings_admin`)
      - `[Phase 13-01]`: SystemConfigService.update uses `updateMany({where:{},data})` + `findFirst()` — single-row table convention
      - `[Phase 13-01]`: PublicPortalService.getHotelInfo reads `address` from DB; HOTEL_ADDRESS_PLACEHOLDER constant removed
      - `[Phase 13-01]`: HotelPhoto URL resolution dual-shape — `p.key ? \`${pub}/${p.key}\` : p.url` — accommodates Phase 12 seeded rows
      - `[Phase 13-02]`: HotelPhotosService duplicates R2 client init from PhotosService — no cross-module DI with InventoryRepository
      - `[Phase 13-02]`: SystemConfigChangeLog audit written AFTER update with try/catch — never throws (informational)
      - `[Phase 13-03]`: ProtectedRoute lacks `roles` prop — role gate is INLINE in HotelSettingsPage
      - `[Phase 13-03]`: Sidebar icon for /settings/hotel is `SlidersHorizontal` to avoid collision with Usuarios (Settings)
      - `[Phase 13-03]`: AlertDialog primitive added via `@radix-ui/react-alert-dialog` (new dep)
      - `[Phase 13-04]`: HTML5 native drag-and-drop for photo reorder — no @dnd-kit install (sufficient for 5-10 photos)
      - `[Phase 13-04]`: All photo mutations invalidate BOTH `['admin','hotel-photos']` AND `['public','hotel-photos']` queries
    - Append velocity rows to the table at lines 65-84

    **C) Update `.planning/ROADMAP.md`:**

    Mark Phase 13 as `[x]` complete with completion date. Update the progress table at the bottom:
    ```
    | 13. Hotel Settings Admin Page | 5/5 | Complete | <DATE> |
    ```

    Update the Phase 13 section's `**Plans**: TBD` to `**Plans**: 5 plans` and add the plan list:
    ```
    Plans:
    - [x] 13-01-PLAN.md — Migration + SystemConfig PATCH + PublicPortalService dual-shape
    - [x] 13-02-PLAN.md — HotelPhotos admin module (presign + confirm + reorder + delete)
    - [x] 13-03-PLAN.md — Settings page + form + Sidebar nav + Textarea/AlertDialog primitives
    - [x] 13-04-PLAN.md — Gallery manager + HTML5 drag + multi-step upload hook
    - [x] 13-05-PLAN.md — Regression gate + manual QA + Phase 13 closeout
    ```

    Update REQUIREMENTS.md traceability section: mark HSP-01..06 as DONE with date, update count rollups if needed.
  </action>
  <verify>
    <automated>test -f .planning/phases/13-hotel-settings-admin-page/13-SUMMARY.md && test -f .planning/phases/13-hotel-settings-admin-page/13-REGRESSION-LOG.md && test -f .planning/phases/13-hotel-settings-admin-page/13-MANUAL-QA-CHECKLIST.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - 13-SUMMARY.md exists with HSP-01..06 in coverage table
    - STATE.md updated: completed_plans 24 → 29, completed_phases 4 → 5
    - ROADMAP.md Phase 13 marked [x] with plan list
    - REQUIREMENTS.md HSP-01..06 marked done
    - All three docs committed in a single commit titled `docs(13): phase 13 complete — closeout`
  </acceptance_criteria>
  <done>Phase 13 documentation complete; STATE/ROADMAP/REQUIREMENTS reflect closure.</done>
</task>

</tasks>

<verification>
- Regression log shows all 9 suites green
- 13-MANUAL-QA-CHECKLIST.md filled with 9/9 PASS (or surfaced failures)
- 13-SUMMARY.md authored with full HSP coverage table
- STATE.md, ROADMAP.md, REQUIREMENTS.md all reflect Phase 13 completion
- Commit history shows clear closeout commit
</verification>

<success_criteria>
1. All tests green across both apps
2. Manual QA documented and confirmed by user
3. Phase 13 SUMMARY documents all 6 HSP requirements as DONE
4. STATE.md, ROADMAP.md, REQUIREMENTS.md updated
5. Phase 13 marked complete in roadmap
</success_criteria>

<threat_model>
- **Test pollution from prior runs:** Migrations and seeded data persist across vitest runs. If Plan 13-04's manual upload tests left R2 objects, subsequent runs that count `hotel_photos.findMany` may see extra rows. Mitigation: tests should clean up their own fixtures; manual QA assumes a known baseline state.
- **Manual QA blocked by environment:** If R2 credentials are not in the local `.env`, Plan 13-04's upload scenario fails. Mitigation: scenario 6 explicitly requires the same R2 bucket already validated by Phase 2 — if Phase 2 works locally, Phase 13 will work.
- **Audit log row count after migration:** Initial state is 0 rows. After scenario 1, exactly 1 row should appear (assuming the only change is `hotelName`). If multiple fields change in one save, fieldsChanged should reflect all of them. The audit log is INSERT-only — no UPDATE/DELETE paths from app code.
- **Documentation drift:** STATE.md decisions list is append-only. If an executor mis-formats an entry, downstream agents may parse it oddly. Use the verbatim bullet template from past phases.
</threat_model>

<output>
After completion, the SUMMARY itself IS the closeout artifact. No additional file needed.
</output>
