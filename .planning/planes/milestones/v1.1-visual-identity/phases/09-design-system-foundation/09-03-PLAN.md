---
phase: 09-design-system-foundation
plan: 03
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - apps/web/src/components/ui/badge.tsx
  - apps/web/src/components/ui/status-pill.tsx
  - apps/web/src/components/ui/theme-toggle.tsx
  - apps/web/src/hooks/useTheme.ts
  - apps/web/index.html
autonomous: true
requirements: [VIS-03, VIS-04, VIS-05]
must_haves:
  truths:
    - "Badge component renders 7 variants (default + 6 status states) using bundle status utility classes"
    - "StatusPill wraps Badge, accepts a typed `status` prop, and renders Spanish labels (Disponible/Reservada/Ocupada/Limpieza/Mantenimiento/Bloqueada)"
    - "ThemeToggle button reads current theme via useTheme hook, displays Sun icon when light / Moon icon when dark"
    - "Clicking ThemeToggle toggles `data-theme=dark` attribute on `<html>` AND writes localStorage('hos-theme', newValue)"
    - "On page load before React mounts, an inline script in index.html reads localStorage('hos-theme') (and falls back to prefers-color-scheme) and sets data-theme synchronously — no FOUC"
    - "Theme persists across hard refresh — refreshing while in dark mode keeps dark mode active"
  artifacts:
    - path: "apps/web/src/components/ui/badge.tsx"
      provides: "Badge primitive with CVA variants — default + 6 status states (available/reserved/occupied/cleaning/maintenance/blocked)"
      contains: "badgeVariants"
    - path: "apps/web/src/components/ui/status-pill.tsx"
      provides: "Semantic wrapper over Badge — type-safe status prop, Spanish labels, dot indicator"
      contains: "RoomStatus"
    - path: "apps/web/src/components/ui/theme-toggle.tsx"
      provides: "Theme switch button — Sun/Moon icon, uses useTheme hook"
      contains: "ThemeToggle"
    - path: "apps/web/src/hooks/useTheme.ts"
      provides: "useTheme hook — reads/writes localStorage('hos-theme'), sets data-theme on <html>, respects prefers-color-scheme on first visit"
      contains: "STORAGE_KEY"
    - path: "apps/web/index.html"
      provides: "Inline FOUC-prevention script BEFORE React mounts — reads localStorage + matchMedia, sets data-theme synchronously"
      contains: "localStorage.getItem('hos-theme')"
  key_links:
    - from: "apps/web/src/components/ui/theme-toggle.tsx"
      to: "apps/web/src/hooks/useTheme.ts"
      via: "import { useTheme } from '@/hooks/useTheme'"
      pattern: "import.*useTheme.*from.*hooks/useTheme"
    - from: "apps/web/src/components/ui/status-pill.tsx"
      to: "apps/web/src/components/ui/badge.tsx"
      via: "import { Badge } from './badge'"
      pattern: "import.*Badge.*from.*['\"]\\./badge"
    - from: "apps/web/index.html FOUC script"
      to: ".hos[data-theme=dark] block in globals.css"
      via: "document.documentElement.setAttribute('data-theme', 'dark')"
      pattern: "setAttribute\\(['\"]data-theme['\"]"
threat_model:
  scope: "Frontend-only — adds 5 new files and ~6 lines to index.html. No backend, no auth, no PII. localStorage is the only persistence surface; key is `hos-theme` (single value, enum `light|dark`). No XSS risk: inline script in index.html is statically authored (no user input concatenation) and writes only a hardcoded attribute name."
  asvs_gates: "ASVS 5.1 (input validation): N/A — only reads a localStorage value compared against `'dark'` string literal. ASVS 14.2 (CSP): inline `<script>` is required for FOUC prevention; if a strict CSP is later introduced, this script will need a nonce or hash exception — document this in the SUMMARY."
---

<objective>
Complete the design system foundation by delivering the three remaining primitives plus the dark-mode runtime:

1. `Badge` — new shadcn-style primitive with 6 status variants matching bundle `data-status` pattern (VIS-04, VIS-05).
2. `StatusPill` — semantic wrapper for room-status display, consumed by Phase 11 Calendar/Rooms/Housekeeping (VIS-04).
3. `ThemeToggle` + `useTheme` hook + inline FOUC script — complete the dark mode toggle stack (VIS-03).

Purpose: Without Badge, VIS-05 is not satisfied (the 4th primitive). Without StatusPill + status variants, VIS-04 has no consumer surface — Phase 11 will be unable to render the 6-state pattern consistently. Without ThemeToggle + FOUC script, VIS-03 is not satisfied (toggle persists, no flash on refresh).

Output: 5 new files + 1 edit to index.html. ThemeToggle does NOT have to be mounted yet — placement is Phase 11's job. This plan delivers the component, the hook, and the FOUC guard.
</objective>

<execution_context>
@C:\Users\Admin\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Admin\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/09-design-system-foundation/09-CONTEXT.md
@.planning/phases/09-design-system-foundation/09-RESEARCH.md
@.planning/phases/09-design-system-foundation/09-01-PLAN.md
@.design-fetch/hotelos-ai/project/tokens.jsx
@apps/web/index.html
@apps/web/src/components/ui/button.tsx
@apps/web/src/lib/utils.ts

<interfaces>
Available types and utilities (the executor uses these directly):

From apps/web/src/lib/utils.ts:
  export function cn(...inputs: ClassValue[]): string;

From class-variance-authority (already installed, used by Button):
  import { cva, type VariantProps } from 'class-variance-authority';

From lucide-react (already installed):
  import { Sun, Moon } from 'lucide-react';

Bundle status utilities (declared in globals.css @theme inline after Plan 09-01):
- bg-status-available-bg, text-status-available
- bg-status-reserved-bg,  text-status-reserved
- bg-status-occupied-bg,  text-status-occupied
- bg-status-cleaning-bg,  text-status-cleaning
- bg-status-maintenance-bg, text-status-maintenance
- bg-status-blocked-bg,   text-status-blocked

Bundle pill structure (tokens.jsx lines 255-269):
- Base: inline-flex items-center gap-1.5 (6px), px-2.5 py-0.5, rounded-full, text-xs (~11.5px), font-medium
- A `dot` child span 6×6px, background currentColor, rounded-full

Bundle status FG hex (Plan 09-01 lock): cleaning FG uses var(--status-cleaning) = #d4a23a (NOT #a8801c)

Spanish status labels (from tokens.jsx lines 460-467):
- available: 'Disponible'
- reserved:  'Reservada'
- occupied:  'Ocupada'
- cleaning:  'Limpieza'
- maintenance: 'Mantenimiento'
- blocked:   'Bloqueada'

RoomStatus type (canonical — used by Phase 11):
  export type RoomStatus = 'available' | 'reserved' | 'occupied' | 'cleaning' | 'maintenance' | 'blocked';
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Badge primitive with CVA + 6 status variants</name>
  <files>apps/web/src/components/ui/badge.tsx</files>
  <read_first>
    - apps/web/src/components/ui/button.tsx (reference for CVA + forwardRef pattern)
    - apps/web/src/lib/utils.ts (cn helper)
    - .design-fetch/hotelos-ai/project/tokens.jsx (lines 255-269 — .hos-pill + data-status patterns)
    - .planning/phases/09-design-system-foundation/09-RESEARCH.md (Pattern 5 — Badge structure recommendation; "Approach A — CVA variants with Tailwind token utilities")
    - apps/web/src/styles/globals.css (confirm Plan 09-01 declared bg-status-{name}-bg and text-status-{name} utilities)
  </read_first>
  <behavior>
    - File apps/web/src/components/ui/badge.tsx exists.
    - Exports Badge (HTMLSpanElement-based), badgeVariants (cva output), BadgeProps.
    - 7 variant keys in CVA: default (neutral warm-cream bg + ink-2 text), available, reserved, occupied, cleaning, maintenance, blocked.
    - Each status variant applies bg-status-{name}-bg text-status-{name} Tailwind utilities.
    - defaultVariants.variant = 'default'.
    - Base classes: inline-flex items-center gap-1.5 rounded-full px-2.5 py-0.5 text-xs font-medium transition-colors.
    - Badge accepts standard HTMLSpanElement props (children, className, onClick, ...) via spread.
    - File has zero hex, zero Tailwind palette colors, zero inline styles.
  </behavior>
  <action>
    Create apps/web/src/components/ui/badge.tsx with exactly this structure (verbatim — do not add/remove imports or variants):

      import * as React from 'react';
      import { cva, type VariantProps } from 'class-variance-authority';
      import { cn } from '@/lib/utils';

      const badgeVariants = cva(
        'inline-flex items-center gap-1.5 rounded-full px-2.5 py-0.5 text-xs font-medium transition-colors',
        {
          variants: {
            variant: {
              default:     'bg-warm-cream text-ink-2',
              available:   'bg-status-available-bg text-status-available',
              reserved:    'bg-status-reserved-bg text-status-reserved',
              occupied:    'bg-status-occupied-bg text-status-occupied',
              cleaning:    'bg-status-cleaning-bg text-status-cleaning',
              maintenance: 'bg-status-maintenance-bg text-status-maintenance',
              blocked:     'bg-status-blocked-bg text-status-blocked',
            },
          },
          defaultVariants: { variant: 'default' },
        },
      );

      export interface BadgeProps
        extends React.HTMLAttributes<HTMLSpanElement>,
          VariantProps<typeof badgeVariants> {}

      const Badge = React.forwardRef<HTMLSpanElement, BadgeProps>(
        ({ className, variant, ...props }, ref) => (
          <span
            ref={ref}
            className={cn(badgeVariants({ variant }), className)}
            {...props}
          />
        ),
      );
      Badge.displayName = 'Badge';

      export { Badge, badgeVariants };

    Do NOT add a data-status attribute selector approach in CSS — the RESEARCH locked "Approach A" (CVA + Tailwind utilities). StatusPill (Task 2) will forward data-status as a data attribute for analytics/CSS hooks, but the color comes from the CVA variant class only.
  </action>
  <verify>rg "badgeVariants" apps/web/src/components/ui/badge.tsx ; cd apps/web ; pnpm tsc --noEmit -p tsconfig.json</verify>
  <acceptance_criteria>
    - apps/web/src/components/ui/badge.tsx exists
    - `rg "export\s+\{\s*Badge,\s*badgeVariants\s*\}" apps/web/src/components/ui/badge.tsx` returns exactly one match
    - `rg "available:\s+'bg-status-available-bg text-status-available'" apps/web/src/components/ui/badge.tsx` returns exactly one match
    - `rg "blocked:\s+'bg-status-blocked-bg text-status-blocked'" apps/web/src/components/ui/badge.tsx` returns exactly one match
    - `rg "#[0-9a-fA-F]{3,6}" apps/web/src/components/ui/badge.tsx` returns zero matches
    - `rg "text-(blue|red|green|yellow|purple|pink|gray|slate|zinc|neutral|stone|amber|orange|lime|emerald|teal|cyan|sky|violet|fuchsia|rose|indigo)-[0-9]" apps/web/src/components/ui/badge.tsx` returns zero matches
    - `cd apps/web && pnpm tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
  <done>Badge primitive exists with 7 variants; types compile; bundle utilities used exclusively.</done>
</task>

<task type="auto">
  <name>Task 2: Create StatusPill component (semantic wrapper over Badge)</name>
  <files>apps/web/src/components/ui/status-pill.tsx</files>
  <read_first>
    - apps/web/src/components/ui/badge.tsx (must already exist from Task 1)
    - .design-fetch/hotelos-ai/project/tokens.jsx (lines 460-474 — HOS_STATUS_LABELS + HosStatusPill reference)
    - .planning/phases/09-design-system-foundation/09-RESEARCH.md (status-pill.tsx code example)
  </read_first>
  <action>
    Create apps/web/src/components/ui/status-pill.tsx with this exact structure:

      import * as React from 'react';
      import { Badge } from './badge';
      import { cn } from '@/lib/utils';

      export type RoomStatus =
        | 'available'
        | 'reserved'
        | 'occupied'
        | 'cleaning'
        | 'maintenance'
        | 'blocked';

      const STATUS_LABELS: Record<RoomStatus, string> = {
        available:   'Disponible',
        reserved:    'Reservada',
        occupied:    'Ocupada',
        cleaning:    'Limpieza',
        maintenance: 'Mantenimiento',
        blocked:     'Bloqueada',
      };

      export interface StatusPillProps
        extends Omit<React.HTMLAttributes<HTMLSpanElement>, 'children'> {
        status: RoomStatus;
        showDot?: boolean;
        label?: string;
      }

      const StatusPill = React.forwardRef<HTMLSpanElement, StatusPillProps>(
        ({ status, showDot = true, label, className, ...props }, ref) => (
          <Badge
            ref={ref}
            variant={status}
            data-status={status}
            className={cn(className)}
            {...props}
          >
            {showDot && (
              <span
                aria-hidden="true"
                className="inline-block h-1.5 w-1.5 rounded-full bg-current"
              />
            )}
            {label ?? STATUS_LABELS[status]}
          </Badge>
        ),
      );
      StatusPill.displayName = 'StatusPill';

      export { StatusPill, STATUS_LABELS };

    Key implementation details:
    - data-status attribute IS forwarded to the rendered span — kept for future CSS targeting and analytics.
    - The dot uses bg-current (resolves to the CVA-applied text color = status fg).
    - label prop optional — when provided, overrides the Spanish default.
    - showDot defaults to true; can be disabled for compact contexts.
    - No new dependencies; no hex; no palette colors; no inline styles.
  </action>
  <verify>rg "export type RoomStatus" apps/web/src/components/ui/status-pill.tsx ; rg "STATUS_LABELS" apps/web/src/components/ui/status-pill.tsx ; cd apps/web ; pnpm tsc --noEmit -p tsconfig.json</verify>
  <acceptance_criteria>
    - apps/web/src/components/ui/status-pill.tsx exists
    - `rg "export type RoomStatus" apps/web/src/components/ui/status-pill.tsx` returns exactly one match
    - `rg "'Disponible'|'Reservada'|'Ocupada'|'Limpieza'|'Mantenimiento'|'Bloqueada'" apps/web/src/components/ui/status-pill.tsx` returns at least 6 matches
    - `rg "data-status=" apps/web/src/components/ui/status-pill.tsx` returns at least one match
    - `rg "import.*Badge.*from.*['\"]\\./badge['\"]" apps/web/src/components/ui/status-pill.tsx` returns exactly one match
    - `rg "#[0-9a-fA-F]{3,6}" apps/web/src/components/ui/status-pill.tsx` returns zero matches
    - `cd apps/web && pnpm tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
  <done>StatusPill semantic wrapper exists; type-safe RoomStatus prop; Spanish labels; type-check passes.</done>
</task>

<task type="auto">
  <name>Task 3: Create useTheme hook and ThemeToggle component</name>
  <files>apps/web/src/hooks/useTheme.ts, apps/web/src/components/ui/theme-toggle.tsx</files>
  <read_first>
    - .planning/phases/09-design-system-foundation/09-RESEARCH.md (Pattern 4 — useTheme hook code; FOUC prevention pattern)
    - apps/web/src/components/ui/button.tsx (Button — ThemeToggle wraps it)
    - apps/web/index.html (FOUC script target — handled in Task 4)
  </read_first>
  <action>
    Step 3a — Create the hook at apps/web/src/hooks/useTheme.ts:

      import { useEffect, useState } from 'react';

      const STORAGE_KEY = 'hos-theme';
      export type Theme = 'light' | 'dark';

      function getInitialTheme(): Theme {
        if (typeof window === 'undefined') return 'light';
        const stored = window.localStorage.getItem(STORAGE_KEY);
        if (stored === 'dark' || stored === 'light') return stored;
        return window.matchMedia('(prefers-color-scheme: dark)').matches
          ? 'dark'
          : 'light';
      }

      export function useTheme() {
        const [theme, setTheme] = useState<Theme>(getInitialTheme);

        useEffect(() => {
          const root = document.documentElement;
          if (theme === 'dark') {
            root.setAttribute('data-theme', 'dark');
          } else {
            root.removeAttribute('data-theme');
          }
          try {
            window.localStorage.setItem(STORAGE_KEY, theme);
          } catch {
            /* localStorage unavailable (private mode etc.) — non-fatal */
          }
        }, [theme]);

        const toggle = () => setTheme((t) => (t === 'dark' ? 'light' : 'dark'));

        return { theme, setTheme, toggle };
      }

      export { STORAGE_KEY };

    Step 3b — Create the component at apps/web/src/components/ui/theme-toggle.tsx:

      import * as React from 'react';
      import { Sun, Moon } from 'lucide-react';
      import { Button } from './button';
      import { useTheme } from '@/hooks/useTheme';
      import { cn } from '@/lib/utils';

      export interface ThemeToggleProps
        extends React.ButtonHTMLAttributes<HTMLButtonElement> {}

      const ThemeToggle = React.forwardRef<HTMLButtonElement, ThemeToggleProps>(
        ({ className, ...props }, ref) => {
          const { theme, toggle } = useTheme();
          const isDark = theme === 'dark';
          const label = isDark ? 'Cambiar a modo claro' : 'Cambiar a modo oscuro';

          return (
            <Button
              ref={ref}
              type="button"
              variant="ghost"
              size="icon"
              aria-label={label}
              title={label}
              onClick={toggle}
              className={cn(className)}
              {...props}
            >
              {isDark ? (
                <Sun aria-hidden="true" className="h-4 w-4" />
              ) : (
                <Moon aria-hidden="true" className="h-4 w-4" />
              )}
            </Button>
          );
        },
      );
      ThemeToggle.displayName = 'ThemeToggle';

      export { ThemeToggle };

    Notes:
    - Button is from Plan 09-02 (refactored, uses bundle tokens). ThemeToggle adds no new color classes.
    - Hook uses lazy useState initializer so SSR (if ever added) does not crash — typeof window === 'undefined' guard returns 'light'.
    - The hook does NOT mount the toggle anywhere — placement is Phase 11's call (Sidebar footer per CONTEXT.md "Specific Ideas"). Plan 09-04 demo route mounts it explicitly for verification.
    - Spanish aria-label per project language convention (Spanish UI throughout — see Phase 5/6/7 patterns).
  </action>
  <verify>rg "STORAGE_KEY\s*=\s*'hos-theme'" apps/web/src/hooks/useTheme.ts ; rg "setAttribute\('data-theme'" apps/web/src/hooks/useTheme.ts ; rg "ThemeToggle" apps/web/src/components/ui/theme-toggle.tsx ; cd apps/web ; pnpm tsc --noEmit -p tsconfig.json</verify>
  <acceptance_criteria>
    - apps/web/src/hooks/useTheme.ts exists
    - apps/web/src/components/ui/theme-toggle.tsx exists
    - `rg "STORAGE_KEY\s*=\s*'hos-theme'" apps/web/src/hooks/useTheme.ts` returns exactly one match
    - `rg "setAttribute\('data-theme',\s*'dark'\)" apps/web/src/hooks/useTheme.ts` returns exactly one match
    - `rg "removeAttribute\('data-theme'\)" apps/web/src/hooks/useTheme.ts` returns exactly one match
    - `rg "localStorage.setItem\(STORAGE_KEY" apps/web/src/hooks/useTheme.ts` returns exactly one match
    - `rg "import.*useTheme.*from\s*['\"]@/hooks/useTheme['\"]" apps/web/src/components/ui/theme-toggle.tsx` returns exactly one match
    - `rg "import\s*\{\s*Sun,\s*Moon\s*\}\s*from\s*'lucide-react'" apps/web/src/components/ui/theme-toggle.tsx` returns exactly one match
    - `rg "#[0-9a-fA-F]{3,6}" apps/web/src/hooks/useTheme.ts apps/web/src/components/ui/theme-toggle.tsx` returns zero matches
    - `cd apps/web && pnpm tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
  <done>useTheme hook + ThemeToggle component exist; both type-check; toggle reads/writes localStorage and data-theme attribute.</done>
</task>

<task type="auto">
  <name>Task 4: Add inline FOUC-prevention script to index.html</name>
  <files>apps/web/index.html</files>
  <read_first>
    - apps/web/index.html (current file — already has class="hos" on <html> from Plan 09-01)
    - .planning/phases/09-design-system-foundation/09-RESEARCH.md (FOUC prevention pattern + Example 2)
  </read_first>
  <action>
    Edit apps/web/index.html. The current file (after Plan 09-01 lands) is:

      <!DOCTYPE html>
      <html lang="es" class="hos">
      <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>HotelOS AI</title>
        <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
      </head>
      <body>
        <div id="root"></div>
        <script type="module" src="/src/main.tsx"></script>
      </body>
      </html>

    Insert the FOUC-prevention script as the FIRST element inside <head> (BEFORE meta charset, BEFORE the viewport meta, BEFORE the title — so the attribute is set on documentElement as early as possible). The script is intentionally tiny and self-contained:

      <script>
        (function () {
          try {
            var t = localStorage.getItem('hos-theme');
            if (t === 'dark' || (!t && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
              document.documentElement.setAttribute('data-theme', 'dark');
            }
          } catch (e) { /* localStorage blocked — fall back to light */ }
        })();
      </script>

    The final file becomes:

      <!DOCTYPE html>
      <html lang="es" class="hos">
      <head>
        <script>
          (function () {
            try {
              var t = localStorage.getItem('hos-theme');
              if (t === 'dark' || (!t && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
                document.documentElement.setAttribute('data-theme', 'dark');
              }
            } catch (e) { /* localStorage blocked — fall back to light */ }
          })();
        </script>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>HotelOS AI</title>
        <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
      </head>
      <body>
        <div id="root"></div>
        <script type="module" src="/src/main.tsx"></script>
      </body>
      </html>

    Notes:
    - The script reads ONLY the hardcoded key 'hos-theme' and compares against the literal 'dark'. No user input is concatenated. No XSS surface.
    - The try/catch guards against private-mode browsers where localStorage throws.
    - Place BEFORE other <head> children so the attribute is set the instant the parser hits documentElement.
    - The CSP exception note (ASVS 14.2): if a strict CSP is later added, this inline script will need a nonce or hash exception — flag this in the SUMMARY for Phase 11 to address if needed.
  </action>
  <verify>rg "localStorage.getItem\('hos-theme'\)" apps/web/index.html ; rg "setAttribute\('data-theme',\s*'dark'\)" apps/web/index.html</verify>
  <acceptance_criteria>
    - `rg "localStorage.getItem\('hos-theme'\)" apps/web/index.html` returns exactly one match
    - `rg "setAttribute\('data-theme',\s*'dark'\)" apps/web/index.html` returns exactly one match
    - `rg "prefers-color-scheme: dark" apps/web/index.html` returns exactly one match
    - `rg "<html lang=\"es\" class=\"hos\">" apps/web/index.html` returns exactly one match (still present from Plan 09-01)
    - The `<script>` block appears BEFORE `<meta charset>` (verify visually by reading the file post-edit; line number of `<script>` < line number of `<meta charset`)
    - `cd apps/web && pnpm vite build` exits 0
  </acceptance_criteria>
  <done>FOUC-prevention script is the first child of head; toggles data-theme synchronously; build passes.</done>
</task>

</tasks>

<verification>
After all 4 tasks:

1. Type-check passes: `cd apps/web && pnpm tsc --noEmit -p tsconfig.json` exits 0
2. Build passes: `cd apps/web && pnpm vite build` exits 0
3. Phase 9 scope (components/ui + hooks) is hex-free:
   `rg "#[0-9a-fA-F]{3,6}" apps/web/src/components/ui apps/web/src/hooks` returns zero matches
4. Phase 9 scope is palette-color-free:
   `rg "text-(blue|red|green|yellow|purple|pink|gray|slate|zinc|neutral|stone|amber|orange|lime|emerald|teal|cyan|sky|violet|fuchsia|rose|indigo)-[0-9]" apps/web/src/components/ui apps/web/src/hooks` returns zero matches
5. ThemeToggle wires to useTheme:
   `rg "import.*useTheme.*from\s*['\"]@/hooks/useTheme['\"]" apps/web/src/components/ui/theme-toggle.tsx` returns exactly one match
6. FOUC script present:
   `rg "localStorage.getItem\('hos-theme'\)" apps/web/index.html` returns exactly one match
</verification>

<success_criteria>
- VIS-03: ThemeToggle reads/writes localStorage('hos-theme'); useTheme sets data-theme on <html>; FOUC script applies dark mode synchronously before React mounts.
- VIS-04: Badge has 6 status variants with bundle utilities; StatusPill provides a typed wrapper with Spanish labels.
- VIS-05: Badge is the 4th primitive (joining Button + Card + Input from Plan 09-02) refactored to read tokens. No hex, no inline color, no Tailwind palette.
</success_criteria>

<output>
After completion, create `.planning/phases/09-design-system-foundation/09-03-SUMMARY.md` covering:
  - Files created (5 new) + 1 edit (index.html)
  - Decision: Approach A (CVA + Tailwind utilities) over CSS attribute-selector for Badge variants — locked in RESEARCH
  - Decision: STATUS_LABELS uses Spanish (Disponible, Reservada, Ocupada, Limpieza, Mantenimiento, Bloqueada) — matches bundle and Phase 5/6/7 UI language
  - CSP note: inline FOUC script will need nonce/hash if strict CSP is added later (ASVS 14.2 flag for Phase 11)
  - Mount placement deferred: ThemeToggle component exists but is NOT mounted in StaffLayout — Phase 11 owns placement (Sidebar footer per CONTEXT.md)
  - Verification command outputs (zero hex, zero palette colors, type-check passes)
</output>
