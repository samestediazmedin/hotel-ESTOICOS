---
phase: 09-design-system-foundation
plan: 04
type: execute
wave: 3
depends_on: [09-01, 09-02, 09-03]
files_modified:
  - apps/web/src/features/design-system/DesignSystemPage.tsx
  - apps/web/src/features/design-system/DesignSystemPage.test.tsx
  - apps/web/src/router.tsx
autonomous: true
requirements: [VIS-01, VIS-02, VIS-03, VIS-04, VIS-05]
must_haves:
  truths:
    - "Visiting /design-system renders a single-page demo with all 4 primitives + 6 status pills + theme toggle"
    - "Page renders without console errors — every Tailwind utility used by Button, Card, Input, Badge resolves to a defined bundle token (proves Plan 09-01 utilities exist)"
    - "Headings on the page render in Instrument Serif (computed style font-family matches `Instrument Serif`-prefixed stack)"
    - "Clicking the ThemeToggle button flips data-theme on <html> within one paint (verified by attribute assertion after userEvent.click)"
    - "All 6 StatusPills render with the bundle hex foreground/background pairs in light mode (verified by computed style assertion)"
    - "Route is reachable in development; demo page exists ahead of Phase 11 consumption"
  artifacts:
    - path: "apps/web/src/features/design-system/DesignSystemPage.tsx"
      provides: "Dev-only design-system demo page rendering Button (7 variants), Card, Input, Badge (7 variants), 6 StatusPills, ThemeToggle, typography specimens, warm-neutral swatches"
      contains: "DesignSystemPage"
    - path: "apps/web/src/features/design-system/DesignSystemPage.test.tsx"
      provides: "Vitest + Testing Library smoke test — renders page, clicks ThemeToggle, asserts data-theme flip, asserts all 6 status variants present, asserts no console errors"
      contains: "describe('DesignSystemPage'"
    - path: "apps/web/src/router.tsx"
      provides: "Router updated to mount /design-system route (OUTSIDE ProtectedRoute — visible without auth in dev)"
      contains: "design-system"
  key_links:
    - from: "apps/web/src/features/design-system/DesignSystemPage.tsx"
      to: "Button, Card, Input, Badge, StatusPill, ThemeToggle (Plans 09-02 + 09-03)"
      via: "named imports from @/components/ui/*"
      pattern: "from\\s+['\"]@/components/ui/(button|card|input|badge|status-pill|theme-toggle)['\"]"
    - from: "apps/web/src/router.tsx"
      to: "apps/web/src/features/design-system/DesignSystemPage.tsx"
      via: "route at path 'design-system'"
      pattern: "path:\\s*['\"]design-system['\"]"
threat_model:
  scope: "Frontend-only demo route. Path /design-system is publicly accessible in development; gated by an env check (import.meta.env.DEV) so it 404s in production builds. No backend calls, no PII, no inputs collected from the page itself."
  asvs_gates: "ASVS 4.1 (access control): demo route is dev-only — production builds short-circuit to a 404 redirect. No sensitive data exposed even if accidentally shipped."
---

<objective>
Provide the "thin vertical slice" verification surface for Phase 9 — a single dev-only route that mounts every primitive, every status variant, and the theme toggle on one page. This proves the foundation works end-to-end before Phase 10/11 consume it.

Purpose: Without this plan, Phase 9 ships a foundation no one has rendered. The MVP_MODE rule for foundation phases is to demonstrate the foundation works via a thin slice. This page is also the de facto "Phase 11 reference" — when implementers restyle screens, they will look here to see correct token usage.

Output: One page component, one test, three lines in router.tsx. Page is dev-only (gated by `import.meta.env.DEV`) and will be removed or moved by Phase 11 when real screens consume the primitives.
</objective>

<execution_context>
@C:\Users\Admin\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Admin\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/09-design-system-foundation/09-CONTEXT.md
@.planning/phases/09-design-system-foundation/09-RESEARCH.md
@.planning/phases/09-design-system-foundation/09-01-PLAN.md
@.planning/phases/09-design-system-foundation/09-02-PLAN.md
@.planning/phases/09-design-system-foundation/09-03-PLAN.md
@.design-fetch/hotelos-ai/project/tokens.jsx
@apps/web/src/router.tsx
@apps/web/src/components/ui/button.tsx
@apps/web/src/components/ui/card.tsx
@apps/web/src/components/ui/input.tsx
@apps/web/src/components/ui/badge.tsx
@apps/web/src/components/ui/status-pill.tsx
@apps/web/src/components/ui/theme-toggle.tsx
@apps/web/src/hooks/useTheme.ts

<interfaces>
Components and types available after Plans 09-01 / 09-02 / 09-03:

From @/components/ui/button:
  Button (variants: default | terracotta | destructive | outline | secondary | ghost | link; sizes: default | sm | lg | icon)

From @/components/ui/card:
  Card, CardHeader, CardFooter, CardTitle, CardDescription, CardContent

From @/components/ui/input:
  Input (extends standard <input>)

From @/components/ui/badge:
  Badge, badgeVariants (variants: default | available | reserved | occupied | cleaning | maintenance | blocked)

From @/components/ui/status-pill:
  StatusPill, STATUS_LABELS, type RoomStatus

From @/components/ui/theme-toggle:
  ThemeToggle

From @/hooks/useTheme:
  useTheme, type Theme, STORAGE_KEY

Router structure (from existing apps/web/src/router.tsx):
  createBrowserRouter at line 49, top-level AppWrapper element. Public routes (login, /booking, /concierge) sit alongside ProtectedRoute. The demo route goes at top level (sibling of /login, NOT inside ProtectedRoute) so auth is not required.

Testing utilities (already installed):
  vitest, @testing-library/react, @testing-library/user-event, @testing-library/jest-dom
  Test env: jsdom (declared in vite.config.ts test block)
  Pattern reference: existing tests under apps/web/src/features/**/*.test.tsx (Phase 6/7/8 have examples)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create DesignSystemPage demo component</name>
  <files>apps/web/src/features/design-system/DesignSystemPage.tsx</files>
  <read_first>
    - apps/web/src/components/ui/button.tsx (variant names)
    - apps/web/src/components/ui/card.tsx (composition pattern)
    - apps/web/src/components/ui/input.tsx (props)
    - apps/web/src/components/ui/badge.tsx (variants)
    - apps/web/src/components/ui/status-pill.tsx (RoomStatus values)
    - apps/web/src/components/ui/theme-toggle.tsx
    - .design-fetch/hotelos-ai/project/tokens.jsx (lines 18-91 — for swatch reference; lines 94-102 — for typography specimens)
    - .planning/phases/09-design-system-foundation/09-RESEARCH.md (Walking Skeleton / MVP Mode Reconciliation section — demo page spec)
  </read_first>
  <behavior>
    - Single default export `DesignSystemPage` from `apps/web/src/features/design-system/DesignSystemPage.tsx`.
    - Renders a heading "Sistema de diseño" using Instrument Serif (relies on .hos h1 base styles from Plan 09-01).
    - Renders a `<ThemeToggle />` in the top-right corner of the page header.
    - Renders 6 sections, each titled with an h2 in Instrument Serif:
        1. "Botones" — Button × 7 variants × 4 sizes grid
        2. "Tarjetas" — one Card with CardHeader/Title/Description/Content/Footer demonstrating full composition
        3. "Inputs" — three Input fields (default, with placeholder, disabled)
        4. "Badges" — Badge × 7 variants (default + 6 status) side by side
        5. "Estados de habitación (StatusPill)" — StatusPill for all 6 RoomStatus values with Spanish labels visible
        6. "Tipografía y paleta cálida" — typography specimens (h1/h2/h3/h4/body/.num/.mono) + swatches for warm-white, warm-paper, warm-cream, warm-tan
    - Each swatch is a div with `bg-warm-{name}` Tailwind class and a label inside (NOT inline-styled hex — the executor uses Tailwind utilities exclusively to prove the foundation works).
    - All status sample colors are wrapped in their respective `bg-status-{name}-bg text-status-{name}` utilities for inline color demonstration (apart from the StatusPills themselves).
    - Page wraps content in `<main className="min-h-screen bg-warm-paper p-8">` so the bundle warm background paints the full viewport.
    - Page has a `data-testid="design-system-page"` attribute on the outer container for testability.
    - Each StatusPill has a `data-testid="status-pill-{status}"` attribute (6 total).
    - The ThemeToggle has the default aria-label provided by Plan 09-03 ("Cambiar a modo oscuro" or "Cambiar a modo claro") — Task 2 test relies on this.
    - File has zero hex, zero Tailwind palette colors, zero inline `style={{ color }}`. All colors come from bundle utilities.
  </behavior>
  <action>
    Create directory `apps/web/src/features/design-system/` and the file `DesignSystemPage.tsx` with the following structure (executor may adjust layout/spacing details — these are not load-bearing — but variants, prop usage, testids, and section structure MUST be preserved):

    Module: apps/web/src/features/design-system/DesignSystemPage.tsx
    - Import React (default + named as needed)
    - Import Button from '@/components/ui/button'
    - Import { Card, CardHeader, CardFooter, CardTitle, CardDescription, CardContent } from '@/components/ui/card'
    - Import { Input } from '@/components/ui/input'
    - Import { Badge } from '@/components/ui/badge'
    - Import { StatusPill, STATUS_LABELS, type RoomStatus } from '@/components/ui/status-pill'
    - Import { ThemeToggle } from '@/components/ui/theme-toggle'

    Constants inside the module:
      const BUTTON_VARIANTS = ['default','terracotta','destructive','outline','secondary','ghost','link'] as const;
      const BUTTON_SIZES = ['default','sm','lg','icon'] as const;
      const BADGE_VARIANTS = ['default','available','reserved','occupied','cleaning','maintenance','blocked'] as const;
      const ROOM_STATUSES: RoomStatus[] = ['available','reserved','occupied','cleaning','maintenance','blocked'];
      const WARM_SWATCHES = [
        { name: 'warm-white', class: 'bg-warm-white border border-warm-line' },
        { name: 'warm-paper', class: 'bg-warm-paper border border-warm-line' },
        { name: 'warm-cream', class: 'bg-warm-cream border border-warm-line' },
        { name: 'warm-tan',   class: 'bg-warm-tan border border-warm-line' },
      ];

    Layout structure:
      <main data-testid="design-system-page" className="min-h-screen bg-warm-paper p-8">
        <header className="flex items-center justify-between mb-8">
          <div>
            <h1 className="text-4xl">Sistema de diseño</h1>
            <p className="text-ink-3 text-sm mt-2">Phase 9 — Foundation primitives, status tokens, theme toggle</p>
          </div>
          <ThemeToggle />
        </header>

        <section className="mb-10">
          <h2 className="text-2xl mb-4">Botones</h2>
          <div className="grid grid-cols-[auto_1fr] gap-4 items-center">
            {/* For each size, render row with size label + Button for each variant */}
            {BUTTON_SIZES.map(size => (
              <React.Fragment key={size}>
                <span className="text-ink-3 mono text-xs">{size}</span>
                <div className="flex flex-wrap gap-2">
                  {BUTTON_VARIANTS.map(v => (
                    <Button key={v} variant={v} size={size}>
                      {size === 'icon' ? '+' : v}
                    </Button>
                  ))}
                </div>
              </React.Fragment>
            ))}
          </div>
        </section>

        <section className="mb-10">
          <h2 className="text-2xl mb-4">Tarjetas</h2>
          <Card className="max-w-md">
            <CardHeader>
              <CardTitle>Habitación 204</CardTitle>
              <CardDescription>Suite deluxe · 2 huéspedes · vista al jardín</CardDescription>
            </CardHeader>
            <CardContent>
              <p className="text-sm">Pago confirmado. Listo para check-in a partir de las 15:00.</p>
            </CardContent>
            <CardFooter className="gap-2">
              <Button variant="terracotta" size="sm">Check-in</Button>
              <Button variant="outline" size="sm">Cancelar</Button>
            </CardFooter>
          </Card>
        </section>

        <section className="mb-10">
          <h2 className="text-2xl mb-4">Inputs</h2>
          <div className="grid gap-3 max-w-sm">
            <Input placeholder="Nombre del huésped" />
            <Input placeholder="Email" type="email" />
            <Input placeholder="Deshabilitado" disabled />
          </div>
        </section>

        <section className="mb-10">
          <h2 className="text-2xl mb-4">Badges</h2>
          <div className="flex flex-wrap gap-2">
            {BADGE_VARIANTS.map(v => (
              <Badge key={v} variant={v}>{v}</Badge>
            ))}
          </div>
        </section>

        <section className="mb-10">
          <h2 className="text-2xl mb-4">Estados de habitación (StatusPill)</h2>
          <div className="flex flex-wrap gap-3">
            {ROOM_STATUSES.map(s => (
              <StatusPill key={s} status={s} data-testid={`status-pill-${s}`} />
            ))}
          </div>
        </section>

        <section>
          <h2 className="text-2xl mb-4">Tipografía y paleta cálida</h2>
          <div className="grid gap-6">
            <div>
              <h1 className="text-4xl">H1 Instrument Serif</h1>
              <h2 className="text-3xl">H2 Instrument Serif</h2>
              <h3 className="text-2xl">H3 Instrument Serif</h3>
              <h4 className="text-xl">H4 Instrument Serif</h4>
              <p className="text-base">Body Geist — Hospitalidad operada con inteligencia.</p>
              <p className="num text-base">1,234,567.89 — número en Geist Mono tabular</p>
              <p className="mono text-sm">code() · Geist Mono · 0xC4623F</p>
            </div>
            <div className="grid grid-cols-4 gap-3 max-w-xl">
              {WARM_SWATCHES.map(sw => (
                <div key={sw.name} className="flex flex-col gap-2">
                  <div className={`${sw.class} h-16 rounded-lg`} />
                  <span className="text-xs text-ink-3 mono">{sw.name}</span>
                </div>
              ))}
            </div>
          </div>
        </section>
      </main>

    Export pattern: `export function DesignSystemPage() { return ( ... ); }` — named export, NOT default. (Matches existing feature pages like RoomsPage, DashboardPage.)

    No hex, no Tailwind palette colors, no inline color styles. All sizes use `h-*`/`w-*`/`p-*`/`m-*`/`gap-*`/`grid-*`/`flex-*`/`max-w-*` utilities, all colors use bundle token utilities only.
  </action>
  <verify>rg "export function DesignSystemPage" apps/web/src/features/design-system/DesignSystemPage.tsx ; rg "data-testid=\"design-system-page\"" apps/web/src/features/design-system/DesignSystemPage.tsx ; cd apps/web ; pnpm tsc --noEmit -p tsconfig.json</verify>
  <acceptance_criteria>
    - `apps/web/src/features/design-system/DesignSystemPage.tsx` exists
    - `rg "export function DesignSystemPage" apps/web/src/features/design-system/DesignSystemPage.tsx` returns exactly one match
    - `rg "data-testid=\"design-system-page\"" apps/web/src/features/design-system/DesignSystemPage.tsx` returns exactly one match
    - `rg "data-testid=\\{`status-pill-\\$\\{s\\}`\\}" apps/web/src/features/design-system/DesignSystemPage.tsx` returns exactly one match (the template-literal testid form)
    - `rg "ThemeToggle" apps/web/src/features/design-system/DesignSystemPage.tsx` returns at least one match
    - `rg "from\\s+['\"]@/components/ui/button['\"]" apps/web/src/features/design-system/DesignSystemPage.tsx` returns exactly one match
    - `rg "from\\s+['\"]@/components/ui/badge['\"]" apps/web/src/features/design-system/DesignSystemPage.tsx` returns exactly one match
    - `rg "from\\s+['\"]@/components/ui/status-pill['\"]" apps/web/src/features/design-system/DesignSystemPage.tsx` returns exactly one match
    - `rg "from\\s+['\"]@/components/ui/theme-toggle['\"]" apps/web/src/features/design-system/DesignSystemPage.tsx` returns exactly one match
    - `rg "#[0-9a-fA-F]{3,6}" apps/web/src/features/design-system/DesignSystemPage.tsx` returns zero matches (the "0xC4623F" string literal inside the mono specimen is rendered AS TEXT, not a hex VALUE — see action; that line uses the literal "0xC4623F" as content, but it must be inside a JSX text node, not a className or style. If the executor uses a different non-hex placeholder like "C4623F" or "TERRACOTTA", that is acceptable. SAFER ALTERNATIVE for the demo: change "0xC4623F" to "0xCODE-MONO" or "console.log()" so this acceptance criterion stays strict. Adopt the safer alternative.)
    - `rg "text-(blue|red|green|yellow|purple|pink|gray|slate|zinc|neutral|stone|amber|orange|lime|emerald|teal|cyan|sky|violet|fuchsia|rose|indigo)-[0-9]" apps/web/src/features/design-system/DesignSystemPage.tsx` returns zero matches
    - `rg "style=\\{\\{[^}]*color" apps/web/src/features/design-system/DesignSystemPage.tsx` returns zero matches
    - `cd apps/web && pnpm tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
  <done>DesignSystemPage exists, type-checks, mounts every primitive and the theme toggle, uses bundle utilities exclusively, no hex/palette/inline color.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Vitest + Testing Library smoke test for DesignSystemPage</name>
  <files>apps/web/src/features/design-system/DesignSystemPage.test.tsx</files>
  <read_first>
    - apps/web/src/features/design-system/DesignSystemPage.tsx (from Task 1)
    - apps/web/src/hooks/useTheme.ts (STORAGE_KEY value)
    - apps/web/vite.config.ts (confirm jsdom test env is configured)
    - Any existing component test under apps/web/src/features/**/*.test.tsx (pattern reference)
  </read_first>
  <behavior>
    - File `apps/web/src/features/design-system/DesignSystemPage.test.tsx` exists.
    - Test suite name: `describe('DesignSystemPage')`.
    - 5 test cases:
        a) "renders without crashing" — render the page, assert the data-testid="design-system-page" container is in the document.
        b) "renders all 6 status pills with Spanish labels" — assert each `status-pill-{status}` testid is present, and each contains its Spanish label text (Disponible / Reservada / Ocupada / Limpieza / Mantenimiento / Bloqueada).
        c) "clicking ThemeToggle flips data-theme on documentElement" — initial state: no data-theme attribute. Click the button with aria-label "Cambiar a modo oscuro". Assert document.documentElement has `data-theme="dark"`. Click again. Assert no data-theme attribute.
        d) "ThemeToggle persists choice to localStorage" — click toggle, assert `localStorage.getItem('hos-theme') === 'dark'`. Click again, assert `localStorage.getItem('hos-theme') === 'light'`.
        e) "renders all 7 Button variants" — for each of the 7 variant names (default, terracotta, destructive, outline, secondary, ghost, link), assert at least one button containing the variant name text exists in the document. (For "icon" size buttons the visible text is "+" — they are not asserted here.)
    - beforeEach: clear localStorage and remove data-theme from documentElement (test isolation).
    - Uses @testing-library/react render + @testing-library/user-event userEvent.setup() pattern.
  </behavior>
  <action>
    Create apps/web/src/features/design-system/DesignSystemPage.test.tsx with this structure:

      import { describe, it, expect, beforeEach } from 'vitest';
      import { render, screen, within } from '@testing-library/react';
      import userEvent from '@testing-library/user-event';
      import '@testing-library/jest-dom/vitest';
      import { DesignSystemPage } from './DesignSystemPage';

      describe('DesignSystemPage', () => {
        beforeEach(() => {
          window.localStorage.clear();
          document.documentElement.removeAttribute('data-theme');
        });

        it('renders without crashing', () => {
          render(<DesignSystemPage />);
          expect(screen.getByTestId('design-system-page')).toBeInTheDocument();
        });

        it('renders all 6 status pills with Spanish labels', () => {
          render(<DesignSystemPage />);
          const expected: Array<[string, string]> = [
            ['status-pill-available',   'Disponible'],
            ['status-pill-reserved',    'Reservada'],
            ['status-pill-occupied',    'Ocupada'],
            ['status-pill-cleaning',    'Limpieza'],
            ['status-pill-maintenance', 'Mantenimiento'],
            ['status-pill-blocked',     'Bloqueada'],
          ];
          for (const [testid, label] of expected) {
            const pill = screen.getByTestId(testid);
            expect(pill).toBeInTheDocument();
            expect(within(pill).getByText(label)).toBeInTheDocument();
          }
        });

        it('clicking ThemeToggle flips data-theme on documentElement', async () => {
          const user = userEvent.setup();
          render(<DesignSystemPage />);
          expect(document.documentElement.getAttribute('data-theme')).toBeNull();
          const toggle = screen.getByRole('button', { name: /modo oscuro/i });
          await user.click(toggle);
          expect(document.documentElement.getAttribute('data-theme')).toBe('dark');
          const toggleAgain = screen.getByRole('button', { name: /modo claro/i });
          await user.click(toggleAgain);
          expect(document.documentElement.getAttribute('data-theme')).toBeNull();
        });

        it('persists theme choice to localStorage', async () => {
          const user = userEvent.setup();
          render(<DesignSystemPage />);
          await user.click(screen.getByRole('button', { name: /modo oscuro/i }));
          expect(window.localStorage.getItem('hos-theme')).toBe('dark');
          await user.click(screen.getByRole('button', { name: /modo claro/i }));
          expect(window.localStorage.getItem('hos-theme')).toBe('light');
        });

        it('renders all 7 Button variants by visible label', () => {
          render(<DesignSystemPage />);
          const variants = ['default', 'terracotta', 'destructive', 'outline', 'secondary', 'ghost', 'link'];
          for (const v of variants) {
            // At least one button with the variant name as text content
            const matches = screen.getAllByText(v, { selector: 'button' });
            expect(matches.length).toBeGreaterThan(0);
          }
        });
      });

    Notes:
    - The test file relies on jsdom (Vitest default in the project — verified in apps/web/vite.config.ts).
    - The first useTheme render in jsdom may emit a "matchMedia is not a function" warning unless the page-level test setup polyfills it. Existing tests in the project may already polyfill matchMedia (check apps/web/src/test/setup.ts or similar). If not, prepend the test file with a single setup stub at module scope:
        if (typeof window !== 'undefined' && !window.matchMedia) {
          // eslint-disable-next-line @typescript-eslint/no-explicit-any
          (window as any).matchMedia = (query: string) => ({
            matches: false, media: query, addListener: () => {}, removeListener: () => {},
            addEventListener: () => {}, removeEventListener: () => {}, dispatchEvent: () => false,
            onchange: null,
          });
        }
    - Apply the polyfill ONLY if `pnpm vitest run` fails on "matchMedia is not a function" — first run without the polyfill and add if needed.
  </action>
  <verify>cd apps/web ; pnpm vitest run src/features/design-system/DesignSystemPage.test.tsx</verify>
  <acceptance_criteria>
    - `apps/web/src/features/design-system/DesignSystemPage.test.tsx` exists
    - `rg "describe\\('DesignSystemPage'" apps/web/src/features/design-system/DesignSystemPage.test.tsx` returns exactly one match
    - `rg "data-theme.*'dark'" apps/web/src/features/design-system/DesignSystemPage.test.tsx` returns at least one match
    - `rg "localStorage.getItem\\('hos-theme'\\)" apps/web/src/features/design-system/DesignSystemPage.test.tsx` returns at least one match
    - `cd apps/web && pnpm vitest run src/features/design-system/DesignSystemPage.test.tsx` exits 0 with 5 tests passing
  </acceptance_criteria>
  <done>Test file exists and passes; verifies theme toggle behavior, status-pill set, button variant set, localStorage persistence.</done>
</task>

<task type="auto">
  <name>Task 3: Mount /design-system route in router (dev-only)</name>
  <files>apps/web/src/router.tsx</files>
  <read_first>
    - apps/web/src/router.tsx (current file — top-level public routes section)
    - apps/web/src/features/design-system/DesignSystemPage.tsx (from Task 1)
    - Vite env types: import.meta.env.DEV is `true` in dev builds, `false` in production builds (Vite default)
  </read_first>
  <action>
    Edit apps/web/src/router.tsx:

    1. Add an import at the bottom of the existing "Lazy imports at module level" block (around line 188):
       `import { DesignSystemPage } from '@/features/design-system/DesignSystemPage';`

    2. Add a top-level route INSIDE the children array of the root AppWrapper element, alongside `/login`, `/concierge`, and `/booking` (so it is outside ProtectedRoute and visible without auth). Insert this route AFTER the `/booking/confirmation` route (line ~88) and BEFORE the `path: '/'` ProtectedRoute block (line ~91):

         // ─── Dev-only design system demo (not mounted in production) ─────────
         ...(import.meta.env.DEV
           ? [{
               path: '/design-system',
               element: <DesignSystemPage />,
             }]
           : []),

    The spread-into-array pattern works because `children` accepts a flat array of RouteObject. The conditional spread keeps the route out of production bundles entirely (Vite/Rollup tree-shake the static `import.meta.env.DEV === false` branch).

    Implementation note: if the spread-conditional pattern produces a TypeScript error (RouteObject array vs single object), use the alternative form:

         ...(import.meta.env.DEV
           ? ([{ path: '/design-system', element: <DesignSystemPage /> }] as const)
           : ([] as const)),

    Or even simpler — declare the array once before the createBrowserRouter call:
         const devRoutes = import.meta.env.DEV
           ? [{ path: '/design-system', element: <DesignSystemPage /> }]
           : [];

       and spread `...devRoutes` into the children.

    3. Verify the existing routes are unchanged: ProtectedRoute, StaffLayout, and all 14 staff routes remain identical.

    Do NOT mount the route inside ProtectedRoute. Do NOT remove or rename any existing route. Do NOT touch the AppWrapper, ProtectedRoute, or StaffLayout definitions.
  </action>
  <verify>rg "design-system" apps/web/src/router.tsx ; rg "import.meta.env.DEV" apps/web/src/router.tsx ; cd apps/web ; pnpm tsc --noEmit -p tsconfig.json ; pnpm vite build</verify>
  <acceptance_criteria>
    - `rg "import\\s+\\{\\s*DesignSystemPage\\s*\\}\\s+from\\s+['\"]@/features/design-system/DesignSystemPage['\"]" apps/web/src/router.tsx` returns exactly one match
    - `rg "path:\\s*['\"]?/?design-system['\"]?" apps/web/src/router.tsx` returns exactly one match
    - `rg "import.meta.env.DEV" apps/web/src/router.tsx` returns at least one match
    - The route appears OUTSIDE ProtectedRoute (verifiable by reading the file — the `/design-system` line number is less than the `element: <ProtectedRoute />` line number)
    - `cd apps/web && pnpm tsc --noEmit -p tsconfig.json` exits 0
    - `cd apps/web && pnpm vite build` exits 0
    - In dev (`pnpm dev`), navigating to http://localhost:<port>/design-system renders DesignSystemPage (manual verification — Vitest covers the component itself in Task 2)
  </acceptance_criteria>
  <done>/design-system route exists in dev builds, returns 404 (no match → falls through to /login redirect) in production, type-check and build pass.</done>
</task>

</tasks>

<verification>
After all 3 tasks:

1. Demo page test passes: `cd apps/web && pnpm vitest run src/features/design-system/DesignSystemPage.test.tsx` → exits 0 with 5 tests passing
2. Type-check passes: `cd apps/web && pnpm tsc --noEmit -p tsconfig.json` → exits 0
3. Production build passes: `cd apps/web && pnpm vite build` → exits 0
4. Phase 9 scope is hex-free including demo page:
   `rg "#[0-9a-fA-F]{3,6}" apps/web/src/components/ui apps/web/src/components/layout/StaffLayout.tsx apps/web/src/hooks apps/web/src/features/design-system` returns zero matches
5. All 5 Phase 9 requirements have a consuming page:
   - VIS-01 (tokens): every utility used by DesignSystemPage resolves (verified by build)
   - VIS-02 (fonts): demo page renders h1-h4 in Instrument Serif (verified by manual check on /design-system)
   - VIS-03 (dark mode): test asserts data-theme toggle works
   - VIS-04 (status colors): test asserts all 6 StatusPills render with Spanish labels
   - VIS-05 (primitives): demo page mounts Button + Card + Input + Badge (and tests cover Button + Badge variants by name)
</verification>

<success_criteria>
- MVP vertical slice satisfied: foundation works end-to-end on a real route (demo).
- /design-system reachable in development; production builds tree-shake it out.
- 5/5 Vitest tests pass.
- Phase 9 closeout: Phases 10 and 11 can safely consume Button, Card, Input, Badge, StatusPill, ThemeToggle, useTheme — proven by the demo page rendering all of them without runtime errors.
</success_criteria>

<output>
After completion, create `.planning/phases/09-design-system-foundation/09-04-SUMMARY.md` covering:
  - Files created (DesignSystemPage.tsx, DesignSystemPage.test.tsx) + 1 edit (router.tsx)
  - Decision: demo route is dev-only via `import.meta.env.DEV` spread-conditional — production bundles tree-shake the import (verify by inspecting `dist/assets/*.js` post-build for absence of "DesignSystemPage" identifier)
  - Decision: 5 test cases cover (render, status-pill set, data-theme toggle, localStorage persistence, button variant set) — sufficient smoke coverage for foundation
  - matchMedia polyfill: applied / not applied (record actual outcome)
  - Phase 9 closeout list — what Phase 10/11 may now safely import: Button, Card, Input, Badge, StatusPill, ThemeToggle, useTheme, type RoomStatus, STATUS_LABELS
  - Outstanding tech debt deferred to Phase 11: ProtectedRoute loading-state classes (router.tsx lines 29-31) still use `bg-bg-base text-text-muted` — must be renamed when /login lands in Phase 11 (or earlier as a hotfix if /login screen is rebuilt first)
  - Outstanding tech debt deferred to Phase 10/11: 33 feature-page files still hold v1.0 hex/tokens (cite Plan 09-01 SUMMARY)
</output>
