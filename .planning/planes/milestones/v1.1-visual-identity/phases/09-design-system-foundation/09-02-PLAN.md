---
phase: 09-design-system-foundation
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - apps/web/src/components/ui/button.tsx
  - apps/web/src/components/ui/card.tsx
  - apps/web/src/components/ui/input.tsx
autonomous: true
requirements: [VIS-05]
must_haves:
  truths:
    - "Button rendering its default variant shows a terracotta background and warm-white text — colors come from bundle tokens, not Tailwind palette"
    - "A Button with variant='terracotta' renders identically to default (primary terracotta button) — semantic alias for Phase 11 use"
    - "Card renders with bg-warm-white background and 1px warm-line border, no shadow (matches bundle .hos-card)"
    - "Card has 14px border-radius (rounded-xl mapped to --radius-xl: 14px from Plan 09-01)"
    - "Input renders with bg-warm-paper background, warm-line-strong border, ink-3 placeholder, terracotta focus ring"
    - "Zero hex values, zero Tailwind palette colors (text-blue-500 etc.), and zero inline style color in any of the 3 primitive files"
  artifacts:
    - path: "apps/web/src/components/ui/button.tsx"
      provides: "Button primitive refactored to bundle tokens with new `terracotta` variant"
      contains: "bg-terracotta text-warm-white"
    - path: "apps/web/src/components/ui/card.tsx"
      provides: "Card primitive refactored to bundle tokens — warm-white bg, warm-line border, rounded-xl (14px), no shadow"
      contains: "bg-warm-white"
    - path: "apps/web/src/components/ui/input.tsx"
      provides: "Input primitive refactored to bundle tokens — warm-paper bg, warm-line-strong border, terracotta focus ring"
      contains: "focus-visible:ring-terracotta"
  key_links:
    - from: "apps/web/src/components/ui/button.tsx"
      to: "@theme inline color-terracotta in globals.css"
      via: "Tailwind utility bg-terracotta"
      pattern: "bg-terracotta"
    - from: "apps/web/src/components/ui/input.tsx"
      to: "@theme inline color-terracotta in globals.css"
      via: "Tailwind utility focus-visible:ring-terracotta"
      pattern: "focus-visible:ring-terracotta"
threat_model:
  scope: "Frontend primitives only — no security boundaries. Class-string changes to React components. No backend, no auth, no input validation surface, no new dependencies."
  asvs_gates: "N/A — no auth, crypto, session, or input validation involved."
---

<objective>
Refactor the three existing shadcn primitives (Button, Card, Input) so every class name resolves to a bundle token utility instead of a v1.0 token utility or a Tailwind palette color. Add an explicit `terracotta` variant to Button (semantic alias for Phase 11 use). All three files become hex-free, palette-free, and inline-style-free.

Purpose: Phases 10 and 11 will consume these primitives heavily. If the primitives are not bundle-aligned, every consuming screen inherits broken styling. This plan also unblocks Plan 09-03 (Badge primitive uses the same token vocabulary).

Output: Three refactored primitive files. No new files, no API breaking changes to the public component props.
</objective>

<execution_context>
@C:\Users\Admin\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Admin\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/09-design-system-foundation/09-CONTEXT.md
@.planning/phases/09-design-system-foundation/09-RESEARCH.md
@.planning/phases/09-design-system-foundation/09-01-PLAN.md
@.design-fetch/hotelos-ai/project/tokens.jsx
@apps/web/src/components/ui/button.tsx
@apps/web/src/components/ui/card.tsx
@apps/web/src/components/ui/input.tsx
@apps/web/src/styles/globals.css

<interfaces>
<!-- Tailwind utilities now available after Plan 09-01 — the executor MUST use these names
     and MUST NOT introduce any others. -->

Bundle utility classes (declared in globals.css @theme inline after Plan 09-01):
- Backgrounds: bg-warm-white, bg-warm-paper, bg-warm-cream, bg-warm-tan, bg-terracotta, bg-terracotta-deep, bg-terracotta-soft, bg-terracotta-tint, bg-mustard, bg-mustard-soft, bg-mustard-tint, bg-olive, bg-olive-tint, bg-clay, bg-clay-tint
- Text colors: text-ink-1, text-ink-2, text-ink-3, text-ink-4, text-terracotta, text-mustard, text-olive, text-clay, text-warm-white, text-warm-paper
- Borders: border-warm-line, border-warm-line-strong (NOTE: warm-line is rgba — DO NOT use opacity modifier `/50` etc — see Plan 09-01 Pitfall 5)
- Status: bg-status-{available|reserved|occupied|cleaning|maintenance|blocked}-bg and text-status-{...} pairs
- Rings: ring-terracotta
- Radii: rounded-md (8px), rounded-lg (12px), rounded-xl (14px = bundle .hos-card)

Bundle button styles (tokens.jsx lines 235-252):
.hos-btn-pri  : background terracotta, color #faf7f2 (warm-white), inset shadow inset 0 -2px 0 rgba(0,0,0,0.10), hover background terracotta-deep
.hos-btn-sec  : background warm-white, color ink-1, border 1px warm-line-strong, hover background warm-cream
.hos-btn-ghost: background transparent, color ink-2, hover background warm-cream + color ink-1

Bundle card styles (tokens.jsx lines 219-225):
.hos-card     : background warm-white, border 1px warm-line, border-radius 14px, padding 18px
                (NO box-shadow — bundle uses borders, not shadows)

Bundle search/input style (tokens.jsx lines 186-198, used as input reference):
                background warm-paper, border 1px warm-line, color ink-3 (placeholder-ish), padding 7px 12px, border-radius 8px
                For our focused-input pattern, we apply ring-terracotta on focus and border-warm-line-strong (slightly stronger than bundle search so the field reads as interactive)

Existing primitive file signatures (must be preserved — these are PUBLIC APIs):
- Button: forwardRef<HTMLButtonElement, ButtonProps>, exports { Button, buttonVariants }, variants object with default/destructive/outline/secondary/ghost/link + sizes default/sm/lg/icon
- Card: forwardRef<HTMLDivElement, ...>, exports { Card, CardHeader, CardFooter, CardTitle, CardDescription, CardContent }
- Input: forwardRef<HTMLInputElement, InputProps>, exports { Input }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor Button to bundle tokens + add terracotta variant</name>
  <files>apps/web/src/components/ui/button.tsx</files>
  <read_first>
    - apps/web/src/components/ui/button.tsx (current file — full diff target)
    - .design-fetch/hotelos-ai/project/tokens.jsx (lines 235-252 — .hos-btn-pri, .hos-btn-sec, .hos-btn-ghost reference)
    - .planning/phases/09-design-system-foundation/09-RESEARCH.md (Pitfall 4 — exact rename map; Example 3 — Button diff)
  </read_first>
  <behavior>
    - Existing exports preserved: `Button` (forwardRef component), `buttonVariants` (cva output), `ButtonProps` interface.
    - Existing `size` variants unchanged (default/sm/lg/icon — sizes use w/h/px utilities, no color renames needed).
    - Existing `variant` keys preserved: `default`, `destructive`, `outline`, `secondary`, `ghost`, `link`.
    - NEW: add `terracotta` variant key — class string IDENTICAL to `default` (semantic alias for Phase 11 readability).
    - Rendered `<Button variant="default">` has `bg-terracotta` and `text-warm-white` Tailwind classes (verify in DOM snapshot or `render(<Button />).className.includes('bg-terracotta')`).
    - Rendered `<Button variant="outline">` has `border-warm-line-strong` and `bg-warm-white`.
    - No hex value, no Tailwind palette color (e.g. `bg-blue-500`), no inline `style={{ color: ... }}` remains anywhere in the file.
  </behavior>
  <action>
    Rewrite the `variants.variant` block in `buttonVariants` using these EXACT class strings (replacing the current v1.0 class strings line by line):

    - default:     `'bg-terracotta text-warm-white shadow hover:bg-terracotta-deep'`
    - terracotta:  `'bg-terracotta text-warm-white shadow hover:bg-terracotta-deep'`   (NEW — same as default, semantic alias)
    - destructive: `'bg-destructive text-white shadow-sm hover:bg-destructive/90'`  (keep — destructive uses shadcn bridge `--destructive: #dc2626`, which is the ONE allowed hex on the bridge layer)
    - outline:     `'border border-warm-line-strong bg-warm-white shadow-sm hover:bg-terracotta-soft hover:text-ink-1'`
    - secondary:   `'bg-warm-white text-ink-1 border border-warm-line-strong shadow-sm hover:bg-terracotta-soft'`
    - ghost:       `'hover:bg-terracotta-soft hover:text-ink-1'`
    - link:        `'text-terracotta underline-offset-4 hover:underline'`

    Do not touch: the base classes string on cva line 7 (focus-ring-ring uses the shadcn bridge `--ring: var(--terracotta)` from Plan 09-01, so it resolves correctly). Do not touch the size variants. Do not touch the `forwardRef` signature or exports.

    Optional (Claude's Discretion): add a JSDoc comment above `buttonVariants` summarizing the 7 variants. Do NOT add new size variants or new behavior.
  </action>
  <verify>
    <automated>rg "bg-brand-primary|text-text-inverse|hover:bg-brand-primary-hover|border-border-strong|text-text-primary|bg-surface|hover:bg-brand-primary-soft|text-text-brand" apps/web/src/components/ui/button.tsx; rg "bg-terracotta\s+text-warm-white" apps/web/src/components/ui/button.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `rg "brand-primary|text-text-inverse|border-border-strong|text-text-primary|bg-surface|text-text-brand" apps/web/src/components/ui/button.tsx` returns zero matches
    - `rg "terracotta:\s*$|terracotta:\s*'" apps/web/src/components/ui/button.tsx` returns at least one match (the new variant key)
    - `rg "bg-terracotta" apps/web/src/components/ui/button.tsx` returns at least two matches (default + terracotta variant)
    - `rg "#[0-9a-fA-F]{3,6}" apps/web/src/components/ui/button.tsx` returns zero matches
    - `rg "text-(blue|red|green|yellow|purple|pink|gray|slate|zinc|neutral|stone|amber|orange|lime|emerald|teal|cyan|sky|violet|fuchsia|rose|indigo)-[0-9]" apps/web/src/components/ui/button.tsx` returns zero matches
    - `rg "style=\{\{.*color" apps/web/src/components/ui/button.tsx` returns zero matches
    - `cd apps/web && pnpm tsc --noEmit -p tsconfig.json` exits 0 (type-check passes)
  </acceptance_criteria>
  <done>Button uses bundle utilities exclusively; terracotta variant added; public API unchanged; type-check passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor Card to bundle tokens (no shadow, rounded-xl, warm-white/warm-line)</name>
  <files>apps/web/src/components/ui/card.tsx</files>
  <read_first>
    - apps/web/src/components/ui/card.tsx (current file)
    - .design-fetch/hotelos-ai/project/tokens.jsx (lines 219-225 — .hos-card reference)
    - .planning/phases/09-design-system-foundation/09-RESEARCH.md (Example 4 — Card diff)
  </read_first>
  <behavior>
    - Existing exports preserved: `Card`, `CardHeader`, `CardFooter`, `CardTitle`, `CardDescription`, `CardContent`.
    - Card root: `rounded-xl border border-warm-line bg-warm-white text-ink-1` (no `shadow-sm` — bundle uses borders not shadows; `rounded-xl` = 14px from `--radius-xl` declared in Plan 09-01 globals.css).
    - CardTitle: removes `text-text-primary`, becomes `text-ink-1`.
    - CardDescription: removes `text-text-muted`, becomes `text-ink-3`.
    - No hex, no Tailwind palette, no inline color.
  </behavior>
  <action>
    Edit `apps/web/src/components/ui/card.tsx`:

    Line 9 (Card className): replace `'rounded-lg border border-border-subtle bg-surface text-text-primary shadow-sm'` with `'rounded-xl border border-warm-line bg-warm-white text-ink-1'` — drop `shadow-sm`, change radius to xl, swap tokens.

    Line 29 (CardTitle className): replace `'font-semibold leading-none tracking-tight text-text-primary'` with `'font-semibold leading-none tracking-tight text-ink-1'`.

    Line 41 (CardDescription className): replace `'text-sm text-text-muted'` with `'text-sm text-ink-3'`.

    Do NOT touch: CardHeader (only layout classes `flex flex-col space-y-1.5 p-6` — no color), CardContent (only `p-6 pt-0`), CardFooter (only `flex items-center p-6 pt-0`). Do not change forwardRef signatures or display names.
  </action>
  <verify>
    <automated>rg "border-border-subtle|bg-surface|text-text-primary|text-text-muted|shadow-sm" apps/web/src/components/ui/card.tsx; rg "rounded-xl border border-warm-line bg-warm-white text-ink-1" apps/web/src/components/ui/card.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `rg "border-border-subtle|bg-surface|text-text-primary|text-text-muted" apps/web/src/components/ui/card.tsx` returns zero matches
    - `rg "shadow-sm" apps/web/src/components/ui/card.tsx` returns zero matches (Card explicitly drops shadow per bundle)
    - `rg "rounded-xl border border-warm-line bg-warm-white text-ink-1" apps/web/src/components/ui/card.tsx` returns exactly one match
    - `rg "text-ink-3" apps/web/src/components/ui/card.tsx` returns exactly one match (CardDescription)
    - `rg "#[0-9a-fA-F]{3,6}" apps/web/src/components/ui/card.tsx` returns zero matches
    - `cd apps/web && pnpm tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
  <done>Card primitive uses bundle utilities; rounded-xl + no shadow matches bundle .hos-card; type-check passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Refactor Input to bundle tokens (warm-paper bg, terracotta focus ring)</name>
  <files>apps/web/src/components/ui/input.tsx</files>
  <read_first>
    - apps/web/src/components/ui/input.tsx (current file)
    - .design-fetch/hotelos-ai/project/tokens.jsx (lines 186-198 — .hos-search input reference)
    - .planning/phases/09-design-system-foundation/09-RESEARCH.md (Example 5 — Input diff)
  </read_first>
  <behavior>
    - Existing exports preserved: `Input`, `InputProps`.
    - Replace v1.0 tokens in className: `border-border-strong` → `border-warm-line-strong`, `bg-surface-elevated` → `bg-warm-paper`, `text-text-primary` → `text-ink-1`, `placeholder:text-text-muted` → `placeholder:text-ink-3`, `focus-visible:ring-brand-primary` → `focus-visible:ring-terracotta`.
    - All other classes (`flex h-9 w-full rounded-md px-3 py-1 text-sm shadow-sm transition-colors file:border-0 file:bg-transparent file:text-sm file:font-medium focus-visible:outline-none focus-visible:ring-1 disabled:cursor-not-allowed disabled:opacity-50`) preserved as-is.
  </behavior>
  <action>
    Edit `apps/web/src/components/ui/input.tsx` line 12 (the single className string). Replace exactly:

    Before:
    `'flex h-9 w-full rounded-md border border-border-strong bg-surface-elevated px-3 py-1 text-sm text-text-primary shadow-sm transition-colors file:border-0 file:bg-transparent file:text-sm file:font-medium placeholder:text-text-muted focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-brand-primary disabled:cursor-not-allowed disabled:opacity-50'`

    After:
    `'flex h-9 w-full rounded-md border border-warm-line-strong bg-warm-paper px-3 py-1 text-sm text-ink-1 shadow-sm transition-colors file:border-0 file:bg-transparent file:text-sm file:font-medium placeholder:text-ink-3 focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-terracotta disabled:cursor-not-allowed disabled:opacity-50'`

    Do not touch: imports, forwardRef signature, InputProps interface, display name, exports.
  </action>
  <verify>
    <automated>rg "border-border-strong|bg-surface-elevated|text-text-primary|placeholder:text-text-muted|focus-visible:ring-brand-primary" apps/web/src/components/ui/input.tsx; rg "focus-visible:ring-terracotta" apps/web/src/components/ui/input.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `rg "border-border-strong|bg-surface-elevated|text-text-primary|placeholder:text-text-muted|focus-visible:ring-brand-primary" apps/web/src/components/ui/input.tsx` returns zero matches
    - `rg "border-warm-line-strong" apps/web/src/components/ui/input.tsx` returns exactly one match
    - `rg "bg-warm-paper" apps/web/src/components/ui/input.tsx` returns exactly one match
    - `rg "focus-visible:ring-terracotta" apps/web/src/components/ui/input.tsx` returns exactly one match
    - `rg "placeholder:text-ink-3" apps/web/src/components/ui/input.tsx` returns exactly one match
    - `rg "#[0-9a-fA-F]{3,6}" apps/web/src/components/ui/input.tsx` returns zero matches
    - `cd apps/web && pnpm tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
  <done>Input primitive uses bundle utilities exclusively; focus ring is terracotta; type-check passes.</done>
</task>

</tasks>

<verification>
After all 3 tasks:

1. Zero hex in any primitive file:
   `rg "#[0-9a-fA-F]{3,6}" apps/web/src/components/ui/button.tsx apps/web/src/components/ui/card.tsx apps/web/src/components/ui/input.tsx` → zero matches

2. Zero Tailwind palette colors in any primitive file:
   `rg "text-(blue|red|green|yellow|purple|pink|gray|slate|zinc|neutral|stone|amber|orange|lime|emerald|teal|cyan|sky|violet|fuchsia|rose|indigo)-[0-9]|bg-(blue|red|green|yellow|purple|pink|gray|slate|zinc|neutral|stone|amber|orange|lime|emerald|teal|cyan|sky|violet|fuchsia|rose|indigo)-[0-9]" apps/web/src/components/ui/button.tsx apps/web/src/components/ui/card.tsx apps/web/src/components/ui/input.tsx` → zero matches

3. Zero inline color styles:
   `rg "style=\{\{[^}]*color" apps/web/src/components/ui/button.tsx apps/web/src/components/ui/card.tsx apps/web/src/components/ui/input.tsx` → zero matches

4. Zero remaining v1.0 token names in primitives:
   `rg "brand-primary|bg-base|bg-surface|text-text-|border-border-" apps/web/src/components/ui/button.tsx apps/web/src/components/ui/card.tsx apps/web/src/components/ui/input.tsx` → zero matches

5. Type-check passes: `cd apps/web && pnpm tsc --noEmit -p tsconfig.json` → exits 0

6. Build passes: `cd apps/web && pnpm vite build` → exits 0
</verification>

<success_criteria>
- VIS-05: Button, Card, Input refactored to read colors exclusively from bundle CSS variable utilities; zero hardcoded hex, zero Tailwind palette colors, zero inline `style={{ color }}`. Badge is the remaining VIS-05 deliverable and is owned by Plan 09-03.
</success_criteria>

<output>
After completion, create `.planning/phases/09-design-system-foundation/09-02-SUMMARY.md` covering:
  - Table of variant class rename diffs (default/outline/secondary/ghost/link before → after)
  - Decision: new `terracotta` variant is identical to `default` (semantic alias only)
  - Decision: Card drops `shadow-sm` and uses `rounded-xl` (14px) to match bundle .hos-card exactly
  - Verification command outputs (zero matches confirmed)
  - Outstanding: Badge primitive (does not exist yet) — owned by Plan 09-03
</output>
