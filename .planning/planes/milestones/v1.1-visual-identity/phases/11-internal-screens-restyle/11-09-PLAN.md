---
phase: 11-internal-screens-restyle
plan: 09
type: execute
wave: 3
depends_on: [11-01, 11-02, 11-03, 11-04, 11-05, 11-06, 11-07, 11-08]
files_modified:
  - .planning/phases/11-internal-screens-restyle/11-MANUAL-QA-CHECKLIST.md
  - .planning/phases/11-internal-screens-restyle/11-MILESTONE-CLOSEOUT.md
autonomous: false
requirements: [INT-01, INT-02, INT-03, INT-04, INT-05, INT-06, INT-07, INT-08]
must_haves:
  truths:
    - "Full apps/web test suite exits 0 after all 8 restyle plans complete"
    - "Zero hex literals across all 11 Phase 11 file paths"
    - "Zero Tailwind palette classes across all 11 Phase 11 file paths"
    - "Zero .hos-* global class references"
    - "Manual QA checklist exists with verification steps for each of the 8 screens"
    - "Milestone v1.1 closeout document exists, listing shipped surface and remaining gaps"
  artifacts:
    - path: ".planning/phases/11-internal-screens-restyle/11-MANUAL-QA-CHECKLIST.md"
      provides: "User-facing manual QA steps for all 8 restyled screens"
      contains: "Login split-panel"
    - path: ".planning/phases/11-internal-screens-restyle/11-MILESTONE-CLOSEOUT.md"
      provides: "v1.1 milestone summary — what shipped, what deferred"
      contains: "v1.1"
  key_links:
    - from: ".planning/phases/11-internal-screens-restyle/11-MANUAL-QA-CHECKLIST.md"
      to: ".planning/phases/11-internal-screens-restyle/11-CONTEXT.md"
      via: "per-screen contract references"
      pattern: "INT-0[1-8]"
---

<objective>
Final verification gate for Phase 11 AND the v1.1 milestone. Three responsibilities:
1. **Automated regression**: run the full `apps/web` test suite, run the 4 zero-hex / zero-palette / zero-.hos-* checks across all 11 Phase 11 file paths.
2. **Manual QA checklist**: produce `11-MANUAL-QA-CHECKLIST.md` for the user to validate each of the 8 screens visually + behaviorally.
3. **Milestone closeout**: document what v1.1 shipped, what gaps remain, what was deferred.

This plan has a checkpoint (manual QA is human-driven) — `autonomous: false`.

Output:
- `11-MANUAL-QA-CHECKLIST.md` (new)
- `11-MILESTONE-CLOSEOUT.md` (new)
- Automated regression results documented in SUMMARY
</objective>

<execution_context>
@C:/Users/oscarpeinado/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/oscarpeinado/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-internal-screens-restyle/11-CONTEXT.md
@.planning/phases/11-internal-screens-restyle/11-01-PLAN.md
@.planning/phases/11-internal-screens-restyle/11-02-PLAN.md
@.planning/phases/11-internal-screens-restyle/11-03-PLAN.md
@.planning/phases/11-internal-screens-restyle/11-04-PLAN.md
@.planning/phases/11-internal-screens-restyle/11-05-PLAN.md
@.planning/phases/11-internal-screens-restyle/11-06-PLAN.md
@.planning/phases/11-internal-screens-restyle/11-07-PLAN.md
@.planning/phases/11-internal-screens-restyle/11-08-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full apps/web regression suite + cross-cutting verifications</name>
  <files>(no files modified — verification only; records results in SUMMARY)</files>
  <read_first>
    - All 8 prior SUMMARY files (11-01-SUMMARY.md through 11-08-SUMMARY.md if they exist)
    - .planning/phases/11-internal-screens-restyle/11-CONTEXT.md (lines 149-156 — verification commands)
  </read_first>
  <action>
    Run all four verification commands in sequence. Capture pass/fail status of each. Record in the SUMMARY.

    **Command 1 — Full test suite:**
    ```bash
    cd apps/web && pnpm vitest run
    ```
    Expected: 100% pass. Includes Phase 9 design-system smoke (5 tests), reporting tests (11), housekeeping tests (5), Phase 10 portal tests, plus any other suite. Total count should be ≥ 21 tests, all passing.

    **Command 2 — Zero hex literals across Phase 11 file paths:**
    ```bash
    rg "#[0-9a-fA-F]{3,6}" \
       apps/web/src/features/auth/LoginPage.tsx \
       apps/web/src/features/reporting/DashboardPage.tsx \
       apps/web/src/features/reporting/KpiCard.tsx \
       apps/web/src/features/reporting/OccupancyBarChart.tsx \
       apps/web/src/features/reporting/RoomStatusDonut.tsx \
       apps/web/src/features/reservations/RoomRackCalendar.tsx \
       apps/web/src/features/reservations/components/RoomRackTable.tsx \
       apps/web/src/features/inventory/RoomsPage.tsx \
       apps/web/src/features/inventory/RoomDrawer.tsx \
       apps/web/src/features/reservations/wizard/ReservationWizard.tsx \
       apps/web/src/features/reservations/wizard/Step1Dates.tsx \
       apps/web/src/features/reservations/wizard/Step2Room.tsx \
       apps/web/src/features/reservations/wizard/Step3Guest.tsx \
       apps/web/src/features/reservations/wizard/Step4Confirm.tsx \
       apps/web/src/features/reservations/wizard/StepIndicator.tsx \
       apps/web/src/features/housekeeping/HousekeepingPage.tsx \
       apps/web/src/features/ai-assistant/ChatPanel.tsx \
       apps/web/src/features/ai-assistant/ContextPanel.tsx \
       apps/web/src/features/ai-assistant/ChatMessage.tsx \
       apps/web/src/components/layout/StaffLayout.tsx \
       apps/web/src/components/layout/Sidebar.tsx \
       apps/web/src/lib/status-colors.ts \
       apps/web/src/router.tsx
    ```
    Expected: 0 matches.

    **Command 3 — Zero Tailwind palette classes:**
    ```bash
    rg "text-(gray|blue|red|green|yellow|purple|pink|indigo|slate|zinc|neutral|stone|amber|orange|lime|emerald|teal|cyan|sky|violet|fuchsia|rose)-[0-9]|bg-(gray|blue|red|green|yellow|purple|pink|indigo|slate|zinc|neutral|stone|amber|orange|lime|emerald|teal|cyan|sky|violet|fuchsia|rose)-[0-9]|border-(gray|blue|red|green|yellow|purple|pink|indigo|slate|zinc|neutral|stone|amber|orange|lime|emerald|teal|cyan|sky|violet|fuchsia|rose)-[0-9]" \
       apps/web/src/features/auth apps/web/src/features/reporting apps/web/src/features/reservations apps/web/src/features/inventory apps/web/src/features/housekeeping apps/web/src/features/ai-assistant apps/web/src/components/layout
    ```
    Expected: 0 matches (in non-test files). If matches exist in test files, those are acceptable (tests can use arbitrary colors for mocks).

    **Command 4 — Zero `.hos-*` global class references:**
    ```bash
    rg "\\.hos-(card|pill|btn|avatar|logo-mark|app|sidebar|topbar|nav|main|icon-btn)" apps/web/src --glob "*.{ts,tsx,css}"
    ```
    Expected: 0 matches. The `hos` root class itself (on `<html>` / StaffLayout / LoginPage) is acceptable — only the `.hos-*` derivative classes are prohibited (we use Tailwind utility compositions).

    **Command 5 — v1.0 token references swept:**
    ```bash
    rg "bg-(bg-base|surface|surface-elevated|surface-hover|surface-strong|brand-primary|brand-primary-soft|terracota/)|border-border-(subtle|strong)|text-(text-primary|text-secondary|text-muted|text-brand)|text-status-in-progress|ring-brand-primary|focus:ring-brand-primary" \
       apps/web/src/features apps/web/src/components/layout apps/web/src/router.tsx
    ```
    Expected: 0 matches.

    **Command 6 — TypeScript clean:**
    ```bash
    cd apps/web && pnpm tsc --noEmit -p tsconfig.json
    ```
    Expected: exit 0.

    Record results in `11-09-SUMMARY.md` as a table with status per check.
  </action>
  <verify>
    <automated>cd apps/web; pnpm vitest run &amp;&amp; pnpm tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - All 6 verification commands pass (recorded in SUMMARY).
    - `pnpm --filter web vitest run` exits 0.
    - `pnpm --filter web tsc --noEmit` exits 0.
    - Zero hex literals across all 11 Phase 11 file paths.
    - Zero Tailwind palette classes in non-test feature files.
    - Zero `.hos-*` global class references.
    - Zero v1.0 token references in feature/layout files.
  </acceptance_criteria>
  <done>Automated regression gate passed. All Phase 11 acceptance criteria met at the code level.</done>
</task>

<task type="auto">
  <name>Task 2: Write 11-MANUAL-QA-CHECKLIST.md</name>
  <files>.planning/phases/11-internal-screens-restyle/11-MANUAL-QA-CHECKLIST.md</files>
  <read_first>
    - .planning/phases/11-internal-screens-restyle/11-CONTEXT.md (per-screen contract — lines 53-138)
    - .planning/ROADMAP.md (Phase 11 success criteria — lines 253-263)
    - .planning/phases/10-public-portal/ (any existing MANUAL-QA-CHECKLIST.md as a format precedent)
  </read_first>
  <action>
    Create `.planning/phases/11-internal-screens-restyle/11-MANUAL-QA-CHECKLIST.md` with the following structure:

    ```markdown
    # Phase 11 — Manual QA Checklist

    **Created:** YYYY-MM-DD
    **Scope:** 8 staff-facing internal screens restyled to Claude Design bundle
    **Prerequisite:** `pnpm --filter web dev` running on http://localhost:5173

    ---

    ## How to use

    For each screen below: open the URL, perform the listed actions, tick each acceptance criterion. If any item fails, note it and create a gap-closure ticket (run `/gsd-plan-phase 11 --gaps` after this checklist).

    ---

    ## INT-08 — Sidebar + StaffLayout (verify first — affects every other screen)

    URL: any authenticated route (e.g., /dashboard)

    - [ ] Sidebar renders 240px wide with warm-white background and right border in warm-line
    - [ ] Hovering a non-active nav item changes its background to warm-cream
    - [ ] Clicking a nav item activates it: terracotta-tint background + terracotta-deep text + 2px terracotta left accent bar
    - [ ] Lucide icons render in ink-3 (inactive) and terracotta-deep (active)
    - [ ] Section labels (PRINCIPAL, OPERACIÓN, ADMINISTRACIÓN) render in uppercase tracking-widest ink-4
    - [ ] Click the collapse toggle (chevron icon) — sidebar shrinks to 64px in ~200ms
    - [ ] Reload page — collapse state persists (localStorage)
    - [ ] Click toggle again — sidebar expands to 240px in ~200ms
    - [ ] Topbar renders empty at 56px tall, warm-white bg, warm-line border-bottom
    - [ ] ThemeToggle visible in Sidebar footer — clicking it inverts the palette (light ↔ dark)
    - [ ] After theme toggle: reload page — choice persists

    ---

    ## INT-01 — LoginPage (/login)

    URL: /login (log out if currently authenticated)

    - [ ] Desktop ≥1024px: split-panel renders — left ink-1 with radial blobs (terracotta + mustard tint), right warm-white with form
    - [ ] Headline "Hospitalidad, operada con inteligencia" renders in Instrument Serif with italic "operada con inteligencia"
    - [ ] Three-stat strip visible: 42 habitaciones · 78% ocupación · 12 check-ins hoy (numbers in font-mono mustard, labels in ink-4)
    - [ ] Mobile <1024px: only right panel renders (left hidden)
    - [ ] Email input shows font-mono placeholder "admin@hotelsumapaz.co"
    - [ ] Submit button "Entrar" is terracotta-filled with warm-white text
    - [ ] Below button: "Ir al sitio del hotel" link in ink-3, hover underlines + turns terracotta
    - [ ] Submit with invalid credentials → terracotta-soft error banner

    ---

    ## INT-02 — DashboardPage (/dashboard)

    URL: /dashboard (authenticated)

    - [ ] Page h1 in Instrument Serif italic at text-3xl (greeting or title)
    - [ ] KPI cards render with warm-paper background, warm-line border, rounded-xl
    - [ ] Each KPI label in font-mono uppercase tracking-widest text-ink-3 at 11px
    - [ ] Each KPI value in font-mono text-3xl text-ink-1
    - [ ] Delta indicators: positive in olive, negative in terracotta
    - [ ] Occupancy bar chart: today's bar in terracotta, other days in mustard
    - [ ] Room status donut: 6 slice colors match status tokens (occupied=terracotta, cleaning=mustard, etc.)
    - [ ] Chart axes and grid use warm-line-strong / ink-3
    - [ ] No black SVG fills visible (sign of broken Recharts color injection)

    ---

    ## INT-03 — Calendar (/reservations calendar)

    URL: /reservations

    - [ ] Date header row: warm-cream bg, font-mono day labels
    - [ ] Today's column highlighted with terracotta-tint bg + terracotta-deep text
    - [ ] Room row headers: font-mono ink-1, room numbers prominent
    - [ ] Reservation bars color-coded by status:
      - [ ] CONFIRMED → reserved-blue
      - [ ] CHECKED_IN → terracotta (occupied)
      - [ ] CHECKED_OUT → maintenance grey-brown
      - [ ] CANCELLED → faded ink-4 with reduced opacity
    - [ ] Hover over a bar: terracotta outline appears
    - [ ] Drag-to-create still works (preserved behavior)
    - [ ] Click an existing bar opens the reservation detail view

    ---

    ## INT-04 — RoomsPage (/rooms)

    URL: /rooms

    - [ ] Rooms render as card grid (NOT a table)
    - [ ] 4 columns at xl viewport, responsive to 1/2/3 at smaller sizes
    - [ ] Each card: photo placeholder (warm-cream if no photo), room number in font-mono large, type label below, StatusPill at top-right
    - [ ] Cards hover with warm-line-strong border + subtle shadow
    - [ ] "Nueva habitación" button is terracotta variant
    - [ ] Click a card → RoomDrawer slides in from right with warm-cream background
    - [ ] RoomDrawer tabs: active tab has 2px terracotta bottom-border + terracotta-deep text
    - [ ] Amenity chips render with warm-paper bg + warm-line border + rounded-full
    - [ ] Close (X) button top-right of drawer

    ---

    ## INT-05 — ReservationWizard

    URL: /reservations/new (or wherever the wizard mounts)

    - [ ] StepIndicator row at top: 4 circles + labels
    - [ ] Active step: terracotta bg + ring-4 terracotta-tint + warm-white number
    - [ ] Completed step: mustard bg + warm-white checkmark icon
    - [ ] Pending step: warm-tan bg + ink-3 number
    - [ ] Connector lines: mustard if completed, warm-line otherwise
    - [ ] Step labels in Geist 13px uppercase tracking-wide
    - [ ] Step content panel: warm-white bg, warm-line border, rounded-xl, p-6
    - [ ] Navigate forward: previous step turns mustard with check
    - [ ] Back button uses outline variant; Confirmar (step 4) uses terracotta variant
    - [ ] All form validations still trigger appropriately

    ---

    ## INT-06 — HousekeepingPage (/housekeeping)

    URL: /housekeeping

    - [ ] 4-column kanban grid (1 col on mobile, 2 on md, 4 on lg)
    - [ ] Column headers: warm-cream bg + ink-1 title + font-mono count badge in warm-paper
    - [ ] Column accent borders (3px top): DIRTY=terracotta, IN_PROGRESS=mustard, INSPECTION=reserved-blue, CLEAN=olive
    - [ ] Task cards: warm-white bg, warm-line border, hover with shadow-sm
    - [ ] Priority badges:
      - [ ] Alta → terracotta bg, warm-white text
      - [ ] Media → mustard bg, warm-white text
      - [ ] Baja → olive bg, warm-white text
    - [ ] Assignee avatar: 28px circle, terracotta-tint bg, terracotta-deep font-mono initials
    - [ ] Unassigned tasks show dashed border + UserPlus icon
    - [ ] Time-elapsed labels in font-mono ink-3
    - [ ] Click a room card opens RoomStatusModal
    - [ ] Click "Asignar" opens TaskAssignmentDrawer

    ---

    ## INT-07 — ChatPanel (AI Assistant)

    URL: any authenticated screen, click the floating AI button

    - [ ] Floating button: terracotta circle bottom-right with Sparkles icon
    - [ ] Click → panel slides in from right
    - [ ] Desktop ≥1024px: 2-column layout (chat 60%, context 40%)
    - [ ] Mobile <1024px: single column, context panel hidden
    - [ ] Header: "Asistente IA" in Instrument Serif italic; close button top-right
    - [ ] Empty state: ink-3 prompt + suggestion chips with warm-line border
    - [ ] Type a question → user bubble appears right-aligned with warm-paper bg
    - [ ] Assistant streams response → assistant bubble left-aligned with warm-white bg
    - [ ] Streaming indicator: three pulsing ink-4 dots
    - [ ] Context panel section headings (FUENTES CONSULTADAS, ACCIONES SUGERIDAS) in Instrument Serif italic
    - [ ] Tool result cards: warm-white bg, warm-line border, rounded-lg
    - [ ] Send button: terracotta variant with Send icon

    ---

    ## Cross-Cutting

    - [ ] Toggle dark mode in Sidebar footer — entire app inverts to dark palette
    - [ ] All status pills render with consistent fg/bg pairs across Rooms, Calendar, Housekeeping, Dashboard
    - [ ] All numeric displays (room numbers, KPI values, dates, prices, time-elapsed) use Geist Mono (font-mono)
    - [ ] All h1/h2/h3 headings use Instrument Serif (font-display)
    - [ ] No Tailwind palette colors visible (no blue-500, red-100, etc.)
    - [ ] No hardcoded hex values visible (no #ffffff backgrounds outside system surfaces)

    ---

    ## Sign-off

    QA performed by: ___________________
    Date: ___________________
    Result: [ ] All pass [ ] Gaps identified (list below)

    ### Gaps identified
    (Empty if all passed. Each gap should be runnable via `/gsd-plan-phase 11 --gaps`.)
    ```

    Save as `.planning/phases/11-internal-screens-restyle/11-MANUAL-QA-CHECKLIST.md`.
  </action>
  <verify>
    <automated>test -f .planning/phases/11-internal-screens-restyle/11-MANUAL-QA-CHECKLIST.md &amp;&amp; rg "INT-0[1-8]" .planning/phases/11-internal-screens-restyle/11-MANUAL-QA-CHECKLIST.md | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/11-internal-screens-restyle/11-MANUAL-QA-CHECKLIST.md` exists.
    - File mentions all 8 INT requirements (INT-01..08).
    - File has at least 50 unchecked checkbox lines (`- [ ]`).
    - File has a Sign-off section at the end.
  </acceptance_criteria>
  <done>Manual QA checklist ready for human verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual QA — visit all 8 screens, validate per checklist</name>
  <what-built>
    All 8 internal staff screens restyled to the Claude Design bundle:
    - StaffLayout + Sidebar (with collapse + ThemeToggle)
    - LoginPage (split-panel)
    - DashboardPage (Instrument Serif h1 + warm KPIs + terracotta/mustard Recharts)
    - RoomRackCalendar (status-tokened bars)
    - RoomsPage (table → card grid) + RoomDrawer (warm-cream + tabs)
    - ReservationWizard (StepIndicator visual stepper)
    - HousekeepingPage (4-col kanban with priority badges)
    - ChatPanel (2-col warm-palette chat)
  </what-built>
  <how-to-verify>
    1. Start dev server: `pnpm --filter web dev`
    2. Open http://localhost:5173 in Chrome
    3. Open `.planning/phases/11-internal-screens-restyle/11-MANUAL-QA-CHECKLIST.md`
    4. Visit each section's URL and tick each acceptance criterion
    5. If everything passes: sign off the checklist's last section, mark Phase 11 complete
    6. If any gaps: note them in the "Gaps identified" section of the checklist
  </how-to-verify>
  <resume-signal>Type "approved" if all 8 screens match bundle, or list gaps (each gap → a Phase 11 gap-closure plan via `/gsd-plan-phase 11 --gaps`)</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Write 11-MILESTONE-CLOSEOUT.md (v1.1 closeout)</name>
  <files>.planning/phases/11-internal-screens-restyle/11-MILESTONE-CLOSEOUT.md</files>
  <read_first>
    - .planning/ROADMAP.md (v1.1 section)
    - .planning/REQUIREMENTS.md (VIS-01..05, PUB-07..13, INT-01..08)
    - All 11-0X-SUMMARY.md files (1-8)
    - 11-MANUAL-QA-CHECKLIST.md (signed-off version from Task 3)
  </read_first>
  <action>
    Create `.planning/phases/11-internal-screens-restyle/11-MILESTONE-CLOSEOUT.md`:

    ```markdown
    # v1.1 Milestone Closeout — Visual Identity Implementation

    **Closed:** YYYY-MM-DD
    **Status:** Complete
    **Duration:** Phase 9 + Phase 10 + Phase 11

    ---

    ## What Shipped

    ### Phase 9 — Design System Foundation (4 plans, complete 2026-05-17)
    - Token vocabulary in globals.css (~30 CSS variables)
    - Instrument Serif + Geist + Geist Mono fonts via Google Fonts @import
    - Dark mode via data-theme on `.hos` root + useTheme hook + ThemeToggle
    - 6 status pills (available/reserved/occupied/cleaning/maintenance/blocked)
    - Refactored Button (+terracotta variant), Card, Input, Badge primitives
    - Dev-only /design-system demo route + 5/5 Vitest smoke tests

    ### Phase 10 — Public Portal (6 plans)
    - Airbnb-style HotelHomePage (replaces bare booking landing)
    - TopNav (5 anchor sections), HeroGallery (4 hero photos), HotelIdentity
    - RoomsSection, ConciergeTeaser, RestaurantSection, LocationSection, ReviewsSection
    - ReservationWidget (desktop sticky sidebar / mobile bottom bar) with react-day-picker v10
    - Public Concierge chat restyle with warm palette + Instrument Serif

    ### Phase 11 — Internal Screens Restyle (9 plans, this milestone closeout)
    - StaffLayout + Sidebar with collapse mechanism + ThemeToggle footer mount
    - LoginPage split-panel (ink-1 left + warm-white right)
    - DashboardPage (Instrument Serif italic h1 + warm KPI cards + Recharts in terracotta/mustard)
    - RoomRackTable (status-tokened bars via RESERVATION_STATUS_TO_CSS)
    - RoomsPage migrated from table to card grid + RoomDrawer warm-cream restyle
    - ReservationWizard with new StepIndicator component (4 visual steps)
    - HousekeepingPage 4-column kanban with priority badges (Alta/Media/Baja)
    - Staff ChatPanel 2-col layout with warm-palette bubbles + Instrument Serif context headings

    ---

    ## v1.1 Requirements Status

    | ID | Description | Status |
    |----|-------------|--------|
    | VIS-01 | Tailwind v4 tokens for bundle | DONE |
    | VIS-02 | Instrument Serif + Geist + Geist Mono | DONE |
    | VIS-03 | Dark mode toggle | DONE |
    | VIS-04 | 6 status pills consistent across surfaces | DONE |
    | VIS-05 | Shadcn primitives refactored | DONE |
    | PUB-07 | HotelHomePage replaces bare landing | DONE |
    | PUB-08 | Hero gallery 4 photos | DONE |
    | PUB-09 | TopNav 5 items + anchor scroll | DONE |
    | PUB-10 | ReservationWidget sticky/bottom | DONE |
    | PUB-11 | Reseñas section | DONE |
    | PUB-12 | Responsive 360/768/1280 | DONE |
    | PUB-13 | Concierge restyle | DONE |
    | INT-01 | LoginPage split-panel | DONE |
    | INT-02 | DashboardPage tokens + Recharts | DONE |
    | INT-03 | RoomRackTable status bars | DONE |
    | INT-04 | RoomsPage cards + RoomDrawer | DONE |
    | INT-05 | ReservationWizard stepper | DONE |
    | INT-06 | HousekeepingPage kanban | DONE |
    | INT-07 | Staff ChatPanel restyle | DONE |
    | INT-08 | Sidebar restyle + collapse | DONE |

    Total: 20/20 v1.1 REQ-IDs complete.

    ---

    ## Deferred to v1.2

    | Item | Why Deferred |
    |------|--------------|
    | i18n EN/ES language toggle | Scope — UI work for v1.2 |
    | Topbar content (search/breadcrumb/notifications) | Phase 11 ships empty topbar shell |
    | LoginPage three-stat real data | Hardcoded in v1.1; backend endpoint deferred |
    | Storybook for primitives | Post-MVP |
    | Visual regression testing (Playwright screenshot diffs) | Post-MVP |
    | Sidebar collapse keyboard shortcut | Mouse-only in v1.1 |
    | Animation/motion library (Framer Motion) | CSS transitions only |
    | Interactive map (Leaflet/Google Maps) for LocationSection | Static placeholder in v1.1 |
    | Live photos in R2 (vs Unsplash CDN) | Unsplash CDN in v1.1 |
    | system-config endpoint for hotelName | Env var in v1.1 |
    | LegacyBookingPage cleanup | Preserved as rollback path; remove in v1.2 |

    ---

    ## Manual QA Result

    Manual QA performed: [date]
    Signed off by: [name]
    Result: [all pass / N gaps closed via 11-gaps-N plans]

    ---

    ## What's Next

    - Tag release: `git tag v1.1.0`
    - Run `/gsd-complete-milestone` to archive v1.1
    - Run `/gsd-new-milestone` to plan v1.2 (i18n + topbar content + payment gateway exploration)
    ```

    Replace `YYYY-MM-DD`, `[date]`, `[name]`, `[all pass / N gaps closed]` placeholders with actual values from Task 3 sign-off. If Task 3 surfaced gaps, list them under "Manual QA Result" and link to the gap-closure plans that closed them.
  </action>
  <verify>
    <automated>test -f .planning/phases/11-internal-screens-restyle/11-MILESTONE-CLOSEOUT.md &amp;&amp; rg "INT-0[1-8]" .planning/phases/11-internal-screens-restyle/11-MILESTONE-CLOSEOUT.md | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/11-internal-screens-restyle/11-MILESTONE-CLOSEOUT.md` exists.
    - File documents all 20 v1.1 REQ-IDs and their status.
    - File documents at least 5 deferred-to-v1.2 items.
    - File references manual QA sign-off result.
  </acceptance_criteria>
  <done>v1.1 milestone closeout document complete.</done>
</task>

</tasks>

<verification>
1. `cd apps/web && pnpm vitest run` → exits 0
2. `cd apps/web && pnpm tsc --noEmit -p tsconfig.json` → exits 0
3. Verification commands 2-5 (Task 1) all return zero matches
4. `.planning/phases/11-internal-screens-restyle/11-MANUAL-QA-CHECKLIST.md` exists and is signed off
5. `.planning/phases/11-internal-screens-restyle/11-MILESTONE-CLOSEOUT.md` exists with all 20 REQ-IDs marked DONE
</verification>

<success_criteria>
- Full apps/web test suite green.
- Zero hex literals, zero Tailwind palette classes, zero .hos-* global classes, zero v1.0 token references across Phase 11 file paths.
- Manual QA checklist exists, has been used, and is signed off.
- v1.1 milestone closeout document captures the full surface shipped + remaining deferrals.
- All 8 INT-* requirements verified.
- Phase 11 complete. v1.1 milestone complete.
</success_criteria>

<threat_model>
Documentation + verification only — no code changes. Risks:
- **False-pass on automated checks**: a `rg` query with a typo could return 0 matches even if violations exist. Mitigation: cross-check via TypeScript build + test suite (any unknown CSS class generates a runtime warning in dev mode).
- **Manual QA fatigue**: 60+ checklist items. Mitigation: organized per screen, can be split across multiple sessions if needed; checkpoint task gates progression.
- **Closeout document drift from actual state**: if a gap is identified in QA, closeout must reflect it. Mitigation: Task 4 explicitly reads Task 3 sign-off.
</threat_model>

<output>
After completion, create `.planning/phases/11-internal-screens-restyle/11-09-SUMMARY.md` documenting:
- Automated regression command results (table of 6 checks pass/fail)
- Manual QA result (clean / gaps identified)
- Milestone v1.1 closeout summary line
- Recommended next action: `/gsd-complete-milestone` or `/gsd-plan-phase 11 --gaps` if gaps
</output>
