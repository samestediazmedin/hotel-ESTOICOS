---
phase: 11-internal-screens-restyle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/lib/status-colors.ts
  - apps/web/src/router.tsx
  - apps/web/src/components/layout/StaffLayout.tsx
  - apps/web/src/components/layout/Sidebar.tsx
  - apps/web/src/hooks/useSidebarCollapsed.ts
autonomous: true
requirements: [INT-08]
must_haves:
  truths:
    - "Sidebar renders with terracotta-tint active state and 2px terracotta left accent bar on the active nav item"
    - "Sidebar collapses from 240px to 64px and back in 200ms via a toggle button, with state persisted in localStorage"
    - "Lucide icons render in text-ink-3 (inactive) and text-terracotta-deep (active)"
    - "ThemeToggle is mounted in the Sidebar footer and toggles data-theme on <html> as expected"
    - "StaffLayout renders a 56px empty topbar shell with bg-warm-white and border-b border-warm-line"
    - "router.tsx ProtectedRoute loading state uses bg-warm-paper and text-ink-3 (no v1.0 tokens left)"
    - "STATUS_COLORS and RESERVATION_STATUS_TO_CSS maps exist at apps/web/src/lib/status-colors.ts and export the locked status→CSS variable mappings"
  artifacts:
    - path: "apps/web/src/lib/status-colors.ts"
      provides: "Shared STATUS_COLORS + STATUS_BG_COLORS + RESERVATION_STATUS_TO_CSS exports"
      contains: "export const STATUS_COLORS"
    - path: "apps/web/src/hooks/useSidebarCollapsed.ts"
      provides: "Sidebar collapse state hook (localStorage-persisted boolean + toggle)"
      contains: "export function useSidebarCollapsed"
    - path: "apps/web/src/components/layout/StaffLayout.tsx"
      provides: "Topbar shell + main + sidebar composition"
      contains: "border-b border-warm-line"
    - path: "apps/web/src/components/layout/Sidebar.tsx"
      provides: "Restyled sidebar with collapse + accent bar + ThemeToggle"
      contains: "before:bg-terracotta"
    - path: "apps/web/src/router.tsx"
      provides: "ProtectedRoute loading state on bundle tokens"
      contains: "bg-warm-paper"
  key_links:
    - from: "apps/web/src/components/layout/Sidebar.tsx"
      to: "apps/web/src/hooks/useSidebarCollapsed.ts"
      via: "import + hook call"
      pattern: "useSidebarCollapsed\\("
    - from: "apps/web/src/components/layout/Sidebar.tsx"
      to: "apps/web/src/components/ui/theme-toggle"
      via: "ThemeToggle component mount in footer"
      pattern: "<ThemeToggle"
    - from: "apps/web/src/lib/status-colors.ts"
      to: "(downstream Wave 2 plans 11-03 / 11-04 / 11-07)"
      via: "named export STATUS_COLORS / RESERVATION_STATUS_TO_CSS"
      pattern: "export const STATUS_COLORS"
---

<objective>
Establish the visual chrome (StaffLayout + Sidebar) of the staff PMS to the bundle identity, AND publish the shared utilities every Wave 2 plan will consume. This is the Wave 1 prerequisite — all 7 other Phase 11 plans render inside this layout.

Purpose: a stable shell with terracotta active state + 200ms collapse + ThemeToggle in footer + a single source of truth for status→CSS variable mapping. Unblocks the 7 parallel restyles in Wave 2.

Output:
- `apps/web/src/lib/status-colors.ts` (new) — STATUS_COLORS, STATUS_BG_COLORS, RESERVATION_STATUS_TO_CSS
- `apps/web/src/hooks/useSidebarCollapsed.ts` (new) — localStorage-persisted boolean hook
- `apps/web/src/components/layout/StaffLayout.tsx` — Topbar shell + grid composition
- `apps/web/src/components/layout/Sidebar.tsx` — restyled, collapse mechanism, ThemeToggle mount
- `apps/web/src/router.tsx` — ProtectedRoute loading state token fix
</objective>

<execution_context>
@C:/Users/oscarpeinado/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/oscarpeinado/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-internal-screens-restyle/11-CONTEXT.md
@.planning/phases/11-internal-screens-restyle/11-RESEARCH.md
@.planning/phases/09-design-system-foundation/09-04-SUMMARY.md
@apps/web/src/styles/globals.css
@apps/web/src/components/layout/StaffLayout.tsx
@apps/web/src/components/layout/Sidebar.tsx
@apps/web/src/router.tsx
@apps/web/src/hooks/useTheme.ts
@apps/web/src/components/ui/theme-toggle.tsx
@.design-fetch/hotelos-ai/project/tokens.jsx

<interfaces>
<!-- Phase 9 primitives the Sidebar will consume — pre-resolved, no exploration needed -->

From @/hooks/useTheme:
```typescript
export type Theme = 'light' | 'dark';
export const STORAGE_KEY = 'hos-theme';
export function useTheme(): { theme: Theme; toggle: () => void; setTheme: (t: Theme) => void };
```

From @/components/ui/theme-toggle:
```typescript
export function ThemeToggle(props: { className?: string }): JSX.Element;
```

From @/features/auth/useAuth and @/features/auth/auth.store:
```typescript
export function useAuth(): { login: ...; logout: () => void; isLoading: boolean; error: string | null };
export const useAuthStore: ZustandStore<{ user: { role: string } | null }>;
```

Bundle CSS variables (from globals.css — DO NOT redefine):
- `--warm-paper`, `--warm-white`, `--warm-cream`, `--warm-line`
- `--ink-1`, `--ink-2`, `--ink-3`, `--ink-4`
- `--terracotta`, `--terracotta-deep`, `--terracotta-tint`, `--terracotta-soft`
- `--mustard`, `--olive`
- `--status-available`, `--status-reserved`, `--status-occupied`, `--status-cleaning`, `--status-maintenance`, `--status-blocked`
- `--status-available-bg`, `--status-reserved-bg`, `--status-occupied-bg`, `--status-cleaning-bg`, `--status-maintenance-bg`, `--status-blocked-bg`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create status-colors.ts + useSidebarCollapsed hook (shared utilities)</name>
  <files>apps/web/src/lib/status-colors.ts, apps/web/src/hooks/useSidebarCollapsed.ts</files>
  <read_first>
    - apps/web/src/styles/globals.css (lines 1-220, verify --status-* and --warm-* tokens exist)
    - apps/web/src/components/ui/status-pill.tsx (verify exported `RoomStatus` type for STATUS_COLORS keying)
    - .planning/phases/11-internal-screens-restyle/11-RESEARCH.md (lines 423-456 — Pattern 6 STATUS_COLORS shared constant)
  </read_first>
  <action>
    Create `apps/web/src/lib/status-colors.ts` with three exports:

    1. `STATUS_COLORS: Record<RoomStatus, string>` — maps each of the 6 room statuses (available, reserved, occupied, cleaning, maintenance, blocked) to its `var(--status-*)` CSS variable string. Import `RoomStatus` from `@/components/ui/status-pill`.

    2. `STATUS_BG_COLORS: Record<RoomStatus, string>` — same keys mapped to `var(--status-*-bg)` CSS variable strings.

    3. `RESERVATION_STATUS_TO_CSS: Record<ReservationStatus, string>` — locked reservation mapping (per CONTEXT.md decision #8):
       - `CONFIRMED → 'var(--status-reserved)'`
       - `CHECKED_IN → 'var(--status-occupied)'`  (terracotta)
       - `CHECKED_OUT → 'var(--status-maintenance)'`
       - `NO_SHOW → 'var(--status-blocked)'`
       - `CANCELLED → 'var(--ink-4)'` (faded)
       - `PENDING → 'var(--status-cleaning)'`

       Define `ReservationStatus` locally as a string union of those 6 names (no Prisma import — frontend-only — keep the file zero-runtime-dependency). Export both the union type and the record.

    Then create `apps/web/src/hooks/useSidebarCollapsed.ts`:

    - Named export `useSidebarCollapsed(): { collapsed: boolean; toggle: () => void }`.
    - Initial state read from `localStorage.getItem('sidebar-collapsed') === 'true'` inside a lazy `useState` initializer.
    - `toggle` writes the new value back to localStorage as the string `'true'` / `'false'` before setting state.
    - Storage key constant `SIDEBAR_COLLAPSED_KEY = 'sidebar-collapsed'` (also exported for tests).

    No React effect needed — initializer + write-on-toggle is sufficient. No SSR concerns (this is a Vite SPA — `window` is always available at component mount).
  </action>
  <verify>
    <automated>cd apps/web; pnpm tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/lib/status-colors.ts` exists and exports `STATUS_COLORS`, `STATUS_BG_COLORS`, `RESERVATION_STATUS_TO_CSS`, and `ReservationStatus` type union.
    - `rg "var\\(--status-(available|reserved|occupied|cleaning|maintenance|blocked)\\)" apps/web/src/lib/status-colors.ts` returns at least 6 matches.
    - `rg "#[0-9a-fA-F]{3,6}" apps/web/src/lib/status-colors.ts` returns 0 matches.
    - `apps/web/src/hooks/useSidebarCollapsed.ts` exists and exports `useSidebarCollapsed` + `SIDEBAR_COLLAPSED_KEY`.
    - `pnpm --filter web tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Shared utilities published, type-checked, ready for consumption by Wave 2 plans and by Task 2/3 below.</done>
</task>

<task type="auto">
  <name>Task 2: Fix router.tsx ProtectedRoute loading state tokens</name>
  <files>apps/web/src/router.tsx</files>
  <read_first>
    - apps/web/src/router.tsx (entire file — focus on ProtectedRoute component, lines 25-40 region)
    - .planning/phases/09-design-system-foundation/09-04-SUMMARY.md (lines 150-158 — deferred ProtectedRoute fix)
    - .planning/phases/11-internal-screens-restyle/11-RESEARCH.md (lines 525-530 — Pitfall 4)
  </read_first>
  <action>
    Replace the two v1.0 token references in ProtectedRoute's loading state:
    - `bg-bg-base` → `bg-warm-paper`
    - `text-text-muted` → `text-ink-3`

    These appear on the loading-state wrapper div used while session is being restored. No other changes to router.tsx in this task — devRoutes / Phase 9 demo route stays as-is. Do NOT touch the route tree itself.
  </action>
  <verify>
    <automated>cd apps/web; rg "bg-bg-base|text-text-muted" src/router.tsx | (! grep .)</automated>
  </verify>
  <acceptance_criteria>
    - `rg "bg-bg-base|text-text-muted" apps/web/src/router.tsx` returns 0 matches.
    - `rg "bg-warm-paper" apps/web/src/router.tsx` returns at least 1 match.
    - `rg "text-ink-3" apps/web/src/router.tsx` returns at least 1 match.
    - `pnpm --filter web tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>ProtectedRoute loading state no longer uses v1.0 tokens — Phase 9 deferred debt closed.</done>
</task>

<task type="auto">
  <name>Task 3: Restyle StaffLayout + Sidebar (chrome + collapse + ThemeToggle)</name>
  <files>apps/web/src/components/layout/StaffLayout.tsx, apps/web/src/components/layout/Sidebar.tsx</files>
  <read_first>
    - apps/web/src/components/layout/StaffLayout.tsx (current — 23 lines)
    - apps/web/src/components/layout/Sidebar.tsx (current — 116 lines)
    - apps/web/src/styles/globals.css (verify warm-* + ink-* tokens)
    - .design-fetch/hotelos-ai/project/tokens.jsx (lines 105-200 — chrome primitives `.hos-app`, `.hos-sidebar`, `.hos-topbar`, `.hos-nav`, `.hos-nav.active`, `.hos-nav-section`, `.hos-logo-mark`)
    - .planning/phases/11-internal-screens-restyle/11-CONTEXT.md (lines 126-138 — INT-08 contract)
    - .planning/phases/11-internal-screens-restyle/11-RESEARCH.md (lines 330-475 — Patterns 3, 4, 7)
    - apps/web/src/components/ui/theme-toggle.tsx (verify ThemeToggle props)
  </read_first>
  <action>
    **StaffLayout.tsx** — keep flex layout (per research recommendation) but add a topbar row above main content:
    ```tsx
    <div className="hos flex min-h-screen bg-warm-paper">
      <Sidebar />
      <div className="flex-1 flex flex-col min-w-0">
        <header className="h-14 shrink-0 bg-warm-white border-b border-warm-line flex items-center px-6 gap-4" aria-label="Barra superior" />
        <main className="flex-1 overflow-auto">
          <Outlet />
        </main>
      </div>
      <ChatPanel />
    </div>
    ```
    - Wrap with `hos` class (CSS variable scope — already used in Phase 10).
    - Topbar is empty in v1.1 (content deferred to v1.2 per locked decision #9).
    - `min-w-0` on the inner flex column prevents overflow during sidebar collapse animation (per Pitfall 7).
    - Keep `ChatPanel` mount unchanged.

    **Sidebar.tsx** — full restyle:

    1. Import `useSidebarCollapsed` from `@/hooks/useSidebarCollapsed`. Import `ThemeToggle` from `@/components/ui/theme-toggle`. Import `ChevronLeft`, `ChevronRight`, `LogOut` from `lucide-react`.

    2. Replace the `<aside>` className:
       - From `w-56 shrink-0 bg-surface border-r border-border-subtle flex flex-col min-h-screen`
       - To `shrink-0 bg-warm-white border-r border-warm-line flex flex-col transition-[width] duration-200 ease-in-out overflow-hidden ${collapsed ? 'w-16' : 'w-60'}` (template literal, NOT string concat — use a `cn` helper or template literal).

    3. **Branding block**: keep `HotelBranding` import. Wrap in a div `flex items-center px-4 py-4 border-b border-warm-line gap-2`. When `collapsed`, render only the H mark (a span `w-7 h-7 rounded-lg bg-terracotta text-warm-white flex items-center justify-center font-display italic text-lg`) — text hidden. When expanded, render `HotelBranding` as today.

    4. **Collapse toggle button**: insert immediately after Branding, as a self-row at `px-2 py-1`. Button: `<button onClick={toggle} aria-label="Plegar barra lateral" className="w-full flex items-center justify-end px-2 py-1 text-ink-3 hover:text-ink-1 transition-colors">{collapsed ? <ChevronRight size={16} /> : <ChevronLeft size={16} />}</button>`.

    5. **Nav items**: change NavLink className callback to:
       ```tsx
       ({ isActive }) =>
         `relative flex items-center gap-3 px-3 py-2 rounded-md text-sm transition-colors ${
           isActive
             ? 'bg-terracotta-tint text-terracotta-deep before:absolute before:left-0 before:top-1 before:bottom-1 before:w-[3px] before:bg-terracotta before:rounded-r'
             : 'text-ink-2 hover:bg-warm-cream hover:text-ink-1'
         }`
       ```
       Icon: keep `<Icon className="h-4 w-4 shrink-0" aria-hidden />`. Add a className on the label `<span className={collapsed ? 'hidden' : 'block'}>{item.label}</span>`. When `collapsed`, also add `title={item.label}` on the NavLink for tooltip on hover.

    6. **Section labels** (optional structure improvement — add 3 sections "PRINCIPAL", "OPERACIÓN", "ADMINISTRACIÓN" by partitioning NAV_ITEMS):
       - PRINCIPAL: Dashboard, Reservas, Huéspedes
       - OPERACIÓN: Housekeeping, Habitaciones, Tipos de hab.
       - ADMINISTRACIÓN: Tarifas, Temporadas, Reportes, Usuarios, Concierge
       Render each section title as `<p className="text-xs uppercase tracking-widest text-ink-4 px-3 mt-4 mb-1 font-medium ${collapsed ? 'hidden' : ''}">PRINCIPAL</p>` (hidden when collapsed). DO NOT change the underlying NavItem array structure aggressively — partition inline.

    7. **AI Assistant button**: keep functionality. Restyle to `flex items-center gap-3 w-full px-3 py-2 rounded-md text-sm text-ink-2 hover:bg-warm-cream hover:text-ink-1 transition-colors`. Hide label when collapsed.

    8. **Footer block**: replace the existing logout block with a footer `border-t border-warm-line p-2 flex flex-col gap-1`:
       - First row: `<ThemeToggle />` (Phase 9 component — mounts where it likes; if it doesn't accept className, wrap in `<div className="px-1">`).
       - Second row: logout button restyled `flex items-center gap-3 w-full px-3 py-2 rounded-md text-sm text-ink-3 hover:text-ink-1 hover:bg-warm-cream transition-colors`. Replace text-only with `<LogOut size={16} />` icon + `<span className={collapsed ? 'hidden' : ''}>Cerrar sesión</span>`.

    9. **Eliminate the v1.0 typo `bg-terracota/10`** — already removed by step 5 above (per Pitfall on Anti-Patterns, "DO NOT use `bg-terracota` (one 't')").

    Do NOT modify NAV_ITEMS keys/routes/roles. Do NOT remove the `useAuth` / `useAuthStore` / `useAiChatStore` imports. Preserve all behavior.
  </action>
  <verify>
    <automated>cd apps/web; pnpm tsc --noEmit -p tsconfig.json &amp;&amp; rg "#[0-9a-fA-F]{3,6}" src/components/layout/StaffLayout.tsx src/components/layout/Sidebar.tsx | (! grep .) &amp;&amp; rg "bg-(surface|bg-base|terracota/|brand-primary|surface-hover|surface-elevated)|text-(text-primary|text-secondary|text-muted|text-brand)|border-border-subtle" src/components/layout/StaffLayout.tsx src/components/layout/Sidebar.tsx | (! grep .)</automated>
  </verify>
  <acceptance_criteria>
    - `rg "#[0-9a-fA-F]{3,6}" apps/web/src/components/layout/StaffLayout.tsx apps/web/src/components/layout/Sidebar.tsx` returns 0 matches.
    - `rg "bg-(surface|bg-base|terracota/|brand-primary|surface-hover|surface-elevated)|text-(text-primary|text-secondary|text-muted|text-brand)|border-border-subtle" apps/web/src/components/layout/StaffLayout.tsx apps/web/src/components/layout/Sidebar.tsx` returns 0 matches (no v1.0 token names).
    - `rg "before:bg-terracotta" apps/web/src/components/layout/Sidebar.tsx` returns at least 1 match (active accent bar).
    - `rg "transition-\\[width\\] duration-200" apps/web/src/components/layout/Sidebar.tsx` returns at least 1 match.
    - `rg "useSidebarCollapsed" apps/web/src/components/layout/Sidebar.tsx` returns at least 1 match.
    - `rg "<ThemeToggle" apps/web/src/components/layout/Sidebar.tsx` returns at least 1 match.
    - `rg "h-14.*bg-warm-white.*border-b.*border-warm-line|bg-warm-white border-b border-warm-line.*h-14|h-14 shrink-0 bg-warm-white" apps/web/src/components/layout/StaffLayout.tsx` returns at least 1 match (topbar shell).
    - `pnpm --filter web tsc --noEmit` exits 0.
    - Manual visual check (deferred to Wave 3): hard-refresh `/dashboard`, confirm sidebar renders with terracotta active item + 2px accent bar; click toggle button, confirm 200ms width transition; reload page, confirm collapse state persists.
  </acceptance_criteria>
  <done>StaffLayout and Sidebar render the bundle chrome with working collapse + ThemeToggle. Wave 2 plans can now consume the stable shell.</done>
</task>

</tasks>

<verification>
1. `cd apps/web && pnpm tsc --noEmit -p tsconfig.json` → exits 0
2. `rg "#[0-9a-fA-F]{3,6}" apps/web/src/components/layout/ apps/web/src/lib/status-colors.ts apps/web/src/hooks/useSidebarCollapsed.ts apps/web/src/router.tsx` → 0 matches
3. `rg "bg-bg-base|text-text-muted|bg-surface|text-text-primary|text-text-secondary|border-border-subtle|bg-terracota/" apps/web/src/components/layout/ apps/web/src/router.tsx` → 0 matches
4. `pnpm --filter web test src/features/design-system/DesignSystemPage.test.tsx` → still passes (Phase 9 smoke test must remain green — Sidebar restyle doesn't affect it but ThemeToggle's behavior must be unchanged)
</verification>

<success_criteria>
- Shared `status-colors.ts` exported (3 records + 1 type), zero hex literals.
- `useSidebarCollapsed` hook persists state in localStorage under key `sidebar-collapsed`.
- StaffLayout renders a 56px empty topbar shell with `bg-warm-white border-b border-warm-line`.
- Sidebar collapses to `w-16` and expands to `w-60` with `transition-[width] duration-200`.
- Active NavLink shows terracotta-tint bg + 2px terracotta left accent bar via `before:` pseudo-element.
- ThemeToggle mounted in Sidebar footer.
- router.tsx ProtectedRoute uses `bg-warm-paper` + `text-ink-3`.
- TypeScript compiles, Phase 9 smoke test still passes.
- INT-08 ROADMAP success criterion 8 satisfied.
</success_criteria>

<threat_model>
Frontend-only — no security boundary touched. Risk surface:
- **Layout regression**: 7 inner screens (Dashboard, Calendar, Rooms, Reservations, Housekeeping, ChatPanel) depend on the new flex column. Mitigation: keep flex layout (research recommendation), use `min-w-0` on the inner column to prevent overflow.
- **localStorage corruption**: malformed `sidebar-collapsed` value → defaults to `false` (expanded) which is the safer state.
- **ThemeToggle mount duplication**: Phase 9 may already mount ThemeToggle somewhere else. Verified: 09-04-SUMMARY confirms placement deferred to Phase 11 — no prior mount exists.
</threat_model>

<output>
After completion, create `.planning/phases/11-internal-screens-restyle/11-01-SUMMARY.md` documenting:
- New files (status-colors.ts, useSidebarCollapsed.ts)
- Sidebar structural changes (collapse, sections, ThemeToggle, accent bar)
- StaffLayout topbar shell addition
- router.tsx token fix
- Any deviations (e.g., if section partitioning was simpler than the 3-section plan)
</output>
