---
phase: 11-internal-screens-restyle
plan: 03
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - apps/web/src/features/reporting/DashboardPage.tsx
  - apps/web/src/features/reporting/KpiCard.tsx
  - apps/web/src/features/reporting/OccupancyBarChart.tsx
  - apps/web/src/features/reporting/RoomStatusDonut.tsx
autonomous: true
requirements: [INT-02]
must_haves:
  truths:
    - "Dashboard page heading is Instrument Serif italic at text-3xl"
    - "All KPI card numeric values use font-mono and text-ink-1"
    - "OccupancyBarChart highlighted-day bar fills with var(--terracotta), other days with var(--mustard)"
    - "RoomStatusDonut slices read colors from STATUS_COLORS (imported from @/lib/status-colors)"
    - "Existing useDashboardKpis() React Query hook and all data-fetching behavior unchanged"
    - "Existing DashboardPage.test.tsx (7 tests) and OccupancyBarChart.test.tsx (4 tests) still pass"
  artifacts:
    - path: "apps/web/src/features/reporting/DashboardPage.tsx"
      provides: "Restyled dashboard with Instrument Serif heading + restyled KPI grid"
      contains: "font-display italic"
    - path: "apps/web/src/features/reporting/KpiCard.tsx"
      provides: "Restyled KPI card with warm-paper bg, font-mono value, delta indicator"
      contains: "font-mono"
    - path: "apps/web/src/features/reporting/OccupancyBarChart.tsx"
      provides: "Bar chart with var(--terracotta)/var(--mustard) per-bar conditional"
      contains: "var(--terracotta)"
    - path: "apps/web/src/features/reporting/RoomStatusDonut.tsx"
      provides: "Donut chart consuming STATUS_COLORS from lib"
      contains: "STATUS_COLORS"
  key_links:
    - from: "apps/web/src/features/reporting/RoomStatusDonut.tsx"
      to: "apps/web/src/lib/status-colors.ts"
      via: "named import"
      pattern: "import.*STATUS_COLORS.*from.*@/lib/status-colors"
    - from: "apps/web/src/features/reporting/OccupancyBarChart.tsx"
      to: "(Recharts Bar shape prop)"
      via: "shape={(props) => <rect fill={...} />}"
      pattern: "shape="
---

<objective>
Restyle the Dashboard (INT-02): h1 in Instrument Serif italic 32px, KPI cards on warm-paper with font-mono numbers and delta indicator, Recharts BarChart bars in terracotta (today) / mustard (other days), Recharts donut slices via shared STATUS_COLORS.

Purpose: dashboard is the most-viewed staff page — first impression of the visual identity. Recharts SVG fills must use CSS variable strings (Pattern 2 from research).

Output: 4 modified files, all hex literals replaced with `var(--*)`, all v1.0 tokens replaced with bundle tokens.
</objective>

<execution_context>
@C:/Users/oscarpeinado/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/oscarpeinado/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-internal-screens-restyle/11-CONTEXT.md
@.planning/phases/11-internal-screens-restyle/11-RESEARCH.md
@apps/web/src/styles/globals.css
@apps/web/src/features/reporting/DashboardPage.tsx
@apps/web/src/features/reporting/KpiCard.tsx
@apps/web/src/features/reporting/OccupancyBarChart.tsx
@apps/web/src/features/reporting/RoomStatusDonut.tsx
@apps/web/src/features/reporting/__tests__/DashboardPage.test.tsx
@apps/web/src/features/reporting/__tests__/OccupancyBarChart.test.tsx
@.design-fetch/hotelos-ai/project/screens/dashboard.jsx

<interfaces>
From @/lib/status-colors (created in 11-01):
```typescript
export const STATUS_COLORS: Record<RoomStatus, string>;
// e.g. STATUS_COLORS.occupied === 'var(--status-occupied)'
```

Recharts v2 API (Pattern 2 in research, lines 299-327):
```tsx
<Bar
  dataKey="occupancyPct"
  radius={[4, 4, 0, 0]}
  shape={(props: any) => (
    <rect
      x={props.x} y={props.y}
      width={props.width} height={props.height}
      fill={props.payload.isToday ? 'var(--terracotta)' : 'var(--mustard)'}
      rx={4}
    />
  )}
/>

<Pie data={...}>
  {data.map((entry) => <Cell key={entry.name} fill={STATUS_COLORS[entry.statusKey]} />)}
</Pie>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle DashboardPage + KpiCard</name>
  <files>apps/web/src/features/reporting/DashboardPage.tsx, apps/web/src/features/reporting/KpiCard.tsx</files>
  <read_first>
    - apps/web/src/features/reporting/DashboardPage.tsx (entire file, 179 lines)
    - apps/web/src/features/reporting/KpiCard.tsx (entire file, 38 lines)
    - apps/web/src/features/reporting/__tests__/DashboardPage.test.tsx (verify test query patterns — must not break)
    - .design-fetch/hotelos-ai/project/screens/dashboard.jsx (entire file — KPI grid + chart layout reference)
    - .planning/phases/11-internal-screens-restyle/11-CONTEXT.md (lines 61-67 — INT-02 contract)
    - .planning/phases/11-internal-screens-restyle/11-RESEARCH.md (lines 100-114, 564-588 — KPI restyle + before/after example)
    - apps/web/src/styles/globals.css (verify --warm-paper, --warm-line, --ink-1, --ink-3, --status-available, --terracotta, --mustard)
  </read_first>
  <action>
    **DashboardPage.tsx** — token rename pass + heading restyle.

    Apply the migration map (RESEARCH lines 264-291) globally:
    - `bg-bg-base` / `bg-surface-elevated` → `bg-warm-paper` (page bg)
    - `bg-surface` → `bg-warm-white` (any card/panel inside the page)
    - `border-border-subtle` → `border-warm-line`
    - `border-border-strong` → `border-warm-line-strong`
    - `text-text-primary` → `text-ink-1`
    - `text-text-secondary` → `text-ink-2`
    - `text-text-muted` → `text-ink-3`
    - `text-text-brand` → `text-terracotta-deep`
    - `text-brand-primary` → `text-terracotta`
    - `bg-brand-primary` → `bg-terracotta`
    - `bg-brand-primary/10` / `bg-terracota/10` → `bg-terracotta-tint`
    - `bg-brand-primary-soft` → `bg-terracotta-soft`
    - `focus:ring-brand-primary` → `focus:ring-terracotta`

    **Heading restyle**: find the page h1 (typically "Buen día, {firstName}" or "Dashboard") and change className to `font-display italic text-3xl text-ink-1 mb-6`. If the heading currently uses a different copy/structure, preserve the dynamic content (e.g. `{firstName}`) but apply the className.

    **Section dividers**: any `<hr>` or section separator → `border-t border-warm-line my-8`.

    **Loading/error states**: any `bg-surface` skeleton → `bg-warm-cream animate-pulse`; error text → `text-terracotta`.

    DO NOT modify:
    - `useDashboardKpis()` hook usage
    - Any React Query keys or query options
    - The structure of the KPI grid (grid template — only className tokens change)
    - The Recharts component imports (handled in Task 2)

    **KpiCard.tsx** — full restyle:
    ```tsx
    interface KpiCardProps {
      label: string;
      value: string | number;
      delta?: number;        // percentage e.g., 4 = +4%, -2 = -2%
      // existing props preserved — add `delta` only if not already there
    }

    export function KpiCard({ label, value, delta }: KpiCardProps) {
      const showDelta = typeof delta === 'number';
      const isPositive = showDelta && delta > 0;
      return (
        <div className="bg-warm-paper border border-warm-line rounded-xl p-4">
          <p className="text-[11px] font-medium uppercase tracking-widest text-ink-3">
            {label}
          </p>
          <p className="font-mono text-3xl text-ink-1 mt-1">{value}</p>
          {showDelta && (
            <span className={`text-xs mt-1 inline-block ${isPositive ? 'text-olive' : 'text-terracotta'}`}>
              {isPositive ? '↑' : '↓'} {Math.abs(delta!)}%
            </span>
          )}
        </div>
      );
    }
    ```
    - REMOVE any `shadow-sm` class (Phase 9 decision: bundle hos-card uses border, not shadow).
    - REMOVE any v1.0 `tone` prop translation IF the existing KpiCard had one — replace with the `delta` indicator above. If the existing component receives a `tone` prop from the parent, retain backward compatibility by accepting `tone?: string` but ignoring it in the render output. Document this in the SUMMARY.
    - Preserve any existing props passed by DashboardPage. If DashboardPage today passes only `label` and `value`, that's fine. If it passes a `delta` or `change`, keep the wiring.

    Inspect the existing `KpiCard.tsx` and `DashboardPage.tsx` first to see what props are actually in use, then conform to that contract.
  </action>
  <verify>
    <automated>cd apps/web; pnpm tsc --noEmit -p tsconfig.json &amp;&amp; rg "#[0-9a-fA-F]{3,6}" src/features/reporting/DashboardPage.tsx src/features/reporting/KpiCard.tsx | (! grep .) &amp;&amp; pnpm vitest run src/features/reporting/__tests__/DashboardPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `rg "#[0-9a-fA-F]{3,6}" apps/web/src/features/reporting/DashboardPage.tsx apps/web/src/features/reporting/KpiCard.tsx` returns 0 matches.
    - `rg "bg-(bg-base|surface|brand-primary)|border-border-subtle|text-(text-primary|text-secondary|text-muted|text-brand)" apps/web/src/features/reporting/DashboardPage.tsx apps/web/src/features/reporting/KpiCard.tsx` returns 0 matches.
    - `rg "font-display italic" apps/web/src/features/reporting/DashboardPage.tsx` returns at least 1 match (h1).
    - `rg "font-mono" apps/web/src/features/reporting/KpiCard.tsx` returns at least 1 match (value).
    - `rg "bg-warm-paper" apps/web/src/features/reporting/KpiCard.tsx` returns at least 1 match.
    - `pnpm --filter web vitest run src/features/reporting/__tests__/DashboardPage.test.tsx` exits 0 (7/7 tests pass).
  </acceptance_criteria>
  <done>DashboardPage and KpiCard render with bundle tokens. Existing 7 tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle Recharts components (OccupancyBarChart + RoomStatusDonut)</name>
  <files>apps/web/src/features/reporting/OccupancyBarChart.tsx, apps/web/src/features/reporting/RoomStatusDonut.tsx</files>
  <read_first>
    - apps/web/src/features/reporting/OccupancyBarChart.tsx (entire file, 98 lines)
    - apps/web/src/features/reporting/RoomStatusDonut.tsx (entire file, 83 lines)
    - apps/web/src/lib/status-colors.ts (file created in 11-01 — verify export names)
    - apps/web/src/features/reporting/__tests__/OccupancyBarChart.test.tsx (verify test queries — they should query by text label, not by fill color)
    - .planning/phases/11-internal-screens-restyle/11-RESEARCH.md (lines 295-327 — Pattern 2 Recharts color injection; lines 502-516 — Pitfalls 1 & 2; lines 596-617 — code example)
    - .design-fetch/hotelos-ai/project/screens/dashboard.jsx (chart color references)
  </read_first>
  <action>
    **OccupancyBarChart.tsx**:

    1. Replace any `<Bar fill="#c45a3a" />` or `<Bar fill="some-hex" />` with the `shape` prop pattern:
       ```tsx
       <Bar
         dataKey="occupancyPct"
         radius={[4, 4, 0, 0]}
         shape={(props: any) => (
           <rect
             x={props.x}
             y={props.y}
             width={props.width}
             height={props.height}
             fill={props.payload?.isToday ? 'var(--terracotta)' : 'var(--mustard)'}
             rx={4}
           />
         )}
       />
       ```
    2. Update the data transform to add `isToday: boolean` per row. If the chart receives `snapshots` props with `businessDate` field, compare to `todayIso` (existing logic) and set `isToday`. If no `todayIso` is available, accept it as a prop OR compute from a `useTodayIso` helper if one exists.
    3. `<CartesianGrid stroke="..." />` → `stroke="var(--warm-line-strong)"`.
    4. `<XAxis tick={{ fill: "..." }} />` and `<YAxis tick={{ fill: "..." }} />` → `tick={{ fill: 'var(--ink-3)' }}`.
    5. `<Tooltip contentStyle={...} />` — if present, set `backgroundColor: 'var(--warm-white)'`, `border: '1px solid var(--warm-line)'`, `color: 'var(--ink-1)'`.
    6. Replace any v1.0 token classes on container divs using migration map (Task 1).

    **RoomStatusDonut.tsx**:

    1. Replace the local `STATUS_COLORS` hex record (4 hex literals per research) with the import:
       ```tsx
       import { STATUS_COLORS } from '@/lib/status-colors';
       ```
    2. Update `<Cell />` mapping — each cell uses `STATUS_COLORS[entry.statusKey]` (e.g., `STATUS_COLORS.occupied`). Adapt the existing data shape — if data items have a different key name (e.g., `name: 'Ocupada'`), add a `statusKey` field of type `RoomStatus` to each item in the data transform (the union: 'available' | 'reserved' | 'occupied' | 'cleaning' | 'maintenance' | 'blocked'). Spanish label remains for tooltip/legend.
    3. Tooltip + container styling per (Task 2 step 5 above).
    4. Apply migration map to any container divs.

    **DO NOT**:
    - Change the React Query hook calls or any data fetching logic.
    - Change the chart's external prop contract.
    - Pass Tailwind class strings to `fill` (Pitfall 1 — `fill="bg-terracotta"` is invalid).
    - Use `<Cell />` inside `<Bar />` (Pitfall 2 — use `shape` prop instead).
  </action>
  <verify>
    <automated>cd apps/web; pnpm tsc --noEmit -p tsconfig.json &amp;&amp; rg "#[0-9a-fA-F]{3,6}" src/features/reporting/OccupancyBarChart.tsx src/features/reporting/RoomStatusDonut.tsx | (! grep .) &amp;&amp; pnpm vitest run src/features/reporting/__tests__/OccupancyBarChart.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `rg "#[0-9a-fA-F]{3,6}" apps/web/src/features/reporting/OccupancyBarChart.tsx apps/web/src/features/reporting/RoomStatusDonut.tsx` returns 0 matches.
    - `rg "var\\(--terracotta\\)" apps/web/src/features/reporting/OccupancyBarChart.tsx` returns at least 1 match.
    - `rg "var\\(--mustard\\)" apps/web/src/features/reporting/OccupancyBarChart.tsx` returns at least 1 match.
    - `rg "from '@/lib/status-colors'" apps/web/src/features/reporting/RoomStatusDonut.tsx` returns exactly 1 match.
    - `rg "STATUS_COLORS\\[" apps/web/src/features/reporting/RoomStatusDonut.tsx` returns at least 1 match.
    - `rg "shape=" apps/web/src/features/reporting/OccupancyBarChart.tsx` returns at least 1 match (per-bar conditional pattern).
    - `pnpm --filter web vitest run src/features/reporting/__tests__/OccupancyBarChart.test.tsx` exits 0 (4/4 tests pass).
  </acceptance_criteria>
  <done>Recharts components fully themed via CSS variables. Both chart test suites pass.</done>
</task>

<task type="auto">
  <name>Task 3: Regression — full reporting test suite</name>
  <files>(verification only)</files>
  <read_first>
    - All 4 modified files (final state)
  </read_first>
  <action>
    Run the dashboard's test files end-to-end:
    ```
    pnpm --filter web vitest run src/features/reporting/
    ```
    Verify both `DashboardPage.test.tsx` (7 tests) and `OccupancyBarChart.test.tsx` (4 tests) pass. If any new failure surfaces, diagnose: most likely cause is a JSX restructure that broke a test selector — fix the code, not the test.
  </action>
  <verify>
    <automated>cd apps/web; pnpm vitest run src/features/reporting/</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter web vitest run src/features/reporting/` exits 0.
    - 11/11 reporting tests pass.
  </acceptance_criteria>
  <done>INT-02 visually restyled, regression-safe.</done>
</task>

</tasks>

<verification>
1. `cd apps/web && pnpm tsc --noEmit -p tsconfig.json` → exits 0
2. `rg "#[0-9a-fA-F]{3,6}" apps/web/src/features/reporting/*.tsx` → 0 matches in non-test files
3. `rg "text-(gray|blue|red|green|yellow|amber|orange)-[0-9]" apps/web/src/features/reporting/*.tsx --glob '!*test*'` → 0 matches
4. `pnpm --filter web vitest run src/features/reporting/` → 11/11 green
</verification>

<success_criteria>
- DashboardPage h1 in Instrument Serif italic.
- All KPI cards on warm-paper bg with font-mono numbers.
- OccupancyBarChart bars conditionally colored terracotta (today) / mustard (others).
- RoomStatusDonut consumes STATUS_COLORS from `@/lib/status-colors`.
- 11/11 existing tests pass.
- INT-02 ROADMAP success criterion 2 satisfied.
</success_criteria>

<threat_model>
Frontend-only — no security boundary touched. Risk:
- **Recharts `shape` prop typings**: `props: any` is intentional — Recharts v2 typings for `shape` are loose. Mitigation: cast as `any`, runtime behavior verified by existing tests.
- **isToday flag missing**: if the data transform forgets `isToday`, all bars render mustard (no `props.payload?.isToday` → falsy → mustard). Mitigation: explicit acceptance criterion check via test that asserts terracotta fill on the today column (already present in OccupancyBarChart.test.tsx per RESEARCH line 108).
</threat_model>

<output>
After completion, create `.planning/phases/11-internal-screens-restyle/11-03-SUMMARY.md` documenting the Recharts CSS-variable approach, KpiCard delta indicator, and any tone-prop backward-compat note.
</output>
