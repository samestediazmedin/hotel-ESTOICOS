---
phase: 11-internal-screens-restyle
plan: 04
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - apps/web/src/features/reservations/RoomRackCalendar.tsx
  - apps/web/src/features/reservations/components/RoomRackTable.tsx
autonomous: true
requirements: [INT-03]
must_haves:
  truths:
    - "Date header row has bg-warm-cream + border-b border-warm-line with font-mono day labels"
    - "Today's date column is highlighted with bg-terracotta-tint and text-terracotta-deep"
    - "Room row headers are font-mono text-ink-1"
    - "Reservation bars apply color via inline style using RESERVATION_STATUS_TO_CSS (status → CSS variable string)"
    - "Empty cells render bg-warm-white border border-warm-line; selected cell shows bg-terracotta-tint border-2 border-terracotta"
    - "Hover state on reservation bar shows outline outline-2 outline-terracotta"
    - "All existing scroll/click/drag-to-create behaviors preserved"
  artifacts:
    - path: "apps/web/src/features/reservations/components/RoomRackTable.tsx"
      provides: "Restyled room rack table"
      contains: "RESERVATION_STATUS_TO_CSS"
    - path: "apps/web/src/features/reservations/RoomRackCalendar.tsx"
      provides: "Updated chrome (token rename only — 9-line file)"
      contains: "RoomRackTable"
  key_links:
    - from: "apps/web/src/features/reservations/components/RoomRackTable.tsx"
      to: "apps/web/src/lib/status-colors.ts"
      via: "named import"
      pattern: "import.*RESERVATION_STATUS_TO_CSS.*from.*@/lib/status-colors"
---

<objective>
Restyle the RoomRackTable calendar (INT-03): date header row in warm-cream + font-mono labels, today highlighted, reservation bars color-coded by status via inline style consuming RESERVATION_STATUS_TO_CSS from the shared lib, hover/select states, empty cells warm-white.

Purpose: the calendar is the operational heartbeat of the staff PMS — bars must communicate status at a glance using the bundle palette. All existing scroll/click/drag behaviors must remain identical.

Output: 2 modified files. Zero hex literals. Zero Tailwind palette classes.
</objective>

<execution_context>
@C:/Users/oscarpeinado/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/oscarpeinado/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-internal-screens-restyle/11-CONTEXT.md
@.planning/phases/11-internal-screens-restyle/11-RESEARCH.md
@apps/web/src/styles/globals.css
@apps/web/src/features/reservations/RoomRackCalendar.tsx
@apps/web/src/features/reservations/components/RoomRackTable.tsx
@.design-fetch/hotelos-ai/project/screens/calendar.jsx

<interfaces>
From @/lib/status-colors (created in 11-01):
```typescript
export type ReservationStatus = 'CONFIRMED' | 'CHECKED_IN' | 'CHECKED_OUT' | 'NO_SHOW' | 'CANCELLED' | 'PENDING';
export const RESERVATION_STATUS_TO_CSS: Record<ReservationStatus, string>;
// e.g. RESERVATION_STATUS_TO_CSS.CHECKED_IN === 'var(--status-occupied)'
```

Locked mapping per CONTEXT decision #8:
- CONFIRMED → reserved-blue
- CHECKED_IN → occupied (terracotta)
- CHECKED_OUT → maintenance
- NO_SHOW → blocked
- CANCELLED → ink-4 (faded)
- PENDING → cleaning (mustard)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle RoomRackTable + RoomRackCalendar</name>
  <files>apps/web/src/features/reservations/components/RoomRackTable.tsx, apps/web/src/features/reservations/RoomRackCalendar.tsx</files>
  <read_first>
    - apps/web/src/features/reservations/components/RoomRackTable.tsx (entire file, 246 lines — focus on STATUS_COLORS record near top, day header cells, room row headers, reservation bar rendering, empty cells, hover/selected states)
    - apps/web/src/features/reservations/RoomRackCalendar.tsx (entire file, 9 lines — re-export wrapper)
    - apps/web/src/lib/status-colors.ts (verify exports from 11-01)
    - .design-fetch/hotelos-ai/project/screens/calendar.jsx (entire file — visual reference)
    - .planning/phases/11-internal-screens-restyle/11-CONTEXT.md (lines 69-75 — INT-03 contract verbatim)
    - .planning/phases/11-internal-screens-restyle/11-RESEARCH.md (lines 110-129 — INT-03 blast radius; lines 764-769 — Open Question 1 resolution; lines 451-454 — inline style approach)
  </read_first>
  <action>
    **RoomRackCalendar.tsx** (9-line wrapper):

    If this file is purely a re-export (`export { RoomRackTable } from './components/RoomRackTable'` and/or a thin wrapper component), apply only token renames (none likely needed). If there is any wrapper JSX with v1.0 tokens, apply the migration map.

    **RoomRackTable.tsx** — substantive restyle:

    1. **Remove the local `STATUS_COLORS` record** that contains Tailwind palette classes (`bg-blue-500`, `bg-green-500`, `bg-gray-400`, `bg-yellow-400`, `bg-gray-200`, `bg-red-300`).

    2. **Import** at the top:
       ```tsx
       import { RESERVATION_STATUS_TO_CSS, type ReservationStatus } from '@/lib/status-colors';
       ```
       If the file uses a Prisma-generated `ReservationStatus` enum type, KEEP that import (the union from status-colors is a string-literal alias). Cast where necessary: `RESERVATION_STATUS_TO_CSS[reservation.status as ReservationStatus]`.

    3. **Reservation bar rendering** — for each bar, replace the colorClass approach with inline style:
       ```tsx
       <div
         data-status={reservation.status}
         className="absolute top-1 bottom-1 rounded text-warm-white text-xs font-medium px-2 flex items-center cursor-pointer transition-all hover:opacity-90 hover:outline hover:outline-2 hover:outline-terracotta"
         style={{ backgroundColor: RESERVATION_STATUS_TO_CSS[reservation.status as ReservationStatus] }}
       >
         {reservation.guestName ?? 'Reserva'}
       </div>
       ```
       - For CANCELLED bars, set `opacity: 0.6` additionally (inline style merge): `style={{ backgroundColor: ..., opacity: reservation.status === 'CANCELLED' ? 0.6 : 1 }}`. Per CONTEXT decision #8 mapping: CANCELLED is faded.
       - For NO_SHOW bars, `opacity: 0.75`.
       - Keep `data-status` attribute for future CSS hooks and analytics.

    4. **Date header row**:
       - Container: `bg-warm-cream border-b border-warm-line`
       - Day cell default: `font-mono text-xs text-ink-2 px-2 py-2 text-center border-r border-warm-line`
       - Today's column: `bg-terracotta-tint text-terracotta-deep font-medium`
       - If there is a "today" boolean per column, conditionally apply the today classes via template literal.

    5. **Room row headers** (leftmost column):
       - Container: `bg-warm-white border-r border-warm-line` (sticky if already sticky — keep)
       - Room number text: `font-mono text-sm text-ink-1`
       - Status dot indicator (if present): replace any Tailwind palette `bg-*-500` with `var(--status-*)` via inline style or `bg-status-occupied`-style class (only if those tokens exist as Tailwind utilities; if not, use inline style).

    6. **Empty cells** (no reservation):
       - `bg-warm-white border-r border-b border-warm-line` (default grid cell)
       - On hover (if drag-to-create is supported): `hover:bg-warm-cream`
       - Selected cell: `bg-terracotta-tint border-2 border-terracotta` (when the user is mid-selection)

    7. **Apply the token migration map** for any remaining v1.0 tokens:
       - `bg-surface-elevated` → `bg-warm-cream`
       - `bg-surface` → `bg-warm-white`
       - `text-text-secondary` → `text-ink-2`
       - `text-text-primary` → `text-ink-1`
       - `text-text-muted` → `text-ink-3`
       - `border-border-subtle` → `border-warm-line`
       - `bg-brand-primary/10` → `bg-terracotta-tint`
       - `text-brand-primary` → `text-terracotta`
       - `hover:bg-blue-50` → `hover:bg-warm-cream`
       - `focus:ring-brand-primary` → `focus:ring-terracotta`

    **DO NOT**:
    - Modify any data-fetching logic (`useReservations`, React Query).
    - Modify drag-to-create handlers, click-to-edit callbacks, or scroll virtualization.
    - Change keyboard event handlers.
    - Remove existing `aria-*` attributes.
    - Replace inline style if doing so breaks dynamic status mapping (CSS variables via inline style is the locked approach).
  </action>
  <verify>
    <automated>cd apps/web; pnpm tsc --noEmit -p tsconfig.json &amp;&amp; rg "#[0-9a-fA-F]{3,6}" src/features/reservations/components/RoomRackTable.tsx src/features/reservations/RoomRackCalendar.tsx | (! grep .) &amp;&amp; rg "bg-(blue|green|gray|yellow|red|amber|orange)-[0-9]" src/features/reservations/components/RoomRackTable.tsx src/features/reservations/RoomRackCalendar.tsx | (! grep .)</automated>
  </verify>
  <acceptance_criteria>
    - `rg "#[0-9a-fA-F]{3,6}" apps/web/src/features/reservations/components/RoomRackTable.tsx apps/web/src/features/reservations/RoomRackCalendar.tsx` returns 0 matches.
    - `rg "bg-(blue|green|gray|yellow|red|amber|orange|sky|emerald|lime|stone|zinc|neutral|slate)-[0-9]" apps/web/src/features/reservations/components/RoomRackTable.tsx` returns 0 matches.
    - `rg "from '@/lib/status-colors'" apps/web/src/features/reservations/components/RoomRackTable.tsx` returns exactly 1 match.
    - `rg "RESERVATION_STATUS_TO_CSS\\[" apps/web/src/features/reservations/components/RoomRackTable.tsx` returns at least 1 match.
    - `rg "font-mono" apps/web/src/features/reservations/components/RoomRackTable.tsx` returns at least 2 matches (header + room number).
    - `rg "bg-terracotta-tint" apps/web/src/features/reservations/components/RoomRackTable.tsx` returns at least 1 match (today's column or selected cell).
    - `rg "outline-terracotta" apps/web/src/features/reservations/components/RoomRackTable.tsx` returns at least 1 match (hover state).
    - `pnpm --filter web tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>RoomRackTable renders the bundle calendar look with status-tokened bars, font-mono numerics, and hover/select states.</done>
</task>

<task type="auto">
  <name>Task 2: Regression — confirm reservations tests still pass</name>
  <files>(verification only)</files>
  <read_first>
    - apps/web/src/features/reservations/ (look for any .test.tsx files via Glob)
    - Final state of RoomRackTable.tsx and RoomRackCalendar.tsx
  </read_first>
  <action>
    Run the reservations test directory (if any tests exist; per RESEARCH line 118, there are no test files for RoomRackTable in v1.0, but other reservations tests may exist for the wizard or service layer that should remain green):

    ```
    pnpm --filter web vitest run src/features/reservations/
    ```

    If 0 tests are collected, that's acceptable (no tests for this directory yet). If any tests exist, all must pass.
  </action>
  <verify>
    <automated>cd apps/web; pnpm vitest run src/features/reservations/ --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter web vitest run src/features/reservations/ --passWithNoTests` exits 0.
    - Any pre-existing reservation tests still pass.
  </acceptance_criteria>
  <done>INT-03 restyle complete, no regressions.</done>
</task>

</tasks>

<verification>
1. `cd apps/web && pnpm tsc --noEmit -p tsconfig.json` → exits 0
2. `rg "#[0-9a-fA-F]{3,6}" apps/web/src/features/reservations/components/RoomRackTable.tsx apps/web/src/features/reservations/RoomRackCalendar.tsx` → 0 matches
3. `rg "text-(gray|blue|red|green|yellow|amber|orange|sky|emerald|lime|stone|zinc|neutral|slate)-[0-9]" apps/web/src/features/reservations/components/RoomRackTable.tsx apps/web/src/features/reservations/RoomRackCalendar.tsx` → 0 matches
4. `pnpm --filter web vitest run src/features/reservations/ --passWithNoTests` → exits 0
5. Manual (Wave 3): visit `/reservations` calendar → bars colored per status, today highlighted, hover shows terracotta outline, drag-to-create still works
</verification>

<success_criteria>
- Reservation bars color-coded via RESERVATION_STATUS_TO_CSS inline style.
- Today's column highlighted in terracotta-tint.
- Room numbers in Geist Mono.
- Hover/select states distinct.
- Existing calendar behaviors (drag-to-create, click-to-edit, scroll virtualization) preserved.
- Zero hex / zero Tailwind palette classes.
- INT-03 ROADMAP success criterion 3 satisfied.
</success_criteria>

<threat_model>
Frontend-only — no security boundary touched. Risk:
- **Inline style overrides hover outline**: outline is on a pseudo-state, fill is the base — no conflict.
- **CSS variable resolution outside .hos scope**: RoomRackCalendar renders inside StaffLayout which has `hos` class (per 11-01). Variables resolve correctly.
- **Status enum drift**: if the backend adds a new ReservationStatus value, `RESERVATION_STATUS_TO_CSS[status]` will return `undefined` and the bar will be transparent. Mitigation: add a fallback `?? 'var(--ink-4)'` in the inline style expression to render any unmapped status as muted grey instead of invisible.
</threat_model>

<output>
After completion, create `.planning/phases/11-internal-screens-restyle/11-04-SUMMARY.md` documenting the inline-style choice for dynamic status colors, the today-column highlight pattern, and any fallback applied for unknown ReservationStatus values.
</output>
