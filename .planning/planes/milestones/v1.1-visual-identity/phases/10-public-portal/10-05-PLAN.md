---
phase: 10-public-portal
plan: 05
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - apps/web/src/features/concierge/ConciergePage.tsx
  - apps/web/src/features/concierge/ChatMessage.tsx
  - apps/web/src/features/concierge/VenueCard.tsx
  - apps/web/src/layouts/PublicConciergeLayout.tsx
autonomous: true
requirements: [PUB-13]

must_haves:
  truths:
    - "Visiting `/concierge` renders the public chat with `bg-warm-paper` page background (NOT `bg-gray-50`)"
    - "User message bubbles use `bg-terracotta` + `text-warm-white` (NOT `bg-blue-600`)"
    - "Assistant message bubbles use `bg-warm-white` + `text-ink-1` + `border-warm-line` (NOT `bg-gray-100`)"
    - "Send button uses `bg-terracotta` hover `bg-terracotta-deep` (NOT `bg-blue-600`)"
    - "VenueCard titles use `font-display` (Instrument Serif)"
    - "PublicConciergeLayout applies `useForceLightTheme()` so dark-mode never leaks"
    - "Logic files (concierge.api.ts, concierge.store.ts, useConciergeChat.ts, streamMessages.ts) are NOT modified"
    - "Zero hex literals and zero text-blue/gray/red color utilities in the 4 concierge files (status banners keep amber/red — those are semantic, not brand)"
  artifacts:
    - path: "apps/web/src/features/concierge/ConciergePage.tsx"
      provides: "Restyled chat page — warm palette throughout, terracotta send button"
    - path: "apps/web/src/features/concierge/ChatMessage.tsx"
      provides: "Restyled message bubbles — terracotta user, warm-white assistant"
    - path: "apps/web/src/features/concierge/VenueCard.tsx"
      provides: "Restyled venue cards — font-display titles, terracotta primary action"
    - path: "apps/web/src/layouts/PublicConciergeLayout.tsx"
      provides: "Restyled layout — warm-paper bg, terracotta logo mark, useForceLightTheme call"
  key_links:
    - from: "PublicConciergeLayout"
      to: "useForceLightTheme"
      via: "hook call at top of component body"
      pattern: "useForceLightTheme\\(\\)"
    - from: "Send button"
      to: "terracotta token"
      via: "Tailwind utility"
      pattern: "bg-terracotta"
---

<objective>
Apply the bundle's warm palette + Instrument Serif typography to the four concierge UI files. PURELY VISUAL — no logic changes, no streaming/SSE/RAG touch. The chat must continue functioning byte-for-byte the same.

Purpose: PUB-13 makes the public concierge surface match the new portal identity. Today the chat uses `bg-blue-*` / `bg-gray-*` / `text-blue-*` — those are v1.0 leftovers. Phase 9 tokens replace them.

Output: Four files edited. Logic files (concierge.api.ts, concierge.store.ts, useConciergeChat.ts, streamMessages.ts, types.ts) are NOT touched.
</objective>

<execution_context>
Wave 2 — parallel with 10-02, 10-03, 10-04. Only depends on 10-01 (for `useForceLightTheme`). Disjoint file ownership — touches `features/concierge/` and `layouts/PublicConciergeLayout.tsx`, none of which are touched by other wave-2 plans.
</execution_context>

<context>
@.planning/phases/10-public-portal/10-CONTEXT.md
@.planning/phases/10-public-portal/10-RESEARCH.md
@.design-fetch/hotelos-ai/project/screens/portal.jsx
@apps/web/src/features/concierge/ConciergePage.tsx
@apps/web/src/features/concierge/ChatMessage.tsx
@apps/web/src/features/concierge/VenueCard.tsx
@apps/web/src/layouts/PublicConciergeLayout.tsx
@apps/web/src/features/public-portal/hooks/useForceLightTheme.ts

<interfaces>
<!-- The complete restyle delta table is in 10-RESEARCH.md lines 369-420. -->
<!-- Summary mapping (current → target): -->

| Concept | Current | Target |
|---------|---------|--------|
| Page bg | `bg-gray-50` | `bg-warm-paper` |
| Card bg | `bg-white` | `bg-warm-white` |
| Card border | `border-gray-200` | `border-warm-line` |
| Primary text | `text-gray-900` / `text-gray-800` | `text-ink-1` |
| Secondary text | `text-gray-700` / `text-gray-600` | `text-ink-2` |
| Tertiary text | `text-gray-500` | `text-ink-3` |
| Faint text | `text-gray-400` | `text-ink-4` |
| Primary action bg | `bg-blue-600` | `bg-terracotta` |
| Primary action hover | `hover:bg-blue-700` | `hover:bg-terracotta-deep` |
| Primary action text | `text-white` | `text-warm-white` |
| Accent fill (icons) | `bg-blue-50` / `text-blue-600` | `bg-terracotta-tint` / `text-terracotta-deep` |
| Star fill (rating) | `fill-amber-400 text-amber-400` | `fill-mustard text-mustard` |
| Empty star | `text-gray-300` | `text-ink-4` |
| Type badge | `bg-blue-50 text-blue-700` | `bg-terracotta-tint text-terracotta-deep` |
| Focus ring | `focus:ring-blue-500` | `focus:ring-terracotta` |
| User bubble | `bg-blue-600 text-white` | `bg-terracotta text-warm-white` |
| Assistant bubble | `bg-gray-100 text-gray-800` | `bg-warm-white text-ink-1 border border-warm-line` |
| Streaming dots | `bg-gray-400` | `bg-ink-4` |
| Photo placeholder bg | `bg-gray-100` | `bg-warm-cream` |
| Logo mark bg | `bg-blue-600` | `bg-terracotta` |

PRESERVE (do NOT change — semantic colors):
- Error red: `border-red-200 bg-red-50 text-red-700` / `text-red-800`
- Warning amber: `border-amber-200 bg-amber-50 text-amber-800`
- Disabled state: `disabled:opacity-50 disabled:cursor-not-allowed`

NEW import in PublicConciergeLayout:
```typescript
import { useForceLightTheme } from '@/features/public-portal/hooks/useForceLightTheme';
```
Call `useForceLightTheme()` as the FIRST line of the component body.

NEW class on VenueCard `<h3>` (the venue name): replace `text-sm font-semibold text-gray-900` with `font-display text-base text-ink-1` (size bump from `text-sm` to `text-base` matches the bundle's serif weight).
</interfaces>
</context>

<threat_model>
Purely visual. Concierge SSE, JWT-less CSRF flow, rate limiting, prompt-injection defense — ALL untouched (logic files excluded from `files_modified`). New `useForceLightTheme` call adds no security surface (DOM attribute manipulation only).
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Restyle PublicConciergeLayout + ConciergePage (page + chrome)</name>
  <files>
    apps/web/src/layouts/PublicConciergeLayout.tsx,
    apps/web/src/features/concierge/ConciergePage.tsx
  </files>
  <read_first>
    - `.planning/phases/10-public-portal/10-RESEARCH.md` lines 369-388 (full restyle table for ConciergePage)
    - `.planning/phases/10-public-portal/10-RESEARCH.md` lines 411-420 (full restyle table for PublicConciergeLayout)
    - `apps/web/src/layouts/PublicConciergeLayout.tsx` (current implementation — 47 lines, simple)
    - `apps/web/src/features/concierge/ConciergePage.tsx` (current implementation — 173 lines, more complex)
    - `apps/web/src/features/public-portal/hooks/useForceLightTheme.ts` (the hook signature)
  </read_first>
  <action>
    **A. `apps/web/src/layouts/PublicConciergeLayout.tsx`** — apply 5 class replacements + add hook call.

    Add the hook import at the top (after the React import):
    ```typescript
    import { useForceLightTheme } from '@/features/public-portal/hooks/useForceLightTheme';
    ```
    Inside the function body, FIRST line:
    ```typescript
    useForceLightTheme();
    ```
    Then replace the class strings:
    - Root container: `min-h-screen bg-gray-50 flex flex-col` → `hos min-h-screen bg-warm-paper flex flex-col`
      (Note: add `hos` class so token CSS vars are available even when layout is rendered outside StaffLayout)
    - Header: `border-b border-gray-200 bg-white sticky top-0 z-10` → `border-b border-warm-line bg-warm-white sticky top-0 z-10`
    - Logo mark div: `w-7 h-7 rounded-full bg-blue-600 flex items-center justify-center` → `w-8 h-8 rounded-full bg-terracotta flex items-center justify-center`
    - Logo `<span>` text "H": `text-white text-xs font-bold` → `text-warm-white text-xs font-display`
    - Hotel name label: `font-semibold text-gray-900 text-sm sm:text-base` → `font-display text-ink-1 text-base sm:text-lg`
    - Location label: `ml-auto text-xs text-gray-400 hidden sm:block` → `ml-auto text-xs text-ink-4 hidden sm:block`

    **B. `apps/web/src/features/concierge/ConciergePage.tsx`** — class-only replacements (no logic changes). Apply the mappings from the interfaces table:

    | Line area | Before | After |
    |-----------|--------|-------|
    | Empty state icon container (`w-14 h-14 ... bg-blue-50`) | `bg-blue-50` | `bg-terracotta-tint` |
    | Empty state icon (`MessageCircle ... text-blue-600`) | `text-blue-600` | `text-terracotta-deep` |
    | Empty state heading (`text-lg font-semibold text-gray-800`) | `font-semibold text-gray-800` | `font-display text-ink-1` |
    | Empty state paragraph (`text-sm text-gray-500`) | `text-gray-500` | `text-ink-2` |
    | Suggestion chip | `border-gray-200 bg-white px-4 py-3 text-sm text-gray-700 hover:bg-gray-50 hover:border-blue-300` | `border-warm-line bg-warm-white px-4 py-3 text-sm text-ink-2 hover:bg-warm-cream hover:border-terracotta-soft` |
    | Status banners (amber + red) | UNCHANGED — keep `border-amber-200 bg-amber-50 text-amber-800` and `border-red-200 bg-red-50 text-red-700/800` |
    | Form container | `border-t border-gray-200 bg-white` | `border-t border-warm-line bg-warm-white` |
    | Textarea | `border-gray-200 bg-gray-50 text-gray-900 placeholder:text-gray-400 focus:ring-blue-500 focus:border-transparent` | `border-warm-line bg-warm-paper text-ink-1 placeholder:text-ink-4 focus:ring-terracotta focus:border-transparent` |
    | Send button | `bg-blue-600 ... hover:bg-blue-700 ... text-white` | `bg-terracotta ... hover:bg-terracotta-deep ... text-warm-white` |

    Do NOT change: `useEffect` calls, `useQuery` config, `handleSend`, `handleSubmit`, `handleKeyDown`, store accessors. Only the className strings change.

    The page is currently 173 lines — diff should be class-string edits only, line count should remain ~173.
  </action>
  <verify>
    <automated>cd apps/web && pnpm tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/layouts/PublicConciergeLayout.tsx` contains `useForceLightTheme()` call
    - `PublicConciergeLayout.tsx` root container has `bg-warm-paper` (not `bg-gray-50`) AND `hos` class
    - `PublicConciergeLayout.tsx` logo mark div has `bg-terracotta` (not `bg-blue-600`)
    - `rg "(bg-blue-|bg-gray-|text-gray-|text-blue-|hover:bg-blue-)" apps/web/src/layouts/PublicConciergeLayout.tsx` returns 0 matches
    - `apps/web/src/features/concierge/ConciergePage.tsx` send button has `bg-terracotta` and `hover:bg-terracotta-deep`
    - `ConciergePage.tsx` user textarea uses `focus:ring-terracotta` not `focus:ring-blue-500`
    - `rg "bg-(blue|gray)-[0-9]+|text-(blue|gray)-[0-9]+|hover:bg-(blue|gray)-[0-9]+" apps/web/src/features/concierge/ConciergePage.tsx` returns 0 matches
    - `rg "(border-amber|bg-amber|text-amber|border-red|bg-red|text-red)" apps/web/src/features/concierge/ConciergePage.tsx | wc -l` ≥ 4 (status banners preserved — at least 4 status-color tokens stay)
    - `rg "#[0-9a-fA-F]{3,6}" apps/web/src/layouts/PublicConciergeLayout.tsx apps/web/src/features/concierge/ConciergePage.tsx` returns 0
    - `pnpm tsc --noEmit` exits 0
    - Logic preserved: `rg "useQuery|useEffect|handleSend|handleSubmit|sendMessage" apps/web/src/features/concierge/ConciergePage.tsx | wc -l` ≥ 5 (chat behavior intact)
  </acceptance_criteria>
  <done>
    Layout + page restyled. Dark mode prevented. Status banners semantically preserved. Logic untouched.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Restyle ChatMessage + VenueCard (the data-driven chat content)</name>
  <files>
    apps/web/src/features/concierge/ChatMessage.tsx,
    apps/web/src/features/concierge/VenueCard.tsx
  </files>
  <read_first>
    - `.planning/phases/10-public-portal/10-RESEARCH.md` lines 388-410 (ChatMessage + VenueCard restyle tables)
    - `apps/web/src/features/concierge/ChatMessage.tsx` (current implementation)
    - `apps/web/src/features/concierge/VenueCard.tsx` (current implementation)
  </read_first>
  <action>
    **A. `apps/web/src/features/concierge/ChatMessage.tsx`** — class replacements:

    | Element | Before | After |
    |---------|--------|-------|
    | User bubble | `bg-blue-600 text-white rounded-br-sm` | `bg-terracotta text-warm-white rounded-br-sm` |
    | Assistant bubble | `bg-gray-100 text-gray-800 rounded-bl-sm` | `bg-warm-white text-ink-1 border border-warm-line rounded-bl-sm` |
    | Streaming dots | `bg-gray-400` | `bg-ink-4` |
    | Error box | UNCHANGED — keep `border-red-200 bg-red-50 text-red-700` |

    **B. `apps/web/src/features/concierge/VenueCard.tsx`** — class replacements:

    | Element | Before | After |
    |---------|--------|-------|
    | Card wrapper | `rounded-2xl border border-gray-200 bg-white shadow-sm` | `rounded-2xl border border-warm-line bg-warm-white shadow-sm` |
    | Photo placeholder bg | `bg-gray-100` | `bg-warm-cream` |
    | Venue name `<h3>` | `text-sm font-semibold text-gray-900` | `font-display text-base text-ink-1` |
    | Type badge | `bg-blue-50 text-blue-700` | `bg-terracotta-tint text-terracotta-deep` |
    | Rating text | `text-gray-500` | `text-ink-3` |
    | Filled star | `fill-amber-400 text-amber-400` | `fill-mustard text-mustard` |
    | Empty star | `text-gray-300` | `text-ink-4` |
    | Distance text | `text-gray-500` | `text-ink-3` |
    | Address text | `text-gray-400` | `text-ink-4` |
    | "Cómo llegar" primary action | `bg-blue-600 hover:bg-blue-700 text-white` | `bg-terracotta hover:bg-terracotta-deep text-warm-white` |
    | Secondary action button | `border-gray-200 bg-white text-gray-700 hover:bg-gray-50` | `border-warm-line bg-warm-white text-ink-2 hover:bg-warm-cream` |

    NO logic changes. Only className edits. The component prop signatures, conditional rendering branches, and event handlers stay exactly as-is.
  </action>
  <verify>
    <automated>cd apps/web && pnpm tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/features/concierge/ChatMessage.tsx` user bubble uses `bg-terracotta` (verify `rg "bg-terracotta" apps/web/src/features/concierge/ChatMessage.tsx | wc -l` ≥ 1)
    - `ChatMessage.tsx` assistant bubble uses `bg-warm-white` AND `border-warm-line`
    - `ChatMessage.tsx` preserves the error box red palette: `rg "(border-red|bg-red|text-red)" apps/web/src/features/concierge/ChatMessage.tsx | wc -l` ≥ 1 (errors stay semantic)
    - `rg "bg-(blue|gray)-[0-9]+|text-(blue|gray)-[0-9]+|fill-amber-[0-9]+" apps/web/src/features/concierge/ChatMessage.tsx` returns 0
    - `apps/web/src/features/concierge/VenueCard.tsx` venue name `<h3>` uses `font-display` — verify `rg "font-display" apps/web/src/features/concierge/VenueCard.tsx | wc -l` ≥ 1
    - `VenueCard.tsx` rating stars use `fill-mustard text-mustard` (not `fill-amber-400`)
    - `VenueCard.tsx` primary action button uses `bg-terracotta` and `hover:bg-terracotta-deep`
    - `rg "bg-(blue|gray)-[0-9]+|text-(blue|gray|amber)-[0-9]+|fill-amber-[0-9]+|hover:bg-(blue|gray)-[0-9]+" apps/web/src/features/concierge/VenueCard.tsx` returns 0
    - `rg "#[0-9a-fA-F]{3,6}" apps/web/src/features/concierge/ChatMessage.tsx apps/web/src/features/concierge/VenueCard.tsx` returns 0
    - `pnpm tsc --noEmit` exits 0
    - Logic preserved: `VenueCard.tsx` still exports `VenueCard` and still renders the same prop fields (verify `rg "props\\.|venue\\." apps/web/src/features/concierge/VenueCard.tsx | wc -l` is non-zero)
  </acceptance_criteria>
  <done>
    ChatMessage and VenueCard restyled. font-display applied to venue titles. Concierge feature folder is zero-hex.
  </done>
</task>

</tasks>

<verification>
- `pnpm tsc --noEmit` → exit 0
- `rg "#[0-9a-fA-F]{3,6}" apps/web/src/features/concierge/ --glob "*.tsx"` → 0 hits (zero hex in the whole concierge folder)
- `rg "bg-(blue|gray)-[0-9]+|text-(blue|gray)-[0-9]+|hover:bg-(blue|gray)-[0-9]+" apps/web/src/features/concierge/ --glob "*.tsx" apps/web/src/layouts/PublicConciergeLayout.tsx` → 0 hits
- `rg "useForceLightTheme" apps/web/src/layouts/PublicConciergeLayout.tsx` → ≥ 1 hit
- Logic files untouched: `git diff --stat apps/web/src/features/concierge/concierge.api.ts apps/web/src/features/concierge/concierge.store.ts apps/web/src/features/concierge/useConciergeChat.ts apps/web/src/features/concierge/streamMessages.ts apps/web/src/features/concierge/types.ts` should show 0 modifications
- Manual smoke: open `/concierge`, send a test message, confirm streaming still works and venue cards render with the new palette
</verification>

<success_criteria>
PUB-13 (concierge public chat restyled with warm-paper bg, terracotta send button, Instrument Serif VenueCard titles, no logic changes) satisfied. Dark-mode leak prevented via `useForceLightTheme`.
</success_criteria>

<output>
After completion, create `.planning/phases/10-public-portal/10-05-SUMMARY.md` documenting:
- Four files restyled (4 file edits, ~30 class-string replacements total)
- Logic files untouched (list them)
- Decision: status banners (amber warning, red error) keep their semantic colors — they are NOT brand surfaces
- Decision: `hos` class added to PublicConciergeLayout root for token CSS-var availability
- useForceLightTheme integration confirmed
</output>
